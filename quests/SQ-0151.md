---
title: Clean up 19 pre-existing `cargo doc -p app --no-deps` broken-intra-doc-link warnings (doc-gen only; cargo build/test stay clean
status: done
type: feature
priority: low
created: 2026-07-03T15:27:33Z
completed: 2026-07-03T15:27:33Z
commits: []
context: '[TODO-fd29ca] Clean up 19 pre-existing `cargo doc -p app --no-deps` broken-intra-doc-link warnings (doc-gen only; cargo build/test stay clean — the lint is warn, not deny). Three kinds: (a) ~13 false positives where prose `[...]` is read as a doc-link — TOML section names (`[keymap]`/`[symbols]`/`[search]`/`[hotkeys]` in config.rs:9,16,51,108,159,436 + symbols.rs:4) and UI button labels (`[X]`/`[Done]` in input.rs:803, gallery.rs:19, hints_panel.rs:30, verbmenu.rs:64) — fix by escaping (code span `` `[keymap]` `` or `\[keymap\]`); (b) ~4 real links to private items (colors.rs:9 resolve_base, map.rs:432 VRect, style.rs:730 colors::resolve_base, style.rs:1144 style_to_decl) — harmless, target exists but private; (c) ~4 genuinely mis-pathed type links worth fixing (style.rs:115,128 `config::SymbolConfig` — wrong name/path; style.rs:727,1144 `SymbolSet` — unresolvable in doc scope). [docs/cleanup; cargo doc warnings] — completed 2026-07-02'
tags:
  import: babelmap
  source_file: COMPLETED.md
---
