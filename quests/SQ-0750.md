---
title: A side band is rasterised wholesale, so the frame's outer edge mixes font glyphs with a scaled bitmap of the same character
status: confirm
type: bug
priority: low
created: 2026-08-10T03:06:41Z
commits:
  - d309aad4e5ac849bb3ff48c1d15d4e3eeec63b11
context: |-
  branch: main
  head: 531d2dc8
  cwd: /Volumes/Videos/Source/babelmap
  current: SQ-0708

  Measured out of SQ-0747 while chasing Journey's mismatched frame edges; splitting it out because the fix reaches Zork Zero's and Shogun's flanks, where the border genuinely is artwork.
---

--- note 2026-08-10T03:06:55Z ---

MEASURED (SQ-0747's second pass, Journey/Amiga, hybrid). User's report: "we are still using unmatched lines on the outside edges of the journey frame."

Tally of every character in the frame's outer column over its full row span, at 100x71 (same shape at 98x69, 110x51, 138x51):

    left col 0        count
    U+250C / U+2514   1 each   (corners)
    U+2502  '|'       6        (the menu-strip rows)
    U+2584  block     1        (band boundary leak)
    space             61       (the story rows)

One continuous vertical line, THREE producers. Box-drawing glyphs on the six menu-strip rows plus the corners; an uploaded IMAGE over the ~61 story rows, blank in the cell buffer because `decompose_chrome_strips` makes every side band a single `Art` strip wholesale — so the `|` the game printed on every story row is rasterised rather than drawn as text; and block elements leaking at the boundary between the two.

In a real terminal that is the user's font `|` sitting directly above a bitmap of the same character scaled by a non-integer factor. They cannot match in weight or antialiasing however precisely they are aligned, which is exactly what "unmatched lines" describes.

DISTINCT from SQ-0747's corner defect (the top border rounds its columns while `flank_divider_extension` floors and ceils the same native pixel). That is a POSITION error; this is a GLYPH/renderer-family error. Fixing either leaves the other.

WHY IT WAS LEFT: `flank_divider_extension` also serves Zork Zero's and Shogun's flanks, where the band genuinely IS artwork and rasterising it is right. A fix needs a measured "is this run text?" test applied per band, plus corpus proof across those titles, not a blanket change. Check IbmPc too — it draws these dividers as reverse-video spaces, so it may not exercise the same path.

--- note 2026-08-10T04:33:36Z ---

THE PRINCIPLE, from the user 2026-08-09: "difference seems to be how the frame is drawn (line border vs reverse space)".

That is the correct framing and it supplies the rule this quest was missing.

BOTH profiles draw the frame as TEXT. IbmPc prints reverse-video spaces; Amiga prints box-drawing glyphs. Neither is artwork. The current code rasterises every side band wholesale into a single `Art` strip — and that is lossy for characters, but the loss is INVISIBLE for reverse-video spaces, because a solid filled block survives being scaled by a non-integer factor and still looks like a solid filled block. A thin box-drawing stroke does not: it lands between pixels, changes weight, and antialiases against a font glyph drawn from the same character on the adjoining rows. That is precisely why nobody noticed until the Amiga profile arrived; IbmPc's particular input masked the defect for years.

SO THE RULE IS NOT "handle Amiga differently". It is: **a border the game printed as characters should be drawn as characters, whichever characters they are.** A band is art only when it is actually artwork. That is the measured "is this run text?" test this quest already called for — the user has now supplied the principle behind it.

Note the architectural direction this confirms. An audit of `crates/app/src` found exactly ONE branch on the profile variant in the whole app (`interpreter.rs:79`, mapping an interpreter number to a profile); every other Amiga/IbmPc mention in `render/`, `session.rs`, `graphics.rs`, `colors.rs`, `loop_tick.rs`, `reset.rs` and `startup.rs` is a comment. The profile is a data bundle, not a code path. Any fix here must keep it that way — no `if amiga` in the decomposition. The discriminator is what the band CONTAINS, not which profile is loaded.

Corpus reach is unchanged and still the reason for care: Zork Zero's and Shogun's flanks genuinely ARE artwork, and must keep being rasterised.

--- note 2026-08-10T05:46:09Z ---

USER 2026-08-10: "also, we are mixing the reverse space into the amiga line drawing."

So the Amiga frame contains BOTH kinds of border at once — reverse-video spaces (the IbmPc idiom) interleaved with the box-drawing glyphs the Amiga game actually prints. That is a third distinct failure on this edge, alongside the glyph-vs-raster split already measured here and SQ-0747's corner position error.

WHY IT MATTERS: under the Amiga profile the game prints box glyphs and nothing else. A reverse-video space appearing anywhere in that frame is therefore NOT the game's output — it is ours. Something in the chrome path is generating an IbmPc-style border rather than passing through what the game drew. Find what emits it. Candidates worth checking, in no particular order and none of them confirmed: `menu_flank_panel`'s fill, the divider extension replicating a native row, or a generic app-chrome border style applied regardless of profile.

This sharpens the rule already recorded on this quest. It is not only that a text border must be drawn as text rather than rasterised — it is that the characters drawn must be **the game's own**. We should never be choosing border characters at all.

Note the profile audit (2026-08-09): exactly ONE branch on the profile variant exists in the whole app (`interpreter.rs:79`), and the render path never asks which profile is loaded. So this cannot be an `if ibmpc` emitting reverse spaces — it is either unconditional, or driven by profile DATA reaching shared code. Establish which.

--- note 2026-08-10T06:10:36Z ---

FIFTH PASS — the reverse-space emitter is FOUND, FIXED and falsified. Commit d309aad4.

## WHAT EMITS IT

`flank_divider_extension` + `draw_chrome_band_stretched`, and it is unconditional — no profile branch anywhere, exactly as the audit predicted. The extension carries a flank's border down the reclaimed gap as a ONE-PIXEL-TALL slice of the chrome canvas, and `draw_chrome_band_stretched` RESIZES that crop to fill the band. So the crop's width against the band's width IS the extension's horizontal magnification. The crop was `(dnx0, mid, dnx1 - dnx0, 1)` — the border's INK alone.

- A border printed as a reverse-video SPACE inks its whole 8-pixel text cell, so ink width == cell width and the stretch comes out at the letterbox scale. IBM PC was right by accident.
- A `│` glyph's stroke is **ONE pixel** inside its 8-pixel cell. Magnification 16.00x against a 2.03x scale at the user's pane. Both vertical borders became SOLID FILLED BLOCKS two and one terminal columns wide, for their whole height.

That is the reverse-space idiom, generated by us, standing in the same line as the box glyphs the top, bottom and menu rows draw as crisp text. The user's exact words.

## THE MEASUREMENT THAT SETTLES 'IT IS OURS'

Journey/Amiga at the party menu, every run in the menu band: **style=00 on all of them** (the one exception is `Cast`, style=10 — bold, not reverse). Frame drawn with ┌ ─ ┐ │ └ ┘ ▌. The same frame under IBM PC: style=01 blank runs at x=121/249/377/505 on every row — reverse-video spaces. And the rendered Amiga CELL buffer contains zero REVERSED cells in either honour mode. So every reverse-video-looking thing in the Amiga frame came out of the band path, not the game and not the cell path.

Cell evidence at pane 163x61 (the user's), cell 8x18, halfblocks harness, BEFORE:
    cols 66,67 rows 3..48: fg == bg == Rgb(220,220,220)   (left border, 2 cells solid)
    col  162    rows 3..48: fg == bg == Rgb(220,220,220)   (right border, 1 cell solid)
AFTER: 24,24,24 / 39,39,39 / 46,46,46 — a stroke averaged into the panel ground, no filled cell anywhere.

## THE FIX

The crop is now the native columns the band's CELLS cover — `inv_x(dcell0)..inv_x(dcell1)`, the same inversion `flank_crop` already uses, widened (never narrowed) so the ink survives either rounding. Magnification becomes the letterbox scale, like every other pixel in the ring. No profile test added; the discriminator stays what the band CONTAINS.

## CORPUS — the fix does not reach the flanks this quest was deferred over

Only Journey runs this code at all: `divider_exts` is gated on `BottomPlan::Menu`, and measured across the corpus at 163x61 / 138x68 / 100x71 in both honour modes —

| title | plan | flank dividers | frame digest |
|---|---|---|---|
| Journey (Amiga) | menu | 2 | CHANGED (the fix) |
| Journey (IBM PC) | menu | 2 | identical at 163x61 and 138x68; corrected at 100x71 (a milder 1.6x over-magnification of the same kind) |
| Zork Zero | frame | 0 | byte-identical |
| Shogun | frame | 0 | byte-identical |
| Arthur | extend | 0 | byte-identical |
| advent | extend | 0 | byte-identical |
| mysterious01 | extend | 0 | byte-identical |

## FALSIFICATION (verbatim, crop reverted to the ink alone, record left in place)

> `Amiga honor=true pane 163x61: the left flank border is magnified 16.00x while the ring's letterbox scale is 2.03x — the crop must be the native columns the band's CELLS cover, not the ink alone, or a glyph's thin stroke is inflated into a solid filled bar (ext (66, 3, 2, 46), crop (259, 152, 1, 1))`

> `Amiga honor=true: 138 filled cell(s) in the frame's side borders — Journey prints this frame with box-drawing glyphs and no reverse-video run at all, so a solid block standing in its line is ours, not the game's. the left flank border column 66 is a solid filled block (Rgb(220, 220, 220)) at row 3`

## STILL OPEN ON THIS QUEST — the original glyph-vs-raster split

The side border is now a STROKE of the right width, but it is still a scaled bitmap of a `│` sitting above and below font `│` glyphs (the top rule, the menu rows). At 8x18 with s=2.03 the bitmap stroke is ~2 device px against a font stroke of ~1. Narrower than the 16 px it was, and no longer reads as a reverse space, but it is the same renderer-family mismatch this quest was opened for. Drawing it as the game's own characters is still the fix, still needs a per-band 'is this run text?' test, and is still not a rider on a lane about the crop.

## FOUND, NOT FIXED — the flank's OUTER border is not drawn at all

Measured under BOTH profiles at 163x61: pane columns 1..2, rows 3..48, carry only `menu_flank_panel`'s panel fill (Rgb(34,34,34)) — no border. The frame's outer-left edge simply does not exist between the `┌` on row 1 and the `└` on row 55. Cause: under the Menu plan the flank band is not drawn as art at all; `menu_flank_panel` floods the strip and draws ONLY the picture's bounding box (SQ-0547), and the outer border column lies outside that box. The inner divider survives because `flank_divider_extension` redraws it. A SQ-0547 regression, symmetric with SQ-0747's item (A) (the same fill running two columns PAST the inner rule), and the honest reading of 'unmatched lines on the outside edges': one edge is font glyphs, one is a bitmap, and one is nothing.
