---
title: Zork0 post-move display lands in function-key screen; deeper v6 window-record path incomplete
status: done
type: feature
priority: low
created: 2026-07-23T01:40:02Z
completed: 2026-07-23T04:58:55Z
commits: []
context: |-
  branch: main
  head: bb4a4d2b
  cwd: /Volumes/Videos/Source/babelmap
  current: SQ-0186

  Found by Lane F after fixing the movement fault: post-move display iterates the game's window-record fallback template ~16k times and enters a keypress-driven function-key screen headlessly; full intro playability needs the deeper v6 window-record/picture-window behaviour.
---

--- note 2026-07-23T03:42:38Z ---

ROOT CAUSE (Lane G, Opus): the fkey screen is V-DEFINE (input.zil), reached as a PHANTOM dispatch, not by player command. Chain: after intro, Zork0's interp-6 event-driven main loop (MAIN → 0xf678 → 0xf680 → 0xee6c → 0xf1f0 → 0xf300) zeroes its window-record array at 0x6da9 (copy_table #6da9,#00,#22), then 0xf300 walks the static templates at 0xb1df to RECONSTRUCT the current event. In our engine the walk yields empty records → PRSA (g4c = word[0x6db1]) = 0 → PERFORM(0) → dispatch table word[ge8+0] = V-DEFINE → soft-lock in its read_char loop. The phantom @restore prompt on the same path is the same empty-record symptom.

DISPROVEN: the newline interrupt (props 8/9) — implemented per §8.8.3.2.2, verified firing, V-DEFINE still entered; routine 14411 is a 3-instruction stub (storeb 0x6cb4,#00,#3c; set_margins 0,0) whose write is redundant. DISPROVEN as mitigation: interpreter number 2 avoids V-DEFINE but boots the TEXT-mode variant (no graphical layout, extra Y/N gates) — loses all graphics. Interp 1/3/4/6 all hit the phantom path in our engine.

REAL FIX (next effort): model enough of the interp-6 windowed-input subsystem that 0xf300's walk re-derives the typed command into the record array. Command parse itself is correct (tokenise('look') → dict 0x9994 verified). Divergence is in what the walk reads back — needs instruction-level tracing of the walk's memory reads (PCs known) and/or a Frotz-source oracle for interp-6 v6 input behavior (read-opcode terminator store, timed input, window props consumed during record building). Prior agent's ZIL + analysis artifacts in session scratchpad.
