---
title: Give pre-colour games a period look for their machine, kept distinct from the sourced default pair
status: done
type: feature
priority: low
created: 2026-08-14T21:13:23Z
completed: 2026-08-19T16:37:09Z
commits:
  - f0bba4c39de0cfe5f1df9b4d65548d0215181568
  - 6fb4dcedb1949ea52200f5dafd7d58e9d165f298
  - 807f7555964d1d32eb32d488947978458fd59ab4
  - 5aefdc6e22ffc1e167c57af8c0da02639a6129a8
  - 7e98aad946388032048359ccec9908d6e03bb5a0
context: |-
  branch: main
  head: d918c64b
  cwd: /Volumes/Videos/Source/babelmap
  current: SQ-0863

  Agreed with the user after establishing that colour arrives with v5, so a v1-v4 story has no colour concept at all and anything shown is presentation rather than a fact the story is told.
---

--- note 2026-08-14T21:13:45Z ---

THE DISTINCTION THIS QUEST RESTS ON, and it must not blur:

**Colour arrives with Version 5.** `set_colour` is v5+, and so are the `$2C`/`$2D` default-colour header bytes. So a **v1–v4** story has no colour concept at all — it never sets, never reads, never branches. (The user asked about "v3"; the real boundary is v1–v4, since v4 is equally pre-colour.)

- For **v5/v6** games that never call `set_colour`, what we do today is already right and is NOT cosmetic: the default pair is something the story can read and is what an inherited colour resolves to. SQ-0846 is the proof — Zork Zero on the Macintosh never calls `set_colour` at all, so the machine's white page under black ink IS the screen's meaning. Every value there is sourced from Infocom's own code: `mac/xzip.lst`'s `SetColor := (zWHITE*256) + zBLACK; { Mac defaults: white under black }`, `zboot.asm`'s 9 and 2 for the Apple, `st/stx1.s` for the ST. **Do not touch that path.**
- For **v1–v4** there is no such fact. Anything painted is the interpreter's presentation.

THE SOURCING PROBLEM, which is why this is a separate field and not an extension of `default_colours()`:

Infocom's **v3 interpreters had no colour support**, so there is no equivalent line to quote. "What Zork I looked like on a C64" is the MACHINE's text mode — the KERNAL's light blue on dark blue — not anything Infocom wrote. That is genuinely weaker provenance than everything else in the profile table.

**And do not simply reuse `default_colours()` for v1–v4.** The Amiga's 12/9 came from Infocom's v5-era interpreter; assuming the v3 ZIP made the same choice is exactly the kind of plausible-but-unsourced inference this project keeps getting caught by. If the v3 interpreter's behaviour can be sourced, source it; if only the hardware default can be, say so and name it as hardware.

THE DESIGN, agreed with the user:

- A **separate field** beside `default_colours()`, so a future reader can tell which values are Infocom's and which are ours. Two different kinds of claim, two different fields.
- **Sourced or declined**, per machine — the same standard as the rest of the table. A machine whose period look cannot be sourced simply has none, exactly as `AppleIIgs::std_window` and `IbmPc::interpreter_number` decline.
- **Default ON in babelmap**, which paints a pane it owns and where the medium was chosen deliberately — opening Zork I off an `.adf` and having it look like an Amiga is the point.
- **Default OFF in `zvm-cli`**, which writes into a terminal belonging to the user — the same reasoning that makes `IbmPc::default_colours` return `None` so that "default" means what the player actually sees.
- A **config key** either way, so the look can be declined without also losing game-set colours. Check how it composes with `honor_game_colours`, `--no-game-colours` (SQ-0855), `NO_COLOR`, and SQ-0860's monochrome-artwork force-off — it is a fourth thing in that neighbourhood and must not fight them.

SCHEDULING: build this AFTER SQ-0872 centralises the machine table, not before. The period look is a column in that table; adding it first means doing it twice and in the wrong crate.

--- note 2026-08-14T21:18:00Z ---

SOURCING METHOD — the user's, and better than the "documented hardware defaults" fallback in my previous note. Prefer this.

**Boot the original disk images in an emulator and observe.** The corpus now holds self-booting media for most of these machines, and self-booting means the disk carries INFOCOM'S OWN INTERPRETER — so what you see is not "a C64's text mode" but what Infocom's v3 interpreter chose to do on that machine. That is exactly the source I said did not exist in code, and it exists in behaviour.

Media already in `stories/` that boot:
- `Planetfall r29 (clean copy from retail disk).dsk` — Apple II, RWTS boot code, v3 (SQ-0868)
- `TRINITY1.D64` / `TRINITY2.D64`, `Hitchhikers_Guide_to_the_Galaxy_The_1984_Infocom.d64` — Commodore, v4 and v3 (SQ-0869)
- The Apple `.po`/`.2mg` images carry `INFOCOM.SYSTEM` and `INFOCOM`
- `.adf` Amiga floppies, `.st` Atari compilations, `Zork Zero Disk.image` and `InfocomMasterpieces.img` for the Mac

THREE CAVEATS, without which the evidence is not trustworthy:

1. **Name the emulator AND its palette.** VICE alone ships Pepto, Colodore and its own default, and they disagree visibly on the same pixels. A screenshot with no provenance is unreproducible, and this project pins values for years.
2. **Capture raw — no CRT shader, no scanlines, no NTSC composite.** Those change colours substantially. The Apple II is the extreme case: its text was commonly green or amber phosphor on one monitor and white on another, so part of "the Apple II look" is a MONITOR choice rather than a machine fact. Say which you are recording.
3. **Cross-check two independent emulators where possible.** Agreement on the RGB is strong evidence. **Disagreement is itself the finding** — and probably means declining rather than picking a side, per this quest's rule that a machine whose period look cannot be sourced simply has none.

Record for each machine: emulator, version, palette setting, video standard (PAL/NTSC where it applies), the disk booted, and the observed foreground/background as RGB. That provenance goes in the code beside the values, matching the standard the rest of the profile table already meets (`st/stx1.s`, `zboot.asm`, `mac/xzip.lst`).

Note this also gives a way to CHECK the v5+ values we already ship: booting the Macintosh Zork Zero should show white under black, and the Amiga floppies their 12/9. If an emulator contradicts a value we sourced from Infocom's assembly, that is worth knowing loudly.

--- note 2026-08-15T07:23:49Z ---

FIRST DATA POINT — Commodore 64, from the user, by this quest's own preferred method.

An emulator screenshot of `stories/Hitchhikers_Guide_to_the_Galaxy_The_1984_Infocom.d64` booted and played to the first prompt (Bedroom, "It is pitch black."). Measured off the PNG rather than eyeballed — the image contains EXACTLY three colours and nothing else:

  page                        #6C6C6C   (108,108,108)   91,570 px
  ink                         #FFFFFF                    5,535 px
  cursor + reversed status    #000000                    2,255 px

The third is not a third default: black is the status line's reverse video and the cursor, which is §8.2 behaviour. The pair this quest wants is **page #6C6C6C, ink #FFFFFF**.

`#6C6C6C` is a plausible exact match for VIC-II colour 12 (grey) in at least one common palette, but PROVENANCE IS STILL MISSING and caveat 1 of the note above forbids recording it without: emulator name, version, palette setting (Pepto / Colodore / VICE default disagree visibly), and PAL vs NTSC. Ask the user for those before this goes into the table. The screenshot is 368x270, which is a cropped border-inclusive frame rather than the bare 320x200, so the capture settings are not inferable from the file.

NOT USABLE AS A SECOND DATA POINT: a photograph of Bureaucracy running on a real C128 (interpreter 7) was also supplied. Measured: page black `(16,0,0)`-dominant, ink clustering `(0,80,128)`..`(80,176,208)` — i.e. green at ~85% of blue, so cyan-leaning and NOT §8.3.1's blue. It is good enough to REFUTE something (see below) and not good enough to source a constant: caveat 2 rules out CRT capture outright, and a photographed CRT plus JPEG cannot separate the VDC's cyan from its light blue. Ask for a C128 emulator capture of `TRINITY1.D64` instead — Trinity is the C128 release (confirmed below), so it is the exact analogue of the C64 capture already in hand.

WHAT THE C128 PHOTO DOES SETTLE, and it is worth acting on independently of this quest: `zvm::interpreter::MACHINES`'s Commodore 128 row declines `default_colours` on the stated ground that "the machine's famous light-blue-on-blue boot screen is the hardware's reputation rather than the interpreter's evidence" (SQ-0869). The photo shows blue-ish ink on a BLACK page. Whatever the pair turns out to be, that sentence is now false and should be rewritten.

CONFIRMED WHILE CHECKING: `stories/TRINITY1.D64` really does carry a **C128** interpreter, not a C64 one — `STA $FF00` x43 (MMU configuration register) and `STA $D506` (MMU RAM configuration), both C128-only. So the corpus already holds bootable media for interpreter 7 as well as 8.

DEAD END, recorded so nobody repeats it: the C128 constant cannot be read out of that disk statically. Across both 175 KB images there are ZERO stores to the VDC ($D600/$D601), zero PETSCII colour codes sent through the five `JSR $FFD2` sites, and no `$D021`/`$D020` writes. Only the boot/loader is stored plainly — its message table sits at offsets 428..9800 ("The story is loading ...", "Insert Side * of the STORY disk into Drive #1.") — and the interpreter proper is not plainly present. TRINITY2.D64 has no plain 6502 at all. Emulator observation is the route, exactly as this quest already says.

ONE DESIGN POINT CONFIRMED BY THE MEASUREMENT: the C64's page is a mid-grey, and §8.3.1's numbers 2..9 contain no grey at all (the greys are 10..12 and `clamp_default_colour` accepts those only when `version == 6`). So this genuinely cannot be expressed as a `default_colours` pair for any C64 game, all of which are pre-v6 — which is independent confirmation that the separate field this quest specifies is the right shape, and that reusing `default_colours()` would have silently clamped the C64's page to black.

--- note 2026-08-15T07:35:44Z ---

SECOND DATA POINT — Commodore 128 (interpreter 7), from the user, VICE.

`vice-screen-2026081500331205.png`, 856x288, Trinity booted to the first prompt (Palace Gate). Trinity is v4, so pre-colour — the case this quest is about. Measured off the PNG; the frame contains EXACTLY two colours:

  page   #000000                  230,408 px
  ink    #55FFFF  (85,255,255)     16,120 px

The status bar is those same two reversed (cyan ground, black characters), so as with the C64 there is no third default — reverse video is §8.2 behaviour.

`#55FFFF` is exactly RGBI **light cyan**: the VDC drives four digital bits and the primary+intensity encoding puts every channel at 0, 85, 170 or 255, so 85/255/255 is a canonical entry rather than a near miss. **This makes interpreter 7 markedly better sourced than interpreter 8** — the VIC-II's palette is analogue-derived, which is exactly why Pepto, Colodore and VICE's default disagree on the C64's grey, while the VDC's RGBI leaves far less room for emulators to differ. The C64 value still needs its palette named; this one is close to palette-independent.

IT ALSO VINDICATES THE CRT PHOTO, and the way it does is worth keeping. The photo of the same machine measured green at ~0.85 x blue, which was enough to say "cyan-leaning, NOT §8.3.1's blue" and not enough to name a value. The emulator gives G = B = 255 exactly. So the photo pointed the right way and the CRT plus JPEG shifted the magnitude — which is caveat 2 working as written, not an argument for relaxing it.

STILL OUTSTANDING FOR THE RECORD (caveat 1): VICE **version** for both captures, the VDC palette setting for this one, and the VIC-II palette setting plus PAL/NTSC for the C64 one. Caveat 3's second independent emulator is outstanding for both.

THE TWO COMMODORE ROWS, as measured:

  7  Commodore 128   page #000000   ink #55FFFF   (VICE, Trinity, v4)
  8  Commodore 64    page #6C6C6C   ink #FFFFFF   (VICE?, Hitchhiker's, v3)

And note they are NOT the same, which is itself a finding: the same publisher's interpreter on two closely related machines chose a black page with cyan ink on one and a grey page with white ink on the other. Anything that assumed "the Commodore look" was one thing would have been wrong.

--- note 2026-08-15T07:37:10Z ---

CORRECTION to the C64 data point above — the user caught it, and it changes the shape of the field this quest specifies.

I recorded "the third is not a third default: black is the status line's reverse video and the cursor". **That is wrong.** I had sampled row 6, which is border, and inferred the rest. Re-measured row by row, the C64 frame is:

  rows 35..42   STATUS BAND   ground #000000 BLACK (~305 of 368 px/row), characters #6C6C6C GREY
  rows 43..185  BODY          ground #6C6C6C grey,                        characters #FFFFFF WHITE
  row  186      CURSOR        #000000 black

So the status line is NOT the body pair reversed — a true reverse of grey-on-white would be a WHITE ground with grey characters. It uses black, which the body never uses as a ground, and drops white entirely. (It is consistent with the status WINDOW holding black ink on the inherited grey page and then reverse-videoing that, which lands on grey ink over black. Either way the observable is the same.)

THE C128 IS GENUINELY DIFFERENT HERE, not just in palette:

  7  C128   body #000000 / #55FFFF   status = EXACT reverse (cyan ground, black chars)   2 colours total
  8  C64    body #6C6C6C / #FFFFFF   status = BLACK ground, GREY chars                   3 colours total

WHAT THIS MEANS FOR THE DESIGN. A period look expressed as a single (page, ink) pair cannot express the C64, and the C64 is only the second machine measured. The field needs to carry the status band as well — either its own pair, or a rule, and the two machines in hand already disagree about which. Do not settle that shape until a third machine is measured; the Apple II (`Planetfall r29 ... .dsk`) is the obvious next one and its status line is the thing to look at hardest.

METHOD NOTE, since this is the second time in this quest a first reading was too coarse: census the frame ROW BY ROW and find where each colour actually occurs, rather than sampling a row and generalising. The whole-image colour count told me there were three colours and I assigned the third by assumption instead of by position. The row census takes one extra command and would have said "black owns rows 35..42 as a GROUND" outright.

--- note 2026-08-15T07:38:56Z ---

THIRD FINDING — the CURSOR is part of the period look, and it is not a reverse-video block. User's observation, then measured on both captures.

  C64  (Hitchhiker's, v3)   cursor at y=186, x 72..79   8 px wide, 1 px tall, colour #000000 BLACK
  C128 (Trinity,      v4)   cursor at y=238, x 120..127 8 px wide, 1 px tall, colour #55FFFF (the ink)

A character cell is 8 px wide, so both are **an underscore: one cell wide, one scanline tall, on the cell's bottom row**. A reverse-video block cursor would be a solid 8x8 of the ink colour, which is what a naive "invert the cell" implementation gives and is what babelmap would draw today. Neither Infocom Commodore interpreter does that.

On the C128 the `>` prompt glyph occupies rows 231..237 and the cursor sits in the NEXT cell at row 238 — i.e. it is a separate character position, not an overlay on the character under it.

SHAPE AGREES ACROSS THE TWO MACHINES; COLOUR DOES NOT:

  C64   cursor BLACK — neither its page (#6C6C6C) nor its ink (#FFFFFF). Black is used in
        exactly two places on that screen: the status band's ground and this cursor.
  C128  cursor is the ink colour, #55FFFF.

So the cursor needs BOTH a shape and a colour in whatever this quest ships, and the colour cannot be derived from the page/ink pair — the C64 proves it, since its cursor colour appears nowhere in its body pair.

RUNNING TABLE OF MEASURED MACHINES (all values exact, off PNGs, row-censused):

  #  machine        body ground  body ink   status ground  status chars  cursor
  7  Commodore 128  #000000      #55FFFF    #55FFFF        #000000       #55FFFF, 8x1 underscore
  8  Commodore 64   #6C6C6C      #FFFFFF    #000000        #6C6C6C       #000000, 8x1 underscore

Three distinct sub-decisions per machine, then, not one pair: the body, the status band, and the cursor. Two machines and the body and status relationships already differ (C128's status is an exact reverse of its body; the C64's is not), so this is not a case where one rule will fall out of a third sample — expect the field to carry all three explicitly and to be declinable per machine, per this quest's sourced-or-declined standard.

STILL OUTSTANDING: VICE version for both captures; VDC palette for the C128; VIC-II palette and PAL/NTSC for the C64 (its #6C6C6C is the one value here that a palette choice can move — the C128's RGBI cannot). Caveat 3's second independent emulator for both. And a third machine before the shape of the field is settled — `Planetfall r29 (clean copy from retail disk).dsk` (Apple II, v3) is the obvious next, with its status band and cursor the things to measure hardest.

--- note 2026-08-15T07:45:00Z ---

FOURTH DATA POINT — Apple II (Planetfall r29, v3), from the user, an Apple II emulator. Row-censused per the method note above, not sampled.

  size 560x384 (80-column 560x192 at 1x2)
  status band  rows 0..15   ground #FFFFFF WHITE, characters #000000 black
  body         rows 16+     ground #000000 black,  ink #FFFFFF WHITE
  cursor       x 7..13, y 290..303 — 7 px wide, 14 rows tall, #FFFFFF

CAPTURE ARTEFACTS, not colours: #7A7A7A (417 px), #9E9E9E (79), #8F8F8F (16), #454545 (1) — 513 px out of 215,040, all edge pixels. The frame is two colours. Recording this because the whole-image census reports six and only a row census tells you which two are real; that is the same trap the C64 note warns about.

THE CURSOR IS A BLOCK, AND THAT BREAKS THE PATTERN. A cell here is 7 px wide (560/80) and 16 rows tall (8 scanlines x 2); the cursor fills 7 x 14 of it. That is a solid reverse-video block, the OPPOSITE of both Commodores' one-scanline underscores. So cursor shape is genuinely per-machine and not a Commodore-versus-everyone split — which settles the user's point that it has to be an option rather than a constant.

THE STATUS BAND IS AN EXACT REVERSE, like the C128 and unlike the C64. Two of three machines reverse; the C64 does not. Still no rule; still carry it explicitly.

** THIS ALSO CROSS-CHECKS A VALUE WE ALREADY SHIP, AND IT AGREES. ** The Apple rows in `zvm::interpreter::MACHINES` carry `APPLE_DEFAULT_BACKGROUND = 2` (black) and `APPLE_DEFAULT_FOREGROUND = 9` (white), sourced from Infocom's `zboot.asm`. The emulator shows exactly black page under white ink. That is the check the second note above asked for — "booting the Macintosh Zork Zero should show white under black, and the Amiga floppies their 12/9" — run on the Apple and passing. The Mac and Amiga checks are still outstanding.

MONITOR CAVEAT APPLIES HERE AND ONLY HERE. This quest's own note flags the Apple II as the extreme case: its text was commonly green or amber on one monitor and white on another, so part of "the Apple II look" is a MONITOR choice, not a machine fact. This capture is white-on-black, i.e. a white-phosphor/RGB monochrome rendering. Whatever ships must say that it is recording the white-monitor look and not claim the machine had no other.

RUNNING TABLE — three machines, all values exact, all row-censused:

  #   machine        body ground  body ink  status ground  status chars  cursor
  2/9/10  Apple II   #000000      #FFFFFF   #FFFFFF        #000000       #FFFFFF  7x14 BLOCK
  7   Commodore 128  #000000      #55FFFF   #55FFFF        #000000       #55FFFF  8x1 underscore
  8   Commodore 64   #6C6C6C      #FFFFFF   #000000        #6C6C6C       #000000  8x1 underscore

WHAT THE THIRD SAMPLE SETTLES ABOUT THE FIELD'S SHAPE: it needs THREE things per machine and none of them derives from the others.
  - body pair — the C64's grey page is not expressible in §8.3.1's 2..9 at all, so this must be RGB, not colour numbers.
  - status band — 2 of 3 are an exact reverse and the C64 is not, so it cannot be computed.
  - cursor, as SHAPE and COLOUR separately — shape splits 2 underscore / 1 block, and colour is the ink on two machines and a third colour on the C64.
Declinable per machine throughout, per this quest's sourced-or-declined standard.

STILL OUTSTANDING: emulator NAME and version for this capture (the filename says only "appleii"); VICE version for the two Commodore ones; VDC palette for the C128; VIC-II palette and PAL/NTSC for the C64. Caveat 3's second independent emulator for all three. Amiga and Macintosh captures, which double as cross-checks on `mac/xzip.lst` and the Amiga's 12/9.

--- note 2026-08-15T07:57:39Z ---

FIFTH DATA POINT — Amiga (Nord and Bert, r19/870722, v3). NOT SOURCE-GRADE, but it carries a finding this quest predicted, so record it and then go get a clean one.

The story's own banner names the machine: "Infocom Interpreter 4 Version C" — interpreter 4 is the Amiga.

Measured, after snapping to the Workbench 1.x pens (see the provenance problem below):

  body           ground #0055AA BLUE, ink #FFFFFF white   (y=70 across the text: 191 blue to 46 white)
  status band    rows 24..31, WHITE ground
  chrome         rows 0..9 white (the "Workbench Screen" title bar), 10..12 blue, 13..22 white

`#0055AA` is (0,85,170), which is exactly Workbench 1.x pen 0: 4-bit $05A widened by bit replication. Not a near miss.

** IT CONTRADICTS THE ASSUMPTION THIS QUEST WARNED AGAINST, WHICH IS THE POINT. ** The first note says: "Do not simply reuse `default_colours()` for v1-v4. The Amiga's 12/9 came from Infocom's v5-era interpreter; assuming the v3 ZIP made the same choice is exactly the kind of plausible-but-unsourced inference this project keeps getting caught by." The table's Amiga row is 12/9 — a GREY page under white ink. The v3 interpreter here draws a BLUE page under white ink. Different, and by more than a shade. Whatever period look the Amiga gets, it is not its `default_colours` pair.

THREE REASONS THIS IS NOT YET A SOURCED CONSTANT:

1. **The file is lossily compressed or rescaled.** The colour census returns thousands of near-duplicates — #0052A9, #0054AA, #0F5FAF, #EBF2F8, #F0F5FA — where a real Amiga frame has at most a handful of exact pens. About 72k of 81,920 pixels snap to the four WB1.x pens and the rest is fuzz. The DOMINANT values land exactly on the pens, so the identification holds; the file does not meet caveat 2.
2. **No provenance at all** — "official screenshot", so no emulator, no version, no palette, and it may be a press-kit or manual image rather than a capture.
3. **It is a WINDOW ON THE WORKBENCH SCREEN**, not the interpreter's own screen — the title bar says so. Workbench 1.x's default pens are exactly blue/white/black/orange, so this may be the interpreter inheriting the desktop's pens rather than choosing anything. That is the same class of caveat as the C64's KERNAL colours, and it is the difference between "what Infocom's Amiga ZIP chose" and "what a window on Workbench looks like". A capture of the interpreter on its OWN screen (or on a Workbench with non-default pens, which would separate the two immediately) is what settles it.

WHAT TO ASK FOR: an Amiga emulator capture of an `.adf` in `stories/` — raw, named emulator and version, and ideally with the Workbench palette changed from default so the question in (3) answers itself. If the game still draws blue on white with a recoloured Workbench, the pens are Infocom's; if it follows the desktop, they are Workbench's and the Amiga's period look is "whatever the user's Workbench is", which is a legitimate finding and would mean DECLINING the row rather than inventing one.

RUNNING TABLE — four machines now, the Amiga marked as provisional:

  #        machine        body ground  body ink  status ground  status chars  cursor
  2/9/10   Apple II       #000000      #FFFFFF   #FFFFFF        #000000       #FFFFFF 7x14 BLOCK
  4        Amiga (v3) *   #0055AA      #FFFFFF   #FFFFFF        (unmeasured)  (unmeasured)
  7        Commodore 128  #000000      #55FFFF   #55FFFF        #000000       #55FFFF 8x1 underscore
  8        Commodore 64   #6C6C6C      #FFFFFF   #000000        #6C6C6C       #000000 8x1 underscore

  * provisional: lossy file, no provenance, and possibly Workbench's pens rather than Infocom's.

And note the Amiga is now the SECOND machine whose period look differs from its sourced `default_colours` — the C64's grey page cannot be expressed as a §8.3.1 number at all, and the Amiga's v3 blue is not its v5 grey. Two of four. The separate field this quest specifies is not a nicety.

--- note 2026-08-15T07:58:07Z ---

CORRECTION to caveat 3 of the Amiga note, from the user: running the game in a window on the Workbench screen is HOW PEOPLE ACTUALLY RAN INFOCOM GAMES ON THE AMIGA. So "it is a window on Workbench, not the interpreter's own screen" is not a reason to distrust the capture — it is a description of the authentic thing. I had it filed as a disqualification; it is the subject.

What survives of that caveat is narrower and is a labelling question, not a validity one: if #0055AA is Workbench 1.x's default pen 0 rather than something Infocom set, then the Amiga's period look is "the default Workbench", and a player who had recoloured their desktop saw something else. That is still worth knowing — but it argues for how the value is DESCRIBED in the table (as the default-Workbench look), not for declining it. The overwhelming majority saw the default.

The two caveats that do still stand and still need a clean capture: the file is lossily compressed (thousands of near-duplicate colours where an Amiga frame has four pens), and it has no provenance — no emulator, version or palette.

So the ask narrows usefully. An Amiga emulator capture of an `.adf` from `stories/`, raw, emulator and version named, launched from Workbench the way it was played. The recoloured-Workbench experiment is now optional and only tells us whether to write "Infocom's pens" or "the default Workbench's pens" beside the value — it no longer decides whether the value belongs in the table.

--- note 2026-08-19T04:02:52Z ---

SIXTH DATA POINT — Macintosh (interpreter 3), from the user. `machine-screenshots/mac-zork1.jpg`, committed in 396ba326 along with six other Mac captures.

Zork I r88 / serial 840726 — v3, so the pre-colour case this quest is about — at the first prompt (West of House, ">look around" typed).

USABLE DESPITE BEING JPEG, and here is the check rather than the assumption. The capture is 640x480 with the emulator surround at #333333; the screen rectangle inside it censuses to **511x342**, which is the Mac Plus's 512x342 (one column lost to the crop threshold). Within it there are 59 distinct colours, but they are #FFFFFF, #000000 and a ringing gradient between them at glyph edges — a 1-bit screen, so JPEG cannot invent a third *colour*, only soft edges. That is materially different from the Amiga capture's problem, where thousands of near-duplicates sat around four *distinct* pens and any one of them could have been shifted. Row-censused per the method note.

  body          ground #FFFFFF WHITE, ink #000000 black
  status line   rows 45..61 of the screen, ground #FFFFFF WHITE, characters #000000 black
  chrome        rows 0..13 the Mac menu bar; 19 a rule; 20..25 and 30..40 the window
                title bar's stripe pattern; 26, 44, 62, 64 solid black rules

** THE STATUS BAND IS A FOURTH PATTERN — it is not reversed at all. ** Running tally: Apple II and C128 reverse exactly; the C64 uses a black ground with grey characters that is neither its body pair nor a reverse of it; the Macintosh does not distinguish the status line by ground at all, separating it with RULES instead. Four machines, four behaviours. This is now strong confirmation that the status band cannot be computed from the body pair and must be carried explicitly — and that the field needs to express "separated by a rule, same ground" as well as a ground/ink pair.

** IT ALSO RUNS THE CROSS-CHECK THE SECOND NOTE ASKED FOR, AND IT PASSES. ** That note said "booting the Macintosh Zork Zero should show white under black". `mac/xzip.lst`'s `SetColor := (zWHITE*256) + zBLACK; { Mac defaults: white under black }` is what `zvm::interpreter::MACHINES` ships for the Macintosh row, and the v3 interpreter here draws exactly white page under black ink. So unlike the Amiga — whose v3 blue page contradicts its v5-sourced 12/9 — the Macintosh's period look and its `default_colours` AGREE. Two of five machines diverge, three do not; the separate field is still needed, but it is not the case that pre-colour always differs.

CURSOR NOT MEASURED. The frame has ">look around" typed with the caret at the end of the line, and at this crop I did not isolate it from the adjacent glyph. Needs a capture with the cursor on an empty prompt line, as the C64 and C128 ones had. That is the one thing missing from the Macintosh row.

PROVENANCE: `mac-shogun-bw.jpg` carries "Mini vMac 3.3.3 Plus" in its window title, and all four monochrome captures share the same 512x342 screen and #333333 surround, so they are the same emulator — but that is inference across files and the user should confirm it for the Zork I capture specifically. Mini vMac's Plus emulation is 1-bit, so there is no palette question here at all; this row is palette-independent in the way the C128's RGBI is and the C64's VIC-II is not.

THE OTHER SIX CAPTURES are Zork Zero, Shogun, Journey and Arthur — v6, so outside this quest, but `mac-zorkzero.jpg`/`mac-zorkzero-bw.jpg` and `mac-shogun.jpg`/`mac-shogun-bw.jpg` are the SAME FRAME in colour and monochrome, which is a fixture pair worth knowing about for the v6 render work.

RUNNING TABLE — five machines:

  #        machine        body ground  body ink  status ground  status chars  cursor
  2/9/10   Apple II       #000000      #FFFFFF   #FFFFFF        #000000       #FFFFFF 7x14 BLOCK
  3        Macintosh      #FFFFFF      #000000   #FFFFFF        #000000       (unmeasured)
  4        Amiga (v3) *   #0055AA      #FFFFFF   #FFFFFF        (unmeasured)  (unmeasured)
  7        Commodore 128  #000000      #55FFFF   #55FFFF        #000000       #55FFFF 8x1 underscore
  8        Commodore 64   #6C6C6C      #FFFFFF   #000000        #6C6C6C       #000000 8x1 underscore

  * provisional: lossy file, no provenance.

SEPARATE OBSERVATION worth folding into SQ-0872's table work rather than filing alone: `zvm::interpreter::MACHINES` has NO Commodore 64 row at all. It carries 2, 3, 4, 5, 6, 7, 9, 10 — interpreter 8 is absent, while this quest has had a measured C64 period look since the first data point. Whatever ships here needs somewhere to put it.

--- note 2026-08-19T04:07:23Z ---

AMIGA ROW RE-MEASURED AND NO LONGER PROVISIONAL. The fifth note's caveats were about a file that is not in the repo.

The user states that every `machine-screenshots/amiga-*.png` was captured by them, directly out of an emulator, from the `.adf` files in `stories/`. The fifth note above measured a *Nord and Bert* image — "official screenshot", lossily compressed, thousands of near-duplicate colours — and that file is not in `machine-screenshots/`. Its caveats were carried forward onto the Amiga row and should not have been: they never applied to the captures that are actually committed.

MEASURED, on the two v3 titles, which are the pre-colour case this quest is about:

  amiga-spellbreaker.png   721x484   Spellbreaker r87 / 860904, Council Chamber
  amiga-lurking.png        719x485   The Lurking Horror, at the first prompt

Both give an IDENTICAL palette, and both are 7-8 exact colours over ~350k pixels — no interpolation, no ringing, nothing like the discarded file:

  #074BA1   page          (317,738 px in Spellbreaker)
  #FFFFFF   ink            (29,646)
  #FF7E1C   CURSOR            (128) = exactly 8x16
  #DB1B23 / #FFC7A5         (88 / 48)  the Workbench mouse pointer
  #000000 / #032651 / #01001E          window chrome and shadow

So three of the eight colours are the interpreter's and the rest are the desktop's — which is the cleanest separation of the two that this quest has had on any machine.

CURSOR: #FF7E1C, x 55..62, y 420..435 — **8 px wide, 16 rows tall, a solid block**, 128 px exactly. Same position and size on both titles. Like the C64's black cursor, its colour is NEITHER the page nor the ink, which is now two machines out of five where the cursor colour cannot be derived. Shape splits 3 block (Apple II, Amiga) / 2 underscore (C64, C128).

** STATUS BAND — A FIFTH BEHAVIOUR, AND THE ONE NO SINGLE PAIR CAN EXPRESS. ** It is an exact reverse of the body (white ground, blue characters), but ONLY BEHIND THE TEXT. Row-censused at y=75, across the full width:

  x   1..42    blue        (window border)
  x  47..182   WHITE       "Council Chamber" reversed
  x 183..558   blue        376 px of PAGE between the two runs
  x 559..680   WHITE       "Score: 0/0" reversed
  x 683..719   blue

Identical structure on `amiga-lurking.png` with its own text widths. So the status line is not a band at all — it is reverse video applied per run.

Five machines, five behaviours, none derivable from the body pair:
  Apple II, C128   full-width exact reverse
  Commodore 64     full-width, black ground / grey chars — neither the body pair nor its reverse
  Macintosh        no distinguishing ground; separated by RULES
  Amiga            exact reverse, per text run, page showing between

THE ONE CAVEAT THAT SURVIVES, and it is caveat 1, not caveat 2. `#074BA1` is **not** a bit-replicated 4-bit value — OCS registers widen to 0x00, 0x11, ... 0xFF, and 0x07/0x4B/0xA1 are none of those. Workbench 1.x pen 0 is register $05A, which bit-replicates to #0055AA, and (7,75,161) sits consistently darker than (0,85,170) on both channels. So this emulator is applying a gamma or scaling curve of its own, and a second emulator would very likely report a different RGB for the same register. That makes the Amiga the second row after the C64 where the palette choice can move the value, and the ask is narrow: **emulator name and version, and its palette/colour setting**. The raw register is almost certainly $05A; whether the table should record #0055AA (the register, bit-replicated) or #074BA1 (what this emulator draws) is a decision this quest has to make once, for the C64 as well.

RUNNING TABLE — five machines, the Amiga now measured rather than provisional:

  #        machine        body ground  body ink  status                      cursor
  2/9/10   Apple II       #000000      #FFFFFF   full reverse                #FFFFFF 7x14 block
  3        Macintosh      #FFFFFF      #000000   no ground; rules            (unmeasured)
  4        Amiga          #074BA1 *    #FFFFFF   reverse, per text run       #FF7E1C 8x16 block
  7        Commodore 128  #000000      #55FFFF   full reverse                #55FFFF 8x1 underscore
  8        Commodore 64   #6C6C6C      #FFFFFF   full, #000000 / #6C6C6C     #000000 8x1 underscore

  * emulator's rendering of what is almost certainly register $05A; see caveat above.

AND THE FINDING THE FIFTH NOTE GOT RIGHT SURVIVES INTACT, now on solid evidence: the Amiga's v3 period look is a BLUE page under white ink, and its `default_colours` row is 12/9 — a GREY page under white ink, sourced from Infocom's v5-era interpreter. Different, and by more than a shade. The warning in this quest's first note against reusing `default_colours()` for v1-v4 is confirmed on the machine it was written about.

STILL OUTSTANDING FOR THE AMIGA: emulator name, version, palette setting. Nothing else.

--- note 2026-08-19T04:10:39Z ---

MACINTOSH CURSOR MEASURED — a third shape, and the previous note's "unmeasured" was my failure to look, not a missing capture. The user pointed out the cursor is visible and is a thin line; it is.

`mac-zork1.jpg`, tail of the typed line `>look around`, intensity-mapped:

        x=148 ......... 166
  y=377  ...........#.......
  y=380  ..........##.......
  y=382  .##....#####.......   <- the 'd' of "around"
  y=385  ...#..#...##.......
  y=388  ...#...#####.......
  y=392  ...........#.......

The single column at x=159 runs y=377..392: **1 px wide, 16 rows tall** — the full line height, sitting in the gap AFTER the last glyph rather than over a cell. That is the Macintosh text insertion caret.

CROSS-CHECKED on the two colour captures, which are a different Mac model and a different font size. Scanning for isolated vertical black runs 12-20 rows tall with no black neighbours on either side, each frame contains EXACTLY ONE:

  mac-arthur.jpg     x=64  y=297  13 rows tall
  mac-zorkzero.jpg   x=90  y=274  13 rows tall

One candidate per frame, in both cases at the prompt. Three captures, two machines, same form.

CURSOR SHAPES NOW SPLIT THREE WAYS, and none of the three is what a naive "invert the cell under the cursor" would draw:

  bar         Macintosh      1 x line-height, BETWEEN cells (16 rows Mac Plus, 13 colour)
  block       Apple II       7 x 14  (of a 7 x 16 cell)
              Amiga          8 x 16, colour #FF7E1C
  underscore  Commodore 64   8 x 1 on the cell's bottom row, colour #000000
              Commodore 128  8 x 1, colour #55FFFF

RUNNING TABLE — five machines, all three sub-decisions measured on four of them:

  #        machine        body ground  body ink  status                    cursor
  2/9/10   Apple II       #000000      #FFFFFF   full reverse              #FFFFFF 7x14 block
  3        Macintosh      #FFFFFF      #000000   no ground; rules          #000000 1xLH bar
  4        Amiga          #074BA1      #FFFFFF   reverse, per text run     #FF7E1C 8x16 block
  7        Commodore 128  #000000      #55FFFF   full reverse              #55FFFF 8x1 underscore
  8        Commodore 64   #6C6C6C      #FFFFFF   full, #000000 / #6C6C6C   #000000 8x1 underscore

--- USER DECISION: THE PROVENANCE CAVEATS ARE RELAXED ---

The user's instruction: "just go with what we have and don't worry about the specific emulator versions and whatnot."

So caveat 1 of the second note (record emulator, version, palette setting, video standard) and caveat 3 (cross-check a second independent emulator) are **deliberately set aside for this quest**, by the person who took the captures. Recorded here rather than dropped silently, because the rest of the profile table meets a documented-source standard (`st/stx1.s`, `zboot.asm`, `mac/xzip.lst`) and a future reader must be able to see that this field does not, and that it was a choice.

WHAT THAT MEANS CONCRETELY:

- The table records **what the emulator drew**, not the hardware register. The two values a palette can move are the Amiga's #074BA1 (register $05A bit-replicates to #0055AA; this emulator renders it darker) and the C64's #6C6C6C. Both go in as measured.
- The field's doc comment must say so in a line: these are observed from emulator captures in `machine-screenshots/`, not read out of Infocom's source, and they are the period LOOK rather than a fact the story can read. That is the distinction the first note said must not blur, and it now carries the whole weight of separating this field from `default_colours()`.
- Three machines are palette-independent anyway and can be stated flatly: the Macintosh (Mac Plus is 1-bit), the C128 (VDC RGBI, exact 0/85/170/255), and the Apple II (monochrome, with the white-monitor caveat that already stands).

STILL GENUINELY MISSING, and only these: **Atari ST (interpreter 5)** and **IBM PC (interpreter 6)** have no capture at all. Media for both is in `stories/` — `Infocom Compilation 1-9 (19xx)(-).st`, and the `.ima` floppies including `Hitchhiker's ... (r58, Serial 851002) ... .ima` which is v3. The IBM row may legitimately decline, since its look depends on the display adapter and that is already why `IbmPc::default_colours` returns `None`.

--- note 2026-08-19T04:21:25Z ---

THE TABLE NOW CARRIES THE MEASUREMENTS. `MachineProfile::period_look: Option<PeriodLook>` in `crates/zvm/src/interpreter.rs`, beside `default_colours` and deliberately not folded into it.

Shape, driven by the five-machine corpus rather than chosen up front:

    pub struct PeriodLook {
        pub page: (u8, u8, u8),
        pub ink: (u8, u8, u8),
        pub status: StatusBand,
        pub cursor_shape: CursorShape,
        pub cursor_colour: (u8, u8, u8),
    }
    pub enum StatusBand { FullReverse, PerRun, Own { ground, ink }, Ruled }
    pub enum CursorShape { Bar, Block, Underscore }

`(u8, u8, u8)` rather than a new Rgb type, matching `screen::rgb15_to_888` and `screen::grey_rgb`. `Own` carries its own pair because the C64's black-under-grey band is neither the body pair nor its reverse; it is unused by any current row and exists for interpreter 8 (see below).

ROWS FILLED: 2/9/10 Apple (shared `APPLE_PERIOD_LOOK`), 3 Macintosh, 4 Amiga, 7 Commodore 128.
ROWS DECLINED: 5 Atari ST and 6 IBM PC — no capture in `machine-screenshots/` at all. The IBM comment notes it may decline permanently, for the same reason its `default_colours` does.

THE APPLE FAMILY SHARES ONE MEASUREMENT, and that is a judgement worth flagging. The capture cannot say which of the IIe/IIc/IIgs ran it. The three rows already share `APPLE_DEFAULT_BACKGROUND`/`APPLE_DEFAULT_FOREGROUND` for the same reason — one Infocom interpreter, one 80-column text screen — so sharing here is consistent with how the table already treats the family rather than a new inference. Recorded in the constant's doc so a later reader can disagree with it knowingly.

TWO CORRECTIONS TO EXISTING TEXT, both now false and both fixed:
- The `default_colours` doc said the C128 declines because "the machine's famous light-blue-on-blue boot screen is the hardware's reputation rather than the interpreter's evidence". The measurement shows light CYAN on BLACK, which is neither. The decline STANDS — Trinity is v4, so it cannot fill a v5 default pair — but the reasoning is rewritten and now points at `period_look`.
- The `MACHINES` gap note on interpreter 8 said no Commodore interpreter had been read "for either machine's colours". Narrowed to "v5 default pair", with a pointer to the fact that the C64's period look IS measured. The C64 values (page #6C6C6C, ink #FFFFFF, `Own` band of black under grey, black 8x1 underscore) are recorded in `PeriodLook`'s own doc, since there is no row to put them on — a `.d64` is a 1541 image both Commodore machines read, so the medium cannot choose between 7 and 8. That absence is deliberate and pre-existing; my earlier note in this quest called it a gap to fold into SQ-0872, which was wrong.

PROVENANCE IS STATED IN THE TYPE, including what it is not. `PeriodLook`'s doc says outright that every value is observed from an emulator capture rather than read out of Infocom's source, that this is a lower standard than the rest of the table meets, and that it was the user's explicit decision. It names the two values a palette can move (the Amiga's #074BA1, where the register is almost certainly Workbench's $05A and would bit-replicate to #0055AA; and the C64's #6C6C6C) and the three that cannot (Mac Plus 1-bit, C128 RGBI, Apple II monochrome — with the white-phosphor caveat).

FOUR TESTS, pinning the findings rather than restating the data:
- `the_period_look_is_not_the_default_pair` — the Amiga reports grey 12/9 and draws a blue page; the C128 reports nothing and has a look. This is the case that should fail if anyone ever makes one derivable from the other.
- `the_status_band_and_cursor_do_not_follow_from_the_body_pair` — four machines, three status behaviours, three cursor shapes, and the Amiga's cursor colour equal to neither its page nor its ink.
- `the_machines_with_no_capture_decline` — exactly ST and IBM, and the three Apples share one value. Fails if someone fills a row by inference.
- plus the existing `the_pairs_are_the_sourced_ones` and `the_three_apples_differ_only_in_their_number`, untouched.

WHAT IS NOT DONE, and is the remainder of this quest: **nothing reads the field yet.** The consuming half — default ON in babelmap and OFF in `zvm-cli`, a config key either way, and checking how it composes with `honor_game_colours`, `--no-game-colours` (SQ-0855), `NO_COLOR` and SQ-0860's monochrome-artwork force-off — is untouched, and so is `docs/features/interpreter.md`, deliberately: there is no user-visible behaviour to document until something reads it. The status band and cursor are the parts that will take real work in the render path, since babelmap currently draws a cell-inverting block cursor on every machine and no machine measured draws one.

--- note 2026-08-19T04:48:33Z ---

THE COMMODORE 64 NOW HAS A ROW, and the reason it did not was bad. The user caught it by reading `--machines` and asking why the machine they had supplied a screenshot of was listed as not modelled.

The stated ground for excluding interpreter 8 (SQ-0869) was that a `.d64` is a 1541 image both Commodore machines read, so the medium cannot choose between 7 and 8. True — and not a reason for a row to be missing, because **ProDOS cannot choose between 2, 9 and 10 either and all three have rows**. `docs/features/interpreter.md` even argues that case at length: a volume that names a FAMILY means the number is asked for rather than inferred. Same situation, opposite treatment, in one table.

The other half of the old reason — no Infocom Commodore interpreter read — is equally true of the Commodore 128, which has a row and declines `default_colours: None`. Declining is what a row is FOR. So the C64 row declines the pair exactly as the C128 does, and carries the period look.

VALUES FROM THE SOLID GOLD PRESS, on the user's instruction, and it is the better capture on its own merits: `c64-zork1-solidgold.png` is Zork I release 52 / serial 871125, and its banner reads **"Interpreter 8 Version J"** — the machine naming its own number, which is as direct a statement as anything in this table.

  page #000000, ink #FFFFFF, StatusBand::FullReverse, underscore cursor #FFFFFF

The status band runs x 37..522 at rows 48..58 with no interior gap wider than a glyph, so it is a plain full-width reverse. The cursor is 1 row tall at y=314 beside the `>` at x 39..45. (The capture is 560x375 — a non-integer scale of the C64's 320x200 — so it censuses to 452 colours; #000000 at 172,286 px and #FFFFFF at 4,969 dominate and everything else is interpolation between them. Fine for identifying a two-colour screen, and noted.)

** THE TWO C64 CAPTURES DISAGREE, AND THAT IS A FINDING RATHER THAN A CONFLICT TO RESOLVE. **

  c64-hitchhiker.png      (1984)  page #6C6C6C  ink #FFFFFF  status #6C6C6C on #000000  underscore #000000
  c64-zork1-solidgold.png (1987)  page #000000  ink #FFFFFF  status full reverse        underscore #FFFFFF

One machine, one publisher, three years apart, and every one of the four sub-decisions differs. **So a period look is a property of the interpreter BUILD as much as of the machine**, and this table has one row per machine — which is a real limitation of the field's shape, now recorded in `PeriodLook`'s docs rather than papered over. If a second machine ever shows the same drift, the field wants a release dimension and this table cannot carry one.

`StatusBand::Own` is KEPT although no row now uses it: the 1984 build is the evidence that a status band need not be derivable from the body pair, and that evidence does not evaporate because the row that reported it moved to a later press. A variant with no current user is cheaper than re-deriving the finding later.

ALSO CHANGED:
- `zvm-cli/tests/disk_image.rs`'s `an_unmodelled_machine_says_so_instead_of_quietly_becoming_an_ibm_pc` used 8 as one of its three examples of an unmodelled number. Now 1 and 11, with a note saying why 8 left the list.
- `docs/features/interpreter.md` said "Three numbers still have no profile"; now two, with the ProDOS parallel spelled out so the old reasoning is visible as having been overturned rather than quietly deleted.
- `--interpreter 8` is now accepted by both front-ends where it used to be refused as unmodelled.

--- AND THE `--machines` OUTPUT NOW SAYS WHY IT IS TWO TABLES ---

The user's second observation: "we also seem to have two different tables in the --machines output for some reason". The split was deliberate and nothing in the output said so, which is a documentation failure, not a design one. Both blocks are now numbered and titled:

  1. WHAT THE STORY IS TOLD - the header bytes and the section 8.3 screen rules,
     each value read out of Infocom's own interpreter source.
  2. WHAT THE SCREEN LOOKED LIKE - presentation for a v1-v4 story, which has no
     colour concept for the pairs above to be the default OF.
     OBSERVED from emulator captures in machine-screenshots/, not read out of
     Infocom's source as the pairs above are.

Merging them would put a sourced constant and an observation in one row under one authority — the exact confusion this quest's first note said must not blur — and would also run past 160 columns. The reasoning is now in the const's doc comment so the next person to find it odd finds the answer beside it.

--- note 2026-08-19T16:36:45Z ---

THE CONSUMING HALF IS BUILT. Nothing read `period_look`; everything does now.

THE GATE — `crates/app/src/period.rs`, `resolve(profile, enabled, honor_game_colours, zversion)`:
  - **v1-v4 only**, and it is a version clause rather than a knob. For v5+ the pair
    on screen is a fact the story can read and `default_colours` already supplies it
    out of Infocom's source; painting over that would confuse the two kinds of claim
    this quest's first note said must not blur.
  - **`honor_game_colours` is the master and takes the look with it.** The narrow
    key is one-way, which is exactly what this quest specified: declining the
    presentation must not cost a v5+ story the colours it asked for.
  - SQ-0860's monochrome force-off **cannot reach it** and the code says so: it
    fires only for a monochrome artwork ARCHIVE, and an archive means Version 6. A
    story cannot be both v6 and v1-v4, so the two features are disjoint by
    construction. The ordering is belt-and-braces, not a fix.
  - `NO_COLOR` has no reader in the TUI at all — it is a convention about adding
    ANSI colour to command output, and lanthorn is full-screen. In `zvm-cli` it
    already folds into the same `honor` flag `--no-game-colours` sets, so the look
    inherits the composition rather than restating it.

WHERE THE COLOURS GO IN, and why there: `reload::reload_style`, right after
`resolve_theme_layered`. That is the single funnel — startup, `/reload-style`, the
style watcher and the per-game overlay all pass through it — and BOTH inputs move
there: the theme was just rebuilt, and `honor_game_colours` was recomputed twenty
lines above. Resolving at startup instead would mean a `style.toml` edit that
claims the transcript could not take the page back until the next launch.

`Theme::fill_unclaimed` / `set_unclaimed`, per selector AND its registry role:
SQ-0847's rule reused. A selector any layer wrote is a CHOICE and outranks a
machine default. The role matters because `Provenance` is stamped per row and does
NOT travel down the parent chain — someone who recoloured `[roles] text` and left
`transcript` alone has still chosen the transcript's ink, and without the second
argument the Amiga would have taken it back.

Two paints, not one. Prose and the typed line take the machine's PAIR; lanthorn's
own annotations (`transcript_input`, `transcript_meta`, `transcript_warning`) take
only the PAGE — their ink says something lanthorn means and no machine has an
opinion about it, but leaving their ground alone punches the theme's page through
the middle of the machine's.

`set_unclaimed` exists for exactly one selector and it is a trap worth naming:
`status_bar`'s registry default carries REVERSED. A swapped pair patched under a
REVERSED modifier swaps BACK — a full reverse rendered as no reverse at all. The
band is therefore stated absolutely and the row's own modifiers go with it.
`a_full_reverse_band_is_stated_and_not_left_to_reverse_itself` fails if anyone
ever patches it instead.

WHAT A THEME CANNOT CARRY, and so is on `AppState::period_look` and read at the
render sites:
  - **the Amiga's status line, which is not a band.** `render_status_content` fills
    the row with the PAGE and gives each segment the reverse, per `StatusBand::PerRun`.
  - **the cursor's shape.** `draw_caret` in `render/transcript.rs`, replacing the
    two duplicated call sites. `Bar → ▏`, `Block → a space in the cursor's colour`,
    `Underscore → ▁` — the glyph occupying the same eighth of the cell. Over a
    CHARACTER the shape stands down (the character has to stay readable while it is
    edited) and only the colour speaks; an underscore machine can still underline.

`zvm-cli --period-look`, OFF by default, and it does the cursor BETTER than the TUI
can. Page and ink through **OSC 11 and OSC 10 — the terminal's own defaults, not
SGR**, because every styled run ends with `ESC[0m` and would drop an SGR ink on the
floor; a full reverse then costs nothing, since `ESC[7m` swaps exactly the machine's
pair. Cursor through **DECSCUSR** (2 block / 4 underline / 6 bar), which states the
real shape rather than an approximation. What it cannot say is the cursor's COLOUR:
DECSCUSR carries a shape and nothing else, and OSC 12 is far less widely honoured
than the 10/11 pair. `cursor_steady_underline`/`_bar` live in `cli_host::term`
beside the escapes they belong with; `zvm-cli` does the `CursorShape` mapping so the
Z-machine's vocabulary stays out of a host shared with the Glulx and Scott
front-ends. `restore_bytes` gained OSC 110 so the ink is released on every exit
path, including the Ctrl-C ones that never reach `main`.

A RUN BREAKS ON THREE SPACES, NOT TWO, and the smoke is what found it. `status_text`
puts exactly two between "Score:" and "Moves:", and at two the Amiga band reversed
them as separate runs with a two-column hole between — which the capture does not
show. The page shows between the LOCATION and the score, which is the wide fill, and
nowhere else.

THE COMMODORE 64 GOT A PROFILE (`InterpreterProfile::Commodore64`). The zvm row
existed and the app could not reach it: `for_interpreter_number(8)` fell through to
IbmPc, so `--interpreter 8` in the TUI got an IBM PC wearing the C64's number — the
very substitution SQ-0872 exists to stop, still live on the one machine this quest
measured last. No medium selects it; a `.d64` still answers 7.

REAL-GAME SMOKE, both front-ends, and this is the part the unit tests cannot reach:

  `zvm-cli --period-look "Zork I …adf"` emits
    ESC]11;#074ba1  ESC]10;#ffffff  ESC[2 q
  and a status row of ESC[7m per run with the page showing between; teardown emits
  ESC]111 ESC]110 ESC[0 q. Off the Hitchhiker's `.d64`: #000000 / #55ffff / ESC[4 q,
  full-width reverse. Suppressed correctly by default, by --no-game-colours, by
  NO_COLOR=1, and by a v5 story.

  The TUI under a pty (`examples/pty_capture`, the real binary, Zork I off the
  `.adf`) paints **376 cells of #074ba1**, 31 of #ffffff with **row 1 fully white**
  (the band reversed), and **exactly one cell of #ff7e1c** at row 11 col 2 — the
  Amiga's orange caret, a colour in neither channel of its body pair.

TESTS: nine in `app::period`, four render cases in `transcript.rs`, two band cases
in `zvm-cli::screen`, and a new real-media suite `period_look_media.rs` (in the
`engines` group) that drives five disks in `stories/` — Zork I, Zork II and
Hitchhiker's as the dressed v3 cases, Sherlock (v5) and Zork Zero (v6) as the
machine-is-right-and-must-not-apply cases — with a non-vacuity guard on each so a
gitignored skip cannot pass for a pass. Verified all five specimens are found.
Falsified `the_body_and_the_input_line_stand_on_the_machines_page` by stubbing
`apply_to_theme`: it fails with `left: Reset`.

A TRAP FOUND AND DOCUMENTED ON THE WAY: the settings screen's rows are addressed by
INDEX in four separate matches (`config_row_value`, `config_toggle_or_edit`,
`config_cycle`, `one_run_key_for_row`). Inserting `period_look` at 9 silently
retargeted two tests at their neighbours — one cycled `undo_levels` believing it was
`interpreter_number`. Nothing catches it: the count test checks the COUNT and the
count is right either way. Both tests now look their row up by NAME, and the trap is
written at `CONFIG_ROWS`.

STILL OUTSTANDING FOR THIS QUEST: nothing. The Atari ST (5) and IBM PC (6) still
have no capture at all, which is a corpus gap rather than unfinished work — both
decline, exactly as the sourced-or-declined standard says they should.

Gate: 5663/5663, clippy clean.

--- note 2026-08-19T23:16:49Z ---

INTERPRETER 6 HAS A CAPTURE NOW — `machine-screenshots/dos-hitchhiker.png`,
committed in 5aefdc6e. Only the Atari ST (5) remains without one.

Hitchhiker's r47 / 840914, **Version 3**, DOS with a CGA colour display. The
version is what makes it a period-look fixture rather than an SQ-0928 one: colour
arrives with v5, so this story has no colour concept and every pixel is the
interpreter's presentation.

Measured, row- and column-censused per this quest's own method note:

  body        page #0F009E (92.4% of the frame), ink #A0A0A0
  status      NOT a band — 611 contiguous px of the blue PAGE run through the
              middle of the row (x 132..743 at y=14)
  and         #A2000D RED, in the status row and NOWHERE else (0.1% overall),
              in neither channel of the body pair

## IT BREAKS `StatusBand`, and that has to be fixed before the row is filled in

The four variants are `FullReverse`, `PerRun`, `Own { ground, ink }`, `Ruled`.
This frame is **PerRun in structure** (the page shows between the runs) and
**Own in colour** (red is not the body pair reversed). The type carries one or
the other; this needs both.

Two shapes, neither agreed:

- add a variant that carries per-run colours, e.g. `PerRun { ground, ink }` with
  the existing `PerRun` becoming `PerRun { reverse }`; or
- split the enum into two axes — WHERE the ground applies (full width / per run /
  none) and WHAT it is (reverse / a stated pair / nothing). The five machines then
  populate a small grid instead of four special cases, and the Macintosh's `Ruled`
  becomes "no ground" plus a rule flag.

The second is probably right and is a bigger change. **Do not force this frame
into `PerRun` and lose the red** — that would be inventing a value to fit a type,
which is the failure mode this whole quest was written against.

## AND IT CORROBORATES SQ-0928 FROM A DIFFERENT DIRECTION

That quest's evidence is all v6 — Shogun, Arthur, Zork Zero, `$2C`/`$2D` facts a
story can read. This is v3, a decade of interpreter apart, where nothing is read
and everything is shown. The page is the same blue. A default that holds across
both is a real default; a coincidence would not survive the crossing.

Note the two quests are still asking different questions and must not merge: SQ-0928
pins a NUMBER a story reads (blue 6, resolved through the palette and the player's
theme), this pins what a screen LOOKED LIKE (a specific RGB an emulator drew). The
first note of this quest forbids blurring them, and the agreement between them is
evidence precisely because they were arrived at separately.

CAVEAT: 1159x726 is a non-integer scale of the text screen, so glyph edges
interpolate and the run boundaries are ±2px. The three colours are unambiguous;
the cell boundaries are not.
