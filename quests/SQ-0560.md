---
title: map.room / map.connector `glyphs = { ... }` sub-map is parsed into the Theme but the map renderer never reads it
status: open
type: bug
priority: low
created: 2026-07-29T18:02:52Z
commits: []
context: |-
  branch: main
  head: 510fe702
  cwd: /Volumes/Videos/Source/babelmap

  Found while fixing SQ-0558: toml_schema/resolve carry per-selector `glyphs` sub-maps into Theme (SQ-0440), and docs/features/customization.md claimed that was THE way to override individual map glyphs, but render/map.rs only ever reads `.style` from the theme and draws from state.symbols — so `glyphs = { tl = "+" }` on map.room does nothing. The working mechanism is the slot-keyed override table (now [map.overrides]); the doc was corrected to describe it. Either make the renderer consume theme glyph slots or drop the claim from the design spec.
---
