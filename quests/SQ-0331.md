---
title: 'Per-Glk-style theme colours: add glk_styles[wintype][11] slots to ColorScheme with game-hint > theme-per-style > per-app-element resolution (foundation for garglk.ini import)'
status: done
type: feature
priority: low
created: 2026-07-14T18:48:53Z
completed: 2026-07-21T06:08:36Z
commits:
  - 97f213f3648698915368ad64cb57fabe6222acff
context: |-
  branch: main
  head: de63d0b
  cwd: /Volumes/Videos/Source/babelmap
  current: SQ-0330

  User directed adding real per-Glk-style theming (not a lossy garglk.ini import) and adapting the per-app-element scheme as the fallback base; this is the pulled-forward slice of the deferred style redesign (SQ-0309) that SQ-0319 then imports into.
---

--- note 2026-07-14T18:49:06Z ---

Design (user-confirmed layering): ColorScheme gains glk_styles: [[GlkStyleColour; 11]; 2] (buffer=row 0, grid=row 1). Per-app-element colours stay as the BASE (transcript≈buffer Normal, upper_window≈grid Normal); the 10 other slots add expressiveness. Per-run colour resolution, most-specific wins: (1) garglk per-stream override; (2) game stylehint for (wintype, style) — game explicitly coloured it, already flows via SQ-0328; (3) theme glk_styles[wintype][style] — the new per-Glk-style default garglk.ini imports into; (4) per-app-element (transcript/upper_window); (5) terminal default. Plumbing: carry the run's GlkStyle CLASS to the transcript run/elem (today only the derived bold/italic bits survive) so render can look up slot 3. Add per-slot style.toml selectors (e.g. glk.buffer.header). Themeable per styleable-ui-elements rule. Blocked on SQ-0330 finishing in the worktree. Then SQ-0319 imports into glk_styles.

--- note 2026-07-14T18:54:38Z ---

CONFIRMED resolution order (per channel — fg/bg independent; first source that sets a channel wins, else fall through): 1) garglk per-stream override (garglk_set_zcolors/reversevideo); 2) game stylehint for (wintype,style), resolved per-window via SQ-0328 snapshot-then-global hybrid; 3) theme per-Glk-style slot glk_styles[wintype][style] (garglk.ini import target); 4) per-app-element base (transcript=buffer Normal, upper_window=grid Normal — the 10 non-Normal slots fall back here); 5) terminal/theme default. GATE: honor_game_colours (== garglk `stylehint 0/1`). ON (default) → 1>2>3>4>5. OFF (force user/theme styles) → layers 2 and 3 SWAP → 1>3>2>4>5. The garglk per-stream override (layer 1) is NOT suppressed by the gate (only declarative stylehints, layer 2, are). Reverse video (stylehint_ReverseColor / garglk_set_reversevideo) resolves down the same chain to a bool, swapped at draw. Non-colour attrs (Weight→bold, Oblique→italic, SQ-0330 indent/justify) follow an analogous chain ending in the style class's built-in default (Emphasized→italic).

--- note 2026-07-14T18:58:47Z ---

Default slot seeding (user-confirmed): buffer Normal ← transcript, grid Normal ← upper_window (the layer-4 base). Pre-seed two more slots from existing app elements: buffer Input(8) ← input_text, and buffer Subheader(4) ← transcript_location. All other 8 buffer slots + the non-Normal grid slots default to None → inherit Normal, so today's look is preserved until a theme/garglk.ini sets a slot. Attribute defaults (bold/italic/reverse/fixed) continue to come from glk_style_bits (with Emphasized→italic per SQ-0330). These seeds are the resolution chain's layer 3/4 defaults; a game-set stylehint (layer 2) or garglk override (layer 1) still wins when honor_game_colours is on.
