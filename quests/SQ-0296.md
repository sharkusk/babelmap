---
title: Automatic game-initiated persistent @save/@restore (no prompt, cross-session) so CM skips its long init on relaunch
status: open
type: feature
priority: high
created: 2026-07-13T02:39:59Z
commits: []
context: |-
  branch: sq-0284-storage-layout
  head: 1a03f49
  cwd: /Volumes/Videos/Source/babelmap
  current: SQ-0294

  Bug-B (SQ-0292) investigation found CM's init-skip cache is an AUTOMATIC @save to a fixed SavedGame slot at boot (not the player's save verb, not the Glk VFS). Now that "Save failed." is fixed (1a03f49), the remaining gap: saved_game_files is session-transient so on relaunch the slot reads non-existent → CM re-runs full init; and per the user it must be silent (no Save-to-file prompt). Needs: named-fileref @save/@restore auto-resolve to a fixed on-disk .qzl in the per-game .save dir + auto-restore at boot, no prompt (create_by_name/usage → automatic; create_by_prompt SavedGame → player UI).
---
