---
title: No font lanthorn ships can draw the map's ⊙/⊗ portal badges — Fira Code lacks them and so does the bitmap master
status: done
type: bug
priority: low
created: 2026-08-21T22:26:42Z
completed: 2026-08-22T07:09:23Z
commits:
  - 3354e7160911b14f07a341a786c7a0dc5b52a84d
  - ed10e276464a3b97c05c812b40b9a265e5c7f26c
context: |-
  branch: main
  head: 4b0d07e6
  cwd: /Volumes/Videos/Source/babelmap
  current: SQ-0983

  Found by the SQ-0963 lane when it verified Fira Code's cmap rather than trusting the claim that it has every glyph: PORTAL_IN/PORTAL_OUT (U+2299/U+2297, map.rs:1813) are absent from Fira Code and from the generated vga16 master, and bitfont::EXTRA_GLYPHS is scoped to codepoints v6 titles print, which these are not — so a user on such a face gets a blank badge silently.
---

--- note 2026-08-22T03:17:57Z ---

## REFRAMED — the quest's premise is wrong in an instructive way

User: "in nerd fonts, ⊗ can be found at f05c, and ⊙ at f192. not sure why they don't exist in the unicode space. they are rendering in our terminal so not sure where they are coming from."

Measured directly from the font cmaps:

  FiraCodeNerdFontMono-Regular       U+2297 no   U+2299 no   U+F05C YES  U+F192 YES
  JetBrainsMonoNerdFontMono-Regular  U+2297 YES  U+2299 YES  U+F05C YES  U+F192 YES

U+F05C and U+F192 are **Private Use Area** codepoints — where Nerd Fonts patch their icon set, IN ADDITION to whatever the base face already carried. JetBrains Mono's base face happens to include those two mathematical symbols; Fira Code's does not.

**So they render in a terminal for one of two reasons**: the face has the real codepoint (JetBrains Mono), or the terminal's font-FALLBACK chain finds another system font that does (Fira Code). Either way the user sees a glyph.

**A terminal has a fallback chain. Our gallery rasteriser does not.** `bitfont`/`fontdue` draws with the one named face, so a missing glyph is blank. That is where the blank badge lives — in gallery shots, and only since SQ-0963 moved them to Fira Code. It is NOT the user-facing defect this quest was filed as. The SQ-0963 lane already added a per-glyph capability fallback to the bitmap master for exactly this, and reported that no shot in the current manifest reaches these two.

## The real defect underneath, which is a different one

`crates/app/src/render/map.rs:1811-1815` hardcodes all five portal glyphs as consts, with the comment "Named so a font that renders a variant better is a one-line swap" — a one-line swap IN SOURCE. That contradicts the standing rule that every UI element must be styleable via a `style.toml` selector and never hard-coded.

The machinery already exists: the theme registry carries `glyph` on a selector and supports a `glyphs = { … }` sub-map (`crates/app/src/theme/registry.rs:48-58`, the same mechanism map `box_style` presets use).

**Make the portal glyphs themeable and the font question answers itself per user**: a Fira Code user sets U+F05C/U+F192, a JetBrains Mono user keeps the Unicode pair, someone on a bare console picks ASCII. Hardcoding EITHER choice is wrong — PUA codepoints have no meaning without a Nerd Font and would be tofu for anyone else, while the Unicode pair depends on a fallback chain the gallery does not have.

Also worth doing while there: `PORTAL_UP`/`PORTAL_DOWN` (↑ ↓) and `PORTAL_UNKNOWN` (?) are hardcoded in the same block and should move together.

--- note 2026-08-22T03:20:48Z ---

## CORRECTION — the portal glyphs ARE already themeable. Both my previous notes were wrong.

User: "we should already have themeable portal glyphs." They are right, and the PUA route they identified is already built.

`map.portal_icons` is a themed preset (`crates/app/src/theme/registry.rs:299`, `style.example.toml:167`) with three values, resolved in `crates/app/src/symbols.rs:361`:

  ascii (default)   in ⊙ U+2299, out ⊗ U+2297, up ↑, down ↓, unknown ?
  nerdfont          in U+F090 (nf-fa-sign_in), out U+F08B (nf-fa-sign_out),
                    up U+F0B71, down U+F0B72, unknown U+F059, marker U+F111
  nerdfont-stairs   up U+F12BD, down U+F12BE, in U+F0FC4, out U+F0A48

So the answer to the original observation — Fira Code has no U+2297/U+2299 but does have Nerd Font PUA icons — is simply **`portal_icons = "nerdfont"` in style.toml**, which draws them from the patched font with no dependence on a fallback chain. No code change needed for that, and the earlier note's proposal to "make the portal glyphs themeable" was reinventing something that exists.

## What is actually left, and it is small

`render/map.rs:1811-1815` still holds the five glyphs as hardcoded consts behind `portal_glyph(dir)`, whose doc says it is "Shared by the map badge and the dump legend". The map badge path is themed — `draw_portal_icons` (:1994) resolves the preset. The remaining caller is **`crates/app/src/map_dump.rs:193`**, which formats `PORTAL {origin} {glyph} #{dest}` from the unthemed consts.

So `/dump-map` output shows ⊙/⊗ regardless of the user's `portal_icons` setting — a themed map and an unthemed dump of that map disagree. That is the whole defect: one caller on a duplicate const path, not a missing feature.

Fix: thread the resolved `PortalGlyphs` into `map_dump`, delete the consts, and update the five assertions at `render/map.rs:4753-4757` which currently pin the const values.

Priority stays low — a dump legend disagreeing with the map is a cosmetic inconsistency, not the "no font can draw these" defect this quest was filed as. That framing was wrong: a terminal has a font-fallback chain, and a user who wants the PUA icons already has a switch for them.
