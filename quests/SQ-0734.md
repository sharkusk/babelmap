---
title: 'Resource resolution for native Infocom picture files: find and prefer a native archive beside the story, with a rule for when it beats the Blorb'
status: open
type: feature
priority: low
created: 2026-08-09T21:18:11Z
commits: []
context: |-
  branch: main
  head: 0408e18f
  cwd: /Volumes/Videos/Source/babelmap
  current: SQ-0708

  Left as the explicit follow-up when SQ-0713 landed a working decoder but stopped short of integration — this is what the original fmvpoker.z6 arrangement (a story whose art is Zork Zero's picture file renamed to FMVPOKER.EG1) actually needs, and where the five pictures the Blorb truncates would come from.
---

--- note 2026-08-09T21:27:52Z ---

POLICY DECIDED by the user 2026-08-09: "the 3-tier approach seems good". This is the spec for this quest — implement it, do not re-open it.

**Three tiers, in decreasing confidence. Never guess a pairing.**

1. **Blorb** — zero config, as today. The container validates its own contents; a `.blb`/`.blorb` beside the story, or the story itself being a Blorb. Unchanged.
2. **ADF disk image** — zero config. The story and its picture archive live in the same image and are therefore guaranteed to belong together. This is SQ-0719's job; this quest consumes what it mounts.
3. **Explicit config key** — the user names a native archive and thereby ASSERTS the pairing. Use the existing per-game sidecar `<game_dir>/config.toml` (bare lines, absent key = inherit, never templated — see CLAUDE.md), e.g. `pictures = "FMVPOKER.EG1"`. This is the tier that serves fmvpoker, whose art is Zork Zero's picture file renamed and which will never appear on any disk image.

**EXPLICITLY REJECTED: stem-based auto-discovery** (looking for `<story-stem>.pic` or similar beside the story). Reason, measured rather than assumed: every Infocom Amiga release names its picture archive `Pic.data` — Zork Zero and Arthur both do — so any name-based rule requires the user to rename, and a wrong rename is undetectable. The format offers almost nothing to validate a pairing with: the header carries a part number and a picture count, no release number and no serial. The failure mode is therefore "plausible but wrong art", e.g. Arthur's plates drawn into Zork Zero. Do not reintroduce this to close the DOS-art gap below.

**KNOWN GAP, recorded so nobody rediscovers it and quietly re-adds tier-0.** The Blorbs in circulation are already Amiga conversions — SQ-0713's decoder confirms this at 383/388 byte-exact against `Zork0.blb`. So tier 2 mostly yields art we already have. The genuinely DIFFERENT art is DOS EGA/MCGA, which circulates as loose `.EG1`/`.MG1`/`.CG1` files or FAT12 floppy images. Under this policy that art is reachable only through tier 3 today; reaching it with zero config would need FAT12 support, which is not scheduled and is not part of this quest. Related: SQ-0735 (decoding the PC LZW flavour) is the prerequisite for tier 3 being useful on DOS art at all — the current reader handles only the Amiga Huffman codec.

**Open sub-question for the implementer, not yet decided:** when both a Blorb and a tier-2/3 native archive are present, which wins? The tiers above are ordered by confidence in the PAIRING, not necessarily by preference of CONTENT — a user who explicitly names a native archive probably wants it used. Propose a rule with reasoning rather than picking silently.

--- note 2026-08-09T21:30:26Z ---

PRECEDENCE DECIDED by the user 2026-08-09, closing the open sub-question above: **"the config file override always wins (if the file exists)"**.

So tier 3 outranks tiers 1 and 2 whenever the named file is present. Naming an archive in `<game_dir>/config.toml` is an instruction, not a hint — if a Blorb sits beside the story, or the story came out of an ADF carrying its own `Pic.data`, the explicitly named archive is still the one used.

IMPLEMENTATION DETAIL the user did not specify, and which the implementer should NOT resolve by silently falling back: what happens when the key names a file that exists but cannot be used — wrong container flavour (the current reader rejects the PC LZW form as `UnsupportedContainer`; see SQ-0735), corrupt, or truncated. Silently dropping to the Blorb would recreate the exact failure this whole policy exists to prevent, only inverted: the player believes they are seeing native art and is not, with nothing on screen to say so. Surface it — an error or a visible warning naming the file and the reason — and only then decide whether to continue with the Blorb. "If the file exists" was the user's condition for the override winning; a file that exists but does not decode is a different case and deserves to be loud.

--- note 2026-08-09T21:32:47Z ---

FURTHER DIRECTIVE from the user 2026-08-09: "if we specify a specific file-type for override, we should automatically pick the matching interpreter number (unless that is also overriden)".

So **the art you name determines the machine you are emulating.** Naming a DOS `.EG1` means you are asking for the IBM PC experience; naming an Amiga `Pic.data` means the Amiga one. The resource selects the interpreter profile, and an explicit interpreter setting overrides the inference.

PRECEDENCE, most specific first:
1. An explicit interpreter-number setting (config or CLI) — always wins.
2. Inferred from the flavour of the native archive named in the config override (tier 3).
3. Inferred from the medium — a story loaded out of an ADF is an Amiga (SQ-0719's second half).
4. The existing default: Frotz's rule, 6 for v6 and 1 otherwise.

INFER FROM CONTENT, NOT FROM EXTENSION. SQ-0713's reader already distinguishes the containers structurally: the Amiga/Mac flavour is big-endian Huffman + RLE + per-line XOR (flags `EF_PHUFF`), and the PC flavour is little-endian GIF-style LZW — `parse()` currently returns `UnsupportedContainer` for the latter (SQ-0735 will add it). A file's extension can be wrong; its codec cannot. Extension is a reasonable tiebreak only.

Flavour → machine, as far as the source Infocom shipped tells us: `.EG1`/`.MG1`/`.CG1` are the PC family (EGA / MCGA / CGA); `Pic.data` in the Huffman form is Amiga; the same interpreter source also carries a Macintosh graphics path, so a Mac flavour exists and should be recognised rather than assumed away.

**VERIFY THE NUMBERS AGAINST THE ZMSD, DO NOT RECALL THEM.** The interpreter-number table is ZMSD §11.1.3. CLAUDE.md is explicit that spec constants come from an authoritative source; a wrong number here is silently wrong, since the game simply behaves like a different machine.

BLAST RADIUS, already measured: header byte 0x1E is not inert. `crates/zvm/src/cpu/exec.rs:4052` branches on `read_byte(0x1E) == 6`, and the current default gives every v6 story that value. So any inference that moves a v6 game off IBM PC changes what the game does, and the whole v6 corpus (Zork Zero, Arthur, Shogun, Journey, advent, scopa, mysterious01, fmvpoker) must be re-checked frame-by-frame, not just the path that triggered the change. This is why SQ-0719's first agent was scoped to exclude the interpreter profile entirely.

--- note 2026-08-12T05:53:47Z ---

SCOPE CLARIFICATION 2026-08-11, user:

> "we should have the ability to specify which art file to use in a game specific config file"

Mechanically this is **tier 3 exactly as already specified** — `pictures = "…"` in `<game_dir>/config.toml` — so no implementation change. But the framing is broader than this quest was written for, and it should shape the docs and the tests.

This quest was motivated by **fmvpoker**: a story whose art lives under a different name and can never be paired automatically. That is the ORPHAN case. The user's phrasing covers a case never called out here — a game with **several renditions available simultaneously**, where the point is to CHOOSE one.

That case became real today, when SQ-0735's PC decoder landed. Zork Zero now has four usable archives in `stories/`:

    zork0.pic   Amiga  (Huffman + RLE + per-line XOR)
    zork0.mg1   MCGA   (LZW, 320-wide, per-picture palettes)
    zork0.eg1   EGA    (LZW, 640-wide, hardware palette)
    zork0.cg1   CGA    (LZW, 640-wide, hardware palette)

plus `Zork0.blb`. Arthur has `.pic`, `.mg1`, `.eg1`, `.eg2`, `.cg1` and a Blorb; Journey and Shogun similarly. So "which art file" is a genuine choice among renditions of one game, not only a rescue for a game with none.

CONSEQUENCES:
1. **Document as a feature, not a fallback.** `docs/features/` should say a user may select the MCGA, EGA, CGA or Amiga rendition of a game whose art already works — not merely that a misnamed archive can be rescued.
2. **Test rendition selection, not only the orphan.** Point the key at `zork0.eg1` for a story that would otherwise resolve its Blorb and assert the named archive wins. That exercises "override beats a perfectly good tier-1 source" — the precedence the user set on this quest — which the fmvpoker case alone cannot test, since fmvpoker has no competing source.
3. **It reinforces the interpreter-inference rule** rather than complicating it: naming `.eg1` asks for the IBM EGA experience, naming `Pic.data` asks for the Amiga one, which is precisely the "archive flavour selects the interpreter number unless set explicitly" rule already recorded. The header-byte-0x1E caution and the v6-corpus re-check stand unchanged, and deferring the inference remains acceptable if that risk cannot be discharged safely.

Nothing here reopens the policy: three tiers, stem-based auto-discovery still rejected, config override still wins when the named file exists, an unusable named file still loud. The running lane has been sent this.

--- note 2026-08-12T06:09:36Z ---

BOCFEL READ AS EVIDENCE, 2026-08-11 (`spatterlight/terps/bocfel/z6/find_graphics_files.cpp`, 380 lines). It solves this exact problem and it STRENGTHENS the user's policy rather than challenging it. Three findings, in the order they matter.

**1. It validates the pairing with NOTHING.** This was the open question — whether a shipping interpreter has a check we had not thought of that would make a rejected tier safe. It does not. There is not one test in the file that ties an archive to a story: no release number, no serial, no picture-count cross-check, no checksum. The only gates are "does the file open" and, later, "did extraction yield `image_count != 0`". The PC header even HAS a checksum word at offset 10 (SQ-0735, from `bcpic.c`) and bocfel never reads it — and it could not help anyway, being a checksum of the archive, not of the pairing. So the rejection of stem-based auto-discovery now rests on measurement rather than principle: the reference implementation that DOES auto-discover cannot detect a wrong pairing either.

**2. Its discovery is looser than the stem rule we rejected, and it knows.** `find_graphics_files` probes the story's DIRECTORY for a bare `PIC.DATA`/`CPIC.DATA` — no stem involved at all — so any such file is claimed for whatever v6 story is opened. In a flat library like `stories/`, holding Zork Zero, Arthur, Journey and Shogun together, that is precisely the "Arthur's plates in Zork Zero" failure, and only the lowercase name of our `zork0.pic` keeps it from firing. For the PC files it swaps the story's extension, which finds nothing in a release/serial-named library — exactly the user's own objection. And bocfel evidently hit that: `find_and_load_z6_graphics` carries a hardcoded fallback that rewrites the stem to one of four known title names (`is_spatterlight_journey` -> `/journey`, and the same for arthur/shogun/zork0). A per-title identification table is what stem matching degrades into. Tier 3 replaces it with one line the user writes.

**3. On a named archive that will not decode, it falls back SILENTLY.** `try_load` returns `image_count != 0` and the caller walks a fallback table; a user who set `gli_z6_graphics` to EGA and whose `.EG1` fails gets Blorb art with no message. The only diagnostic anywhere in the file is `fprintf(stderr, "No graphics found!\n")` when everything fails. That is the exact inverted failure the user's "must be loud" directive names, observed in the wild — so the directive is confirmed, not contradicted.

**4. Flavour -> platform: it maps to SCREEN GEOMETRY, not to an interpreter number.** `hw_screenwidth`/`pixelwidth`: Blorb/VGA/Amiga -> 320, 1.0; MacBW -> 480, 1.0; EGA/CGA -> **640, 0.5**; Apple II -> 140, 2.0. This corroborates SQ-0735's reading of header flags bit 3 and is what surfaced the EGA half-width-pixel problem now filed as **SQ-0790**. It does not disturb the interpreter-number inference — EGA/MCGA/CGA are three video cards on one machine, and that machine is the IBM PC — but it does mean "which rendition" carries geometry as well as pixels.

**5. One genuinely useful discriminator we did not have.** `extract_image_data.cpp:40` defines `kMacBWFlags = 0x0e` and reclassifies an Amiga `Pic.data` as monochrome Macintosh when the header flags byte equals it, with the honest comment that the flags "always *seem* to equal 0xe if the graphics are monochrome". That is a partial in-band Amiga/Mac separator — it catches only the B&W Mac, and it is a heuristic. Recorded on `InterpreterProfile::for_art_flavour` as the lead to develop before a Macintosh profile is added; `Flavour::AmigaMac` resolves wholly to Amiga today (ZMSD §11.1.3 numbers them 3 and 4 separately, so the distinction would matter if it could be made). It is also why `picture_space_width()` reads bit 3 only on `Flavour::Pc`: bit 3 is set in `0x0e`, so the bit alone would mis-measure a Mac B&W file as 640-wide.
