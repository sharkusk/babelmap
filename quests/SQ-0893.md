---
title: 'Delete three measured-dead things on the v6 render path: story_viewport, flank_native_bottom''s unreachable Menu arm, and a no-op borrow'
status: done
type: feature
priority: low
created: 2026-08-15T20:04:22Z
completed: 2026-08-15T20:13:55Z
commits:
  - ac6ce2aaf3991c1d611d39729a287d1b19808755
context: |-
  branch: main
  head: 155b9b91
  cwd: /Volumes/Videos/Source/babelmap

  Found by the v6 render pipeline mapping commissioned after SQ-0892's first attempt was stopped for patching symptoms — these are the three items its findings log marks MEASURED-dead, separated out because they carry no design risk.
---

--- note 2026-08-15T20:04:47Z ---

All three are from the findings log (§6) of `docs/superpowers/specs/2026-08-15-v6-render-pipeline.md`. Read §6 for the surrounding context; the entries are numbered 1, 2 and 3 there.

**1. `story_viewport` has no production caller** — `crates/app/src/render/v6_layout.rs:1448`. Only its own two unit tests call it (`:2688`, `:2700`). MEASURED by grep over `crates/`.

**DO NOT DELETE THIS ONE WITHOUT READING THE NEXT PARAGRAPH.** It is the shrink-until-clear-then-quantize cell-space viewport — literally step (b) of the ordering the pipeline document was commissioned to assess, sitting dead in the file hybrid reads from. SQ-0894 may well want it. The document also notes it "would need its oracle canvas fixed" before it could be used. So: either delete it AND record in SQ-0894's body that it existed, what it did, and the commit that removed it so it can be recovered — or leave it with a doc comment saying it is currently unused and why it is being kept. **Deleting it silently is the one outcome that is not acceptable**, because the next person to need step (b) would rewrite it from scratch. Your call which, but state your reasoning.

**2. `flank_native_bottom`'s `BottomPlan::Menu` arm is unreachable** — `crates/app/src/render/screen.rs:1152-1155`. Its only consumer is `flank_crop` at `:1647`, gated `matches!(plan, BottomPlan::Frame)`. MEASURED by grep (two hits: `:1152`, `:1647`).

The doc comment at `:1147-1151` describes the Menu case as live and explains at length what it is for — so a reader trusts it. **Fix the comment in the same commit as the arm**, or you have removed the code and left the misleading prose that made it look necessary.

**3. `let _ = &classes[i];` is a no-op** — `crates/app/src/render/screen.rs:5154`, inside the SQ-0508 bridge loop. READ, not measured. Looks like a borrow-checker workaround that outlived its cause. Confirm it compiles without it (it will, or it is not a no-op) and that SQ-0508's own tests still pass.

## Scope discipline

**These three and nothing else.** This quest exists precisely because they carry no design risk, and its value is that it can land immediately without waiting on SQ-0894. Do not "while I am here" any of §6's other 21 entries — several are real but need measurement or a design decision, and they are SQ-0894's business or their own quests.

Also commit `docs/superpowers/specs/2026-08-15-v6-render-pipeline.md` if it is still untracked when you start — it is the document this quest and SQ-0894 both derive from and it should not stay a loose file.

## Gates

`cargo nextest run --workspace 2>&1 | grep -acE "^error(\[|:)| [1-9][0-9]* failed"` must print 0 (grep exiting 1 on zero matches IS the pass; never chain with `&&`), with `Starting N` equal to `N tests run: N passed`. Baseline at `155b9b91` is 5562. Deleting `story_viewport` removes its two unit tests, so a drop of exactly 2 is expected and correct — state the number you get and why.

`cargo clippy --workspace --all-targets -- -D warnings` clean.

Trailer: `Completes: SQ-0893` — deletions of measured-dead code that the gate settles.
