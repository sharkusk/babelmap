---
title: /peel-layer with no direction should cut the passage you arrived through
status: done
type: feature
priority: low
created: 2026-07-29T03:34:17Z
completed: 2026-07-29T04:04:40Z
commits:
  - 5772e5a7bc3e89594b34beb4646a12fcd993a1be
  - 328745a5d9b9babca86d7f041191e8a2535e37f1
context: |-
  branch: main
  head: 2fee794a
  cwd: /Volumes/Videos/Source/babelmap

  User asked for it while reviewing map commands: a bare /peel-layer currently looks for an existing portal seam, and should instead cut at the passage the player just walked in through.
---

--- note 2026-07-29T03:46:32Z ---

REOPENED by user test against ~/.babelmap/saves/advent.blb.save/default.babelmap.

Repro: walk SOUTH from "At West End of ..." into the first "Maze" room, then a bare
/peel-layer. Result: "peel-layer: Maze's Unknown passage is not a boundary — both sides
stay connected another way."

TWO SEPARATE DEFECTS.

1. WRONG EDGE (the real one). The first cut assumed the passage walked is reachable as an
   exit OUT of the room just entered, i.e. opposite(arrived_via) from the current room.
   That only holds when the passage is reciprocal. Here it is not: you enter the Maze going
   SOUTH but leave going DOWN, so the Maze room has no north exit and the edge asked for is
   not the passage the player used at all. `peel_at_edge(graph, room, d)` cuts an edge
   leading OUT of `room` and keeps `room`'s side (SQ-0364), so it cannot express "cut the
   edge I arrived on".

   The passage actually walked is the INCOMING edge `prev --arrived_via--> current`. Cutting
   it must keep the FAR side (the room just entered), which no existing mapper entry point
   does: `peel_at_edge(prev, arrived_via)` would cut the right edge but keep the wrong side.

   User's requirement, verbatim: "the point of not having to specify a direction is that
   this may be a one-way entrance so we want to be able to peel even if there isn't a
   return path." So the fix must work with NO return edge at all.

   Needs: a mapper function that peels at a given incoming edge, taking the destination
   side. Also record the previous room alongside the direction — `Mapper::arrived_via`
   currently stores only the Direction, and the incoming-edge cut needs the origin too.

2. THE REFUSAL NAMES A DIRECTION IT NEVER TRIED. In input.rs the `Err(why)` arm passes the
   ORIGINAL `dir` argument to `peel_refusal_message`, which is still `None` for a bare
   /peel-layer, so it renders as "Unknown". It must report the seam actually attempted.
   Cheap fix, independent of (1), and worth doing regardless — the message is currently
   actively misleading.

ALSO CHECK while fixing: the refusal said "both sides stay connected another way", so even
the intended cut may not disconnect the region here. Now that maze rooms are properly
distinct (SQ-0526) there may be several routes between the maze and the rest, in which case
a single-edge cut can be legitimately insufficient and the command needs to say so in terms
the player can act on.

--- note 2026-07-29T04:01:36Z ---

FIXED in 328745a5. Awaiting user re-test.

(1) WRONG EDGE — new `mapper::layer::peel_at_arrival(graph, from, dir)` cuts the
    `from -dir-> dest` pair and peels DEST's region. `peel_at_edge` and it now share one
    private body (`peel_across`, `keep_far: bool`); only the kept end differs. The kept end
    is still the room the player stands in, so the SQ-0364 rule is intact — a peel always
    takes the side you are on, only the seam moved.
    `Mapper::arrived_via` is now `Option<(RoomId, Direction)>`.

    Guards in input.rs: the arrival steers the peel only when the target room IS
    `graph.current()` (a peel aimed at another selected room falls back to the portal-seam
    search), and only when the walked direction is planar (a portal arrival is already a
    cut — `region_with_cut` never traverses portals — so `peel_region` is correct there).

(2) REFUSAL — `peel_refusal_message` now takes `seam: Option<(RoomId, Direction)>`, the
    passage actually attempted, and names the room it leads out of. No more "Unknown".

ALSO-CHECK, RESOLVED: the "both sides stay connected another way" was NOT a genuinely
multiply-connected maze. Probing the reporter's save: the maze's only PLANAR link to the
rest of the map is `At West End of Long Hall -S-> Maze`. The two other links — `Maze -Down->
hall` and `Maze -Unknown-> Inside Building` — are portals, which the region walk already
treats as cuts. The old message was reporting a failed cut on a maze-internal north edge.
A single-edge cut is sufficient here; no "several routes" wording is needed.

VERIFIED against ~/.babelmap/saves/advent.blb.save/default.babelmap: peel_at_arrival(37214,
S) peels exactly 12 rooms (11 "Maze" + "Dead End, near Vending Machine") into a layer named
"Maze" with parent Main; 20 rooms stay in Main. Interlayer badges keep the Down exit back to
the hall and the Unknown exit to Inside Building.

Tests: `layer.rs::a_one_way_entrance_peels_from_the_passage_walked_in_through` (falsifies the
old behaviour — asserts `peel_at_edge(maze, N)` still returns NotASeam);
`input.rs::a_bare_peel_cuts_the_passage_walked_in_through`,
`::a_bare_peel_at_another_selected_room_ignores_the_arrival`,
`::a_bare_peel_refusal_names_the_passage_it_tried`;
`mapper.rs::arrival_passage_is_remembered_and_reset_when_nothing_was_walked` updated.
Gate: 3383 passing, 48/48 binaries reported, clippy clean.
