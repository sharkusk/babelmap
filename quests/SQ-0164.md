---
title: Standalone map export + load-at-startup prompt
status: discarded
type: feature
priority: low
created: 2026-07-03T15:27:35Z
commits: []
context: (DEFERRED 2026-06-25 — niche; the game bundle already persists+auto-loads the map) Standalone map export + load-at-startup prompt. (a) EXPORT the map to a portable, re-loadable standalone file (separate from the game-bundle zip; reuse the map.json serialization — confirm whether ExportDump already produces a re-loadable map vs. a display dump). (b) At startup, if an exported map file is DETECTED for the story (by IFID/naming), PROMPT the user to load it (mirror the wave25 launch resume-prompt dialog), with a config flag to disable the prompt. [persistence; map export command + startup detect + a load-map prompt dialog + config flag]
tags:
  area: APP
  category: persistence
  horizon: Near
  import: babelmap
  source_file: TODO.md
---

--- note 2026-07-08T19:04:12Z ---

Superseded by SQ-0226 (2026-07-08). The new model replaces this quest's approach: maps are not exported to / auto-loaded from standalone files with a startup prompt. Instead the map lives in the .babelmap archive and loads only with that state (gated by auto_load), and a new /load_map <path> command handles on-demand standalone loading. No startup detect/prompt, no separate export command. Closing as superseded.
