---
title: 'Tidy blocks the user: return control quickly when a re-tidy runs (don''t withhold input)'
status: partial
type: feature
priority: low
created: 2026-07-03T15:27:37Z
commits:
  - a1d30813c3a606f18b796c73243a895633712a6c
  - f0a9a9a81dcb497e8463883ef1fedbf854f884cf
context: 'Tidy progress indicators. REFINED to: the MAP BORDER smoothly pulses red<->green while a tidy runs, back to normal when complete (replaces the red/green corner dot). Progress BAR deferred.  — done (wave16, dee2a0c): background tidy now runs on a worker thread (graph clone + generation guard, stale results discarded, worker panic tolerated); render loop polls ~30fps while running; map border pulses via pulse_border_color. STILL DEFERRED: the filling progress bar. [navigation/UX; render + run loop]'
tags:
  area: APP
  category: navigation/UX
  horizon: Long
  import: babelmap
  source_file: TODO.md
---

--- note 2026-07-10T15:31:02Z ---

REFRAMED (2026-07-10, per user): the real issue is NOT a missing progress indicator — it's that when a re-tidy runs, the user loses control for a long stretch. Treat as a responsiveness/blocking bug: find where input control is withheld during tidy (tidy animation? result panel gating input? a synchronous portion?) and return control to the user quickly, rather than adding a spinner. Tidy already runs on a background thread (TidyJob) + shows a pulse border + post-run stats panel.

--- note 2026-07-10T16:13:10Z ---

RE-REFRAMED again (user): the delay is a long hang BEFORE the animation starts, not during playback/dismiss. Root cause: Action::AnimateTidy runs run_tidy_pipeline SYNCHRONOUSLY on the main thread (input.rs:2143-2148), re-running the full tidy layout with frame capture (cloning the whole MapGraph at every stage) → UI frozen until frames are built, then the anim shows. FIX: build the anim frames on a worker thread (mirror the background TidyJob spawn+poll+apply pattern), so the UI stays responsive during the build; install tidy_anim when the frames are ready.
