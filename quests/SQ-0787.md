---
title: Restoring a scopa Save State loses the blue card panels and dealt cards while the baize survives — paint-ground restore fidelity
status: discarded
type: bug
priority: low
created: 2026-08-12T04:10:01Z
commits: []
context: |-
  branch: main
  head: f5c530be
  cwd: /Volumes/Videos/Source/babelmap
  current: SQ-0784

  Found by the SQ-0784 lane while restoring a user-supplied scopa save headlessly to reproduce the score-row fill; the bridges rendered against bare baize instead of against the card panels, which is not what the live game shows.
---

--- note 2026-08-12T04:10:23Z ---

FOUND 2026-08-11 by the SQ-0784 lane, reported as out-of-scope rather than acted on. Its words:

> "restoring that Save State does **not** bring back scopa's blue card panels or its dealt cards — the baize survives but the panel rectangles do not, which is why my reproduction shows the bridges against green rather than against the panels. That is a display-list/paint-ground restore-fidelity question, not a flood question."

The archive is the user's own scopa Save State, taken at the end of a game (the same one that reproduced SQ-0784). On restore the green baize comes back and the blue card panels do not — so *some* paint ground is restored and some is lost, which is more diagnostic than a wholesale failure.

WHY THIS IS THE INTERESTING KIND OF RESTORE BUG. CLAUDE.md's persistence rule is "persist the recipe, not the result": a Quetzal save carries no screen state by design because the standard assumes the *story* repaints, while a host Save State swaps memory under a game that never learns it happened — so everything the screen needs is ours to carry. A ground that comes back partially says the display list is being restored but something in it is not being replayed, rather than that nothing was saved. Compare `v6_restore_palette_replay.rs`, which is the established pattern for exactly this class.

REPRODUCTION IS ALREADY SOLVED, and cheaply — the SQ-0784 lane worked out a headless restore recipe that needs no `--restore` flag, because `auto_load` defaults to true:

    mkdir -p $S/home/saves/scopa.z6.save
    cp ~/.babelmap/saves/scopa.z6.save/default.babelmap $S/home/saves/scopa.z6.save/
    cargo build -p app --bins --examples
    cargo run -q -p app --example pty_capture -- \
        --story stories/scopa.z6 --user-dir $S/home \
        --size 117x64 --keys wait:4000 --png $S/scopa.png

Two gotchas it paid for and recorded: `pty_capture --user-dir` **overwrites** the per-game `config.toml` with `show_map = false`, so per-game overrides must arrive via `--arg --config <path>`; and `cargo build -p app --example pty_capture` does **not** rebuild `target/debug/babelmap`, which the example actually runs — a stale binary reports "no change" and costs a cycle.

TESTING RULE THAT APPLIES HERE, from CLAUDE.md: restore tests must **perturb before asserting**. Asserting the frame immediately after a restore is precisely when everything still looks correct; the defect surfaces one action later when the game repaints, changes palette, splits or resizes. Restore, make a move, then assert. Cover restoring into a *different* terminal size and a different graphics backend — neither is visible to a same-session round-trip and both are common in the field.

NOT YET ESTABLISHED, and worth settling before any fix: whether this is scopa-specific or reaches the whole v6 corpus. Zork Zero, Shogun, Arthur and Journey all keep painted grounds under their chrome, and a display-list replay gap would show in all of them. Establish the scope first — a per-title fix to a general defect is how the hybrid/raster divergences in this codebase started.

--- note 2026-08-12T05:06:57Z ---

CLOSED by the user 2026-08-11: "i don't care about 787 since scopa save games aren't useful." Discarded on their call.

RECORDED SO THE GENERAL QUESTION SURVIVES THE INSTANCE. This quest's own note flagged that it was never established whether the defect is scopa-specific or reaches the whole v6 corpus — Zork Zero, Shogun, Arthur and Journey all keep painted grounds under their chrome, and a display-list replay gap would show in all of them. The scopa reproduction is uninteresting because nobody saves a card game mid-hand; the underlying "a restore brings back some paint ground and not the rest" question is not disposed of by that.

If a restore ever loses artwork in a game that matters, this is the same defect and this note is the head start: the SQ-0784 lane's headless restore recipe is above, along with the two gotchas it paid for (`pty_capture --user-dir` overwrites the per-game `config.toml`; building the example does not rebuild the binary it runs). CLAUDE.md's rule applies — perturb before asserting, because the frame immediately after a restore is exactly when everything still looks correct.

--- note 2026-08-12T05:21:44Z ---

THE SYMPTOM IS MATERIALLY DIFFERENT FROM WHAT WAS FILED — recorded 2026-08-11 on a discarded quest because the reframing may matter more than the original report did.

Filed as: a restore "loses the blue card panels and dealt cards while the baize survives". User, on 2026-08-11, describing the same restore:

> "the scopa restore does not show the proper cards (instead it shows the stale main menu cards), but it does show the behavior if you click in the correct area"

**That is not lost content. That is content from a DIFFERENT, EARLIER SCREEN persisting across the restore.** The frame renders scopa's **main menu** artwork where the dealt hand and board should be. Losing a paint ground and displaying a previous screen's artwork are different defects with different causes, and the second is the more serious: it means a restore does not clear what was on screen before it.

**THIS PLAUSIBLY JOINS SQ-0753 / SQ-0763.** Those recorded that `a=d` kitty deletes are **zero in every capture taken** — babelmap appears never to delete an image placement. Menu artwork surviving into a restored game frame is exactly what never deleting placements would produce. If that holds, this is not a scopa quirk at all but the same root cause, and it would affect **any v6 game restored or auto-resumed**, which is every v6 game the user resumes at startup since `auto_load` defaults to true.

WHY IT WAS DISCARDED, and why that reasoning may no longer apply: the user's stated ground was "scopa save games aren't useful". That is a judgement about the scopa *instance*. It does not cover "a restore shows the previous screen's artwork in any v6 game", which is what the new description actually reports, and which the user encountered while doing ordinary work rather than while testing saves.

LEFT DISCARDED pending the user's call — this note exists so the finding is recoverable and so nobody re-derives the connection to SQ-0753/0763 from scratch. If it is picked up, CLAUDE.md's rule governs: perturb before asserting, because the frame immediately after a restore is exactly when everything still looks correct; and cover restoring into a different terminal size and a different graphics backend, neither of which a same-session round-trip can see.
