---
title: zvm-cli
status: open
type: feature
priority: low
created: 2026-07-03T15:27:32Z
commits: []
context: '[TODO-2c15f4] zvm-cli: piped stdin at EOF spins the CPU. `read_byte_stdin()` (main.rs) returns `line.bytes().next().unwrap_or(b''\n'')` — at EOF `read_line` gives an empty string → returns `\n` forever, so a `read_char` loop (e.g. a chapter-select menu) never blocks and busy-spins. PRE-EXISTING (predates timed input; last touched in the crossterm migration 583a35d) — surfaced during Border Zone timed-input validation. Fix: detect true EOF (0 bytes read) and exit or return a sentinel. [tooling; zvm-cli read_byte_stdin] — completed 2026-07-02'
tags:
  import: babelmap
  source_file: COMPLETED.md
---
