---
title: A pty harness that drives babelmap and captures the escape bytes it actually emits
status: done
type: feature
priority: high
created: 2026-08-10T15:38:52Z
completed: 2026-08-10T16:26:21Z
commits:
  - 4af4a722d0804169d75b486af173a283f8877c8e
context: |-
  branch: main
  head: 3f275c1a
  cwd: /Volumes/Videos/Source/babelmap
  current: SQ-0708

  Agreed with the user after repeated defects that are correct in the model and wrong on screen — every existing harness asserts on internal state and none on the emitted stream.
---

--- note 2026-08-10T15:39:11Z ---

AGREED WITH THE USER 2026-08-10 ("Tier 1 it is").

THE GAP THIS CLOSES: every harness in this repo asserts on babelmap's internal model — the cell buffer, the layout, the placement records. **None asserts on the bytes we actually write to the terminal.** The model can be right while the emitted stream is wrong, and this session produced several defects that were "not in the model" yet plainly visible on screen. A byte-level harness is the only thing that can tell those two cases apart.

WHAT IT MUST DO:
- Run the real binary under a pty, send keystrokes, capture the emitted byte stream.
- **Verify the graphics protocol actually negotiated.** If the pty does not advertise kitty support and we silently fall back to halfblocks, the harness is exercising the wrong backend and every result is worthless. Check this FIRST and fail loudly rather than quietly measuring the wrong path.
- **Decode the stream into a readable form** — named escape sequences with their parameters (kitty APC graphics commands and their placement rects, SGR colour changes, cursor moves), not raw bytes. Raw output is unreadable and therefore undiagnostic; the decoding is most of the value.
- Be usable ad hoc (drive a story, dump the decoded stream to a file) as well as from a test.

CROSS-PLATFORM CONSTRAINT (see [[cross-platform-requirement]]): a pty is unix-only. Gate it so Windows still builds and its test suite still passes — a `#[cfg]`-gated test or an explicit skip, never a broken build. `zvm`/`gvm`/`scott`/`blorb` take zero external dependencies; the app may add them, and a test-only dev-dependency is the least invasive way in if a pty crate is needed. `script(1)` is present on this machine if a shell-level approach is preferred.

FIRST REAL TARGET, to prove it works: Journey off `stories/Journey - The Quest Begins.adf` (release 30 — NOT `journey.z6`, which is release 83 and a different build, see [[CLAUDE.md]] Test fixtures) at pane 115x61. The open defect is `menu_flank_panel`'s fill painting rows 53..61 under the menu. In the emitted stream that must appear as either a placement rect reaching those rows or SGR spans setting the panel's background there. If the harness cannot distinguish those two, it is not finished — they are different bugs with different fixes.

Related: SQ-0761 (the cell-styling dump, complementary — that shows what we computed, this shows what we sent), SQ-0747 (the fill defect), SQ-0760 (fixture/release discipline).

--- note 2026-08-10T15:39:32Z ---

USER 2026-08-10: "There is also libghostty we can evaluate as a tool harness."

Worth evaluating, and potentially much stronger than the pty harness. A pty gives us the bytes we EMIT; libghostty would give us Ghostty's own parsing and compositing of those bytes — the layer that has defeated every investigation into defects that are correct in our model and wrong on the user's screen (SQ-0747's truncated label above all). It would be tier 3's fidelity without putting GUI windows on the user's desktop.

EVALUATE, DO NOT ASSUME. Open questions, in the order that decides whether it is usable at all:
1. Does libghostty expose terminal state or a rendered framebuffer to an embedder, or only the pieces needed to build a GUI app around it?
2. Does it composite **kitty graphics** in whatever it exposes? If images are the renderer's business and not the library's, it cannot answer the question we care most about, and its value drops sharply.
3. Is it buildable and linkable here — it is Zig, so what does that add to the toolchain, and does it hold against [[cross-platform-requirement]] on Windows and Linux?
4. Licensing and whether it would become a dependency of the app or strictly of a dev/test harness. The latter is the only acceptable shape; `zvm`/`gvm`/`scott`/`blorb` take zero external dependencies regardless.

SEQUENCING: the pty harness is the committed deliverable and does not depend on this. libghostty is a bounded evaluation producing a recommendation, not a build. Do not let it displace tier 1 — a working byte-level harness today is worth more than a promising direction, and if libghostty proves out it can consume the same driving code.

--- note 2026-08-10T16:26:59Z ---

--file /private/tmp/claude-501/-Volumes-Videos-Source-babelmap/8882ca52-c45d-4607-bc1a-10714e553fba/scratchpad/note1.txt

--- note 2026-08-10T16:27:24Z ---

--file /private/tmp/claude-501/-Volumes-Videos-Source-babelmap/8882ca52-c45d-4607-bc1a-10714e553fba/scratchpad/note2.txt
