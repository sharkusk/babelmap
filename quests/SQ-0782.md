---
title: A skinnier scrollbar, and auto-hide it in the story pane when not actively scrolling
status: open
type: feature
priority: low
created: 2026-08-11T22:31:51Z
commits: []
context: |-
  branch: main
  head: b2928d79
  cwd: /Volumes/Videos/Source/babelmap
  current: SQ-0779

  Raised by the user after reviewing rendered frames: the full-block thumb fills its cell so transcript text ends up visually crowded against it.
---

--- note 2026-08-11T22:32:13Z ---

USER REQUEST 2026-08-11: "our scrollbar is a bit fat so text that ends up next to it is very close. is it possible to make a skinnier scrollbar, and auto-hide in the story pane when we are not actively scrolling?"

RECONNAISSANCE (read-only, done at filing time — do not re-derive):

**WHY IT IS FAT.** Every scrollable surface goes through ONE function, `draw_scrollbar` in `crates/app/src/render/scroll.rs`, which renders ratatui's `Scrollbar` with its DEFAULT symbols — thumb `█` (full block), track `│`. A full block fills the whole cell, so neighbouring text has no visual gutter. ratatui exposes `.thumb_symbol()` and `.track_symbol()`, so a thin pair (`▐` U+2590 right half block, or `▕` U+2595 right one-eighth block) makes it a rule hugging the pane edge. One change, every surface consistent.

Call sites, all via that one helper: `picker_ui.rs` (x3), `render/{hints_panel,history,command_band,saves,palette,config_screen,filebrowser}.rs`, and the story pane at `render/transcript.rs:2401`.

**AUTO-HIDE IS CHEAP HERE, and the reason is worth knowing.** The story pane draws its bar at `x = area.right() - 1 + right_margin`, width 1 — i.e. in the MARGIN BAND, outside the text area, because SQ-0345 pushed it past the right text margin so only text is inset. **So showing/hiding it cannot reflow a single character.** That removes the usual objection to auto-hiding scrollbars. Confirm the same is true of any other surface before auto-hiding it there; the modals reserve a 1-column gutter from their content width (`content.width.saturating_sub(1)`) and WOULD reflow, which is a good reason to keep them always-on.

**THE FRAME PUMP ALREADY EXISTS.** `crates/app/src/anim.rs` provides clock-backed `Tween`s that "the run loop advances each frame", already used for smooth transcript scroll. So a visibility window can request frames until it expires, and a fade is as cheap as a pop.

DESIGN POINTS TO SETTLE:
  - **Scope of "skinny"**: apply to every surface (consistency) but auto-hide ONLY in the story pane, per the request. Modals benefit from a persistent bar and would reflow anyway.
  - **What counts as "actively scrolling"**: wheel, PgUp/PgDn, and the scroll keys — probably NOT new text arriving, or the bar would flash on every turn. State that choice explicitly in the implementation.
  - **STYLEABILITY IS MANDATORY** (CLAUDE.md): a `scrollbar` colour selector already exists in the theme; the glyph pair and the hide delay both need to be settable from `style.toml`/`config.toml` rather than hard-coded, with the template updated. Never hard-code a style.
  - Both `honor_game_colours` modes need pinning if any test asserts colour.

OFFER TO THE USER at implementation time: the SQ-0775 rasteriser can render the candidate glyph pairs (full block vs half block vs one-eighth block, thumb and track) as PNGs so the choice is made by eye rather than by description. That is exactly the kind of decision a picture settles.

--- note 2026-08-11T22:52:49Z ---

DECISION 2026-08-11, made from rendered candidates rather than description. The user picked the **background-only** variant: **no glyphs at all** — thumb and track are drawn as SPACES carrying background colour, so there is no vertical rule competing with the pane border one column away.

FIVE CANDIDATES WERE RENDERED (advent.z8, 100x30, real captures through the SQ-0762 harness with the SQ-0775 rasteriser; a throwaway env-var patch to `draw_scrollbar`, since reverted):
  current      `█` thumb, `│` track   — the full block fills the whole cell; this is the reported fatness
  half         `▐` thumb, `│` track
  eighth       `▕` thumb, `│` track
  **bg**       `" "` thumb + `" "` track, distinguished only by BACKGROUND colour   ← CHOSEN
  halfnotrack  `▐` thumb, blank track

WHAT THE PREVIEW DID, and what the real implementation must do differently:
  - preview thumb: `Style::default().bg(style.fg.unwrap_or(Color::Gray))` — i.e. it reused the theme's `scrollbar` FOREGROUND as a background fill. Measured in the capture as `idx(6)`, 22 cells over rows 6..27.
  - preview track: `Style::default().bg(Color::Rgb(52, 52, 52))` — **hard-coded, and therefore not acceptable in the real change.** Measured as `#343434`, 5 cells over rows 1..5.
  - **Both colours must come from `style.toml`** (CLAUDE.md: every UI element styleable, never hard-code a style). The existing theme has a `scrollbar` selector; the track needs its own (a `scrollbar_track` or equivalent), with the template updated and defaults chosen so an unconfigured install looks like the approved render.

AUTO-HIDE, rules agreed with the user:
  - Show on WHEEL and the SCROLL KEYS (PgUp/PgDn and friends); do NOT show when new text arrives, or the bar flashes every turn.
  - Story pane ONLY. Modals keep a persistent bar — they reserve their gutter from content width (`content.width.saturating_sub(1)`) and hiding one there WOULD reflow the list. The story pane cannot reflow: SQ-0345 already places its bar in the margin band at `area.right() - 1 + right_margin`, outside the text area.
  - The hide delay must be configurable, not hard-coded.
  - `crates/app/src/anim.rs` already provides clock-backed tweens advanced by the run loop (smooth transcript scroll uses them), so a FADE is as cheap as a pop — and a background fade suits this variant better than a glyph appearing and vanishing, which is part of why it was chosen.

SKINNY APPLIES EVERYWHERE (all surfaces go through the one `draw_scrollbar` in `crates/app/src/render/scroll.rs`); AUTO-HIDE APPLIES ONLY TO THE STORY PANE.

ARTEFACTS: `/tmp/sb-{current,half,eighth,bg,halfnotrack}.png` and matching `.txt` reports.

NOTE FOR THE IMPLEMENTER: the candidates were rendered against advent.z8, whose lines are short, so none of them actually shows the crowding that prompted this. When verifying, drive a story whose prose wraps to the full pane width so text really does sit beside the bar.
