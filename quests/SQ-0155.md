---
title: BeyondZork setup-screen false room
status: open
type: feature
priority: low
created: 2026-07-03T15:27:33Z
commits: []
context: '[TODO-fc4ab5] BeyondZork setup-screen false room. At game start a spurious room is added named after the player''s character (e.g. "Frank Booth", or the custom name), because the player-name entry on the character-setup screen is shown in a position the room detector reads as a room name. Suppress room capture during the pre-game setup/character-creation screens (detect the setup context, or defer first-room capture until real play begins). [automapping; zvm location.rs room detection + app first-room gating] — completed 2026-07-02. ROOT CAUSE: the setup character-sheet row 1 (`Frank Booth  …  Level 0 Male Peasant`) is status-line-shaped, so it parses to `NameOnly("Frank Booth")`, and `apply_turn` observed every NameOnly into the map. RESOLUTION (app first-room gating, session.rs `apply_turn`): ignore an unvalidated NameOnly location while the map graph is still empty — equivalent to "until a real object-backed room exists", since NameOnly-while-empty is suppressed so the first room to populate the map is always object-backed; NameOnly still works as a mid-game fallback thereafter. The pre-game "Frank Booth" NameOnly is dropped; VT220 play''s PlayerParent "Hilltop" seeds the map normally. Test: `apply_turn_gates_nameonly_until_first_real_room`.'
tags:
  import: babelmap
  source_file: COMPLETED.md
---
