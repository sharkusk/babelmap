---
title: In-game graphics
status: open
type: feature
priority: low
created: 2026-07-03T15:27:36Z
commits: []
context: 'In-game graphics: restart Pict resolution is asymmetric with launch. `reset_game` (crates/app/src/main.rs) only has `story_bytes`, so it re-parses a self-contained `.gblorb` for Pict images but can''t find a sidecar `.blorb` (launch uses `blorb::resolve_sound_blorb(&story_path)`). A bare `.ulx` + sidecar `.blorb` loses in-game images after RESTART (`.gblorb`, the common case, is fine). Fix: thread `story_path` (or the already-resolved Pict Blorb) into `reset_game` so restart matches launch. Also add a `.gblorb` restart test covering the `pict_blorb = Some` branch — currently only the `.ulx` (`pict_blorb = None`) path is exercised, and asserting the rebuilt session''s `graphics_enabled` would need a getter on gvm''s `Machine` (`pub(crate)` today; keep zero-dep in mind). Not a regression (restart had no graphics before this feature). [Glulx graphics; crates/app/src/main.rs reset_game]'
tags:
  area: APP
  category: Glulx graphics
  horizon: Mid
  import: babelmap
  source_file: TODO.md
---
