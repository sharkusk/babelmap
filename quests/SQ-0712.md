---
title: 'mysterious01.z6: the story text window and the graphics window are both placed at (1,1), so prose renders on top of the artwork'
status: open
type: bug
priority: low
created: 2026-08-09T15:16:44Z
commits: []
context: |-
  branch: main
  head: 3d01539e
  cwd: /Volumes/Videos/Source/babelmap
  current: SQ-0708

  Reported after answering 'n' to the resume prompt: /dump-windows shows win0 (Buffer, 640x140) and win1 (Graphics, 640x260, 95 paint runs) BOTH at origin (1,1), so the text overlaps the art instead of sitting clear of it.
---

--- note 2026-08-09T15:17:04Z ---

Reporter's /dump-windows, verbatim, taken in the failing state (after answering 'n' to the resume prompt):

v6 layout — current window 0, input window 0
  render path: cell — modal overlay open: palette
  win0  640x140 at (1,1)  [wrap scroll transcript buffered]  <- current+input
          game: attrs=1111, 0 paint run(s), 0 prose line(s)
          model: Buffer — the story window
          cells: NOT DRAWN this frame
  win1  640x260 at (1,1)  [buffered]
          game: attrs=1000, 95 paint run(s), 0 prose line(s)
          model: Graphics
          cells: NOT DRAWN this frame
  ring: plan letterbox, ring not clipped
  cell size: terminal says 8x18 px (terminal-reported); glk_pixel_scale -> game sees 8x18; pane 89x51 cells implies 712x918 px to the game
  band uploads since launch: 3
  band 89x14@(1,1): cache HIT · proto 89x14 · placed 89x14
  band 89x30@(1,22): cache HIT · proto 89x30 · placed 89x30
  recent render paths (oldest first): hybrid-ring x19 · cell — modal overlay open: palette x5

What the dump establishes before anyone starts guessing:

- BOTH windows are at origin (1,1). win0 is 640x140 and win1 is 640x260, so the text window sits entirely inside the graphics window's box. That is the reported overlap, and it is visible in the MODEL — so the question is whether the game ever moved win0 and we dropped it, or whether it never moved it and the fault is ours in how a 640x140 buffer over a 640x260 graphics window should be composited. Trace the screen ops (machine.trace_screen -> screen_trace) for @move_window/@window_size on windows 0 and 1 through the boot and the 'n' answer; do NOT infer intent from the window list alone.
- win1 carries 95 PAINT RUNS. Those are painted text runs at absolute pixel positions, not prose — so this game paints into its graphics window, which is the same v6 "text is paint" territory as SQ-0697.
- win0 reports 0 paint runs and 0 prose lines, i.e. the story text the reporter sees is coming through the TRANSCRIPT (win0 is [wrap scroll transcript buffered]), not through the window model.
- Both windows say "cells: NOT DRAWN this frame" and the render path is "cell — modal overlay open: palette", because a palette overlay was up when the dump was taken. The preceding 19 frames were hybrid-ring. Re-take the dump WITHOUT an overlay open before drawing conclusions about what the ring did.
- The pane is 89x51 cells at 8x18 px = 712x918 px, against a 640x400 native screen — so this is also a letterbox/scale case, not a 1:1 mapping.

Fixture is present: stories/mysterious01.z6 (27644 bytes). Note stories/ is gitignored and Frotz relocates story files when it opens them, so confirm the file is still there if a probe starts SKIPping.
