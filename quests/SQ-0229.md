---
title: Glulx save-format interop testing (deferred from SQ-0158)
status: partial
type: feature
priority: low
created: 2026-07-09T03:53:23Z
commits:
  - 69f44809d3583f0542243715ddf9a9c08445b123
context: |-
  branch: sq-0158-save-interop
  head: 0aa9768
  cwd: /Volumes/Videos/Source/babelmap

  Split out of SQ-0158 after a spike showed Glulx interop needs a real toolchain investment; the Z-machine half ships in SQ-0158. Findings to reuse: (1) cross-load equivalence oracle + plan are in docs/superpowers/{specs,plans}/2026-07-08-save-interop-testing*; (2) glulxercise.ulx (public-domain, github.com/erkyrath/glk-dev/unittests) is a POOR fixture — its only restore-observable state is a failure counter (vacuous on a correct interp), so AUTHOR a tiny counter.ulx instead (needs `brew install inform6` + the I6 library); (3) homebrew glulxe 0.6.1 is linked against libncurses (glkterm/curses) so it is NOT headless-scriptable — either build glulxe/git against cheapglk (installed, keg-only) for a dumb frontend, or have the user generate the golden .glksave interactively; (4) gvm save format is Glulx-Quetzal FORM IFZS (CMem + Glk chunk).
---

--- note 2026-07-12T02:18:45Z ---

Spike 2026-07-11 (re-confirmed toolchain reality; user chose to defer again). Findings for next attempt: (1) FORMAT PREMISE IS SOUND — gvm::Machine::save_state (exec.rs:1439) emits standard Glulx-Quetzal `FORM IFZS` with IFhd+CMem+Stks+MAll (plus extra GReg + `Glk ` chunks a foreign terp ignores), so a real cross-load test would genuinely verify the Stks/CMem byte layout is spec-conformant. (2) homebrew glulxe is STILL curses-only (otool shows libncurses.5.4.dylib) → not headless-scriptable; a headless reference needs glulxe/git compiled from source against cheapglk (static lib libcheapglk.a + include/ ARE present at /opt/homebrew/opt/cheapglk; source+Glk headers must be fetched). (3) NO fixture exists — crates/gvm/tests/fixtures/ is empty; glulxercise is vacuous (only failure-counter observable state). inform6 IS brew-installable (formula `inform6` 6.44, not yet installed) → author+compile a tiny counter.ulx. (4) zvm interop half already shipped (crates/zvm/tests/fixtures/interop/minizork-at-P.qzl + PROVENANCE.md). Leaner alternative if full toolchain stays unwanted: a zero-dep CI STRUCTURAL-CONFORMANCE test (parse gvm's own save_state output; assert IFhd==mem[0..128], CMem RLE decompresses to memsize, Stks parses as well-formed Glulx call frames, MAll/padding correct) — catches format drift without any foreign interpreter, but can't catch a save/restore-shared spec misreading.
