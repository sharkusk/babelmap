---
title: '`--user-dir` relocates config writes but not the config READ, so it seeds and saves a file the app never loads'
status: open
type: bug
priority: low
created: 2026-07-30T02:36:11Z
commits: []
context: |-
  branch: main
  head: baa142ca
  cwd: /Volumes/Videos/Source/babelmap

  Surfaced while testing the SQ-0573 config seeding: `resolve` reads `config_path(cli)`, which is `--config` or `default_user_dir()/config.toml` and ignores `--user-dir` entirely, while `auto_seed` and `write_config` both write to `cfg.user_dir/config.toml` — so `babelmap --user-dir /tmp/x` seeds /tmp/x/config.toml but loads ~/.babelmap/config.toml, which is also why the "no story given" message names the home path under an override.
---
