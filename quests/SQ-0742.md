---
title: 'Journey in hybrid: the ring frame doesn''t scale to the pane width, and clicks land far from where they are drawn'
status: partial
type: bug
priority: low
created: 2026-08-09T23:33:40Z
commits:
  - c7bd0bd83783c1bed17e666dc5480c9d6486ef13
  - 8c9d150c3fd8a0a0fad636717f73f012956697fe
context: |-
  branch: main
  head: f4ed49a2
  cwd: /Volumes/Videos/Source/babelmap
  current: SQ-0708

  Both spotted by the user in the same session running Arthur from the Amiga disk image, with raster looking correct — which points at the hybrid ring's own pane transform rather than at anything the profile changed.
---

--- note 2026-08-09T23:33:59Z ---

User's report 2026-08-09, Arthur booted from the Amiga disk image:

> "in amiga arthur, in hybrid mode a nice border frame is being drawn, but it doesn't scale to our window width (raster mode looks great). also, hybrid mouse click not accurate (if you click way above the menu you will get a hit)"

UNIFYING HYPOTHESIS — these are probably ONE defect seen twice, and that is why they are filed together. The hybrid ring maps native game pixels onto the pane through a scale-and-offset, and the click map is the INVERSE of that same transform. If the transform is wrong, the frame is drawn at the wrong size AND clicks land at the wrong game pixel — which is exactly a frame that doesn't fill the width plus a hit registering well above the thing you aimed at. Raster looking correct fits: it composites a full frame and does not use the ring transform at all.

Test the hypothesis directly rather than fixing two things: derive the pane→native transform the ring used for a frame, compare it against the transform the click map inverts, and see whether they agree. If they do, these are two defects and should be split.

**FIRST QUESTION, and it is the user's to answer — asked of them:** does this reproduce with `stories/Arthur.blb` (the ordinary Blorb path, no disk image)? That separates a regression introduced by the interpreter profile from a pre-existing hybrid-ring defect that the Amiga route merely made visible. Do not start until that is known; the two answers point at completely different code.

PRIOR ART, related but distinct — read before theorising:
- **SQ-0706 defect A** recorded the same *shape* for scopa: clicks worked in raster and not in hybrid, diagnosed then as "the pane→native-pixel transform for the hybrid ring/letterbox must be landing clicks somewhere the game does not recognise. `V6ClickMap::map_click` and the hybrid scale/offset are where to look."
- **SQ-0738**'s agent found fmvpoker's click failure was NOT a click-map problem — both modes took the same path and recorded a byte-identical map (`img_x 16, img_w 768, img_h 480, native 640x400`, an exact inverse of the letterbox). So the click map is demonstrably correct on the takeover path. Arthur in hybrid takes the RING path instead, which is the one never cleared.
- **SQ-0563** (cell-granular clicks make sub-cell buttons unreachable) is a different, narrower defect; Arthur's menu items are large.

Corpus safety when a fix comes: the ring transform is shared by every v6 game that keeps a viewport — Zork Zero above all. Its frame-by-frame render pins are the guard.

--- note 2026-08-09T23:40:10Z ---

PARTIAL ANSWER from the user 2026-08-09: "journey.z6 using blorb with amiga interpreter set behaves the same as the amiga disk version."

WHAT THIS SETTLES: the behaviour follows the **interpreter profile**, not the container. A Blorb with the Amiga interpreter selected reproduces what the disk image does. That is the profile working as designed — the medium picks a default, it is not itself the mechanism.

WHAT IT DOES **NOT** SETTLE, and the question on this quest is still open: whether Arthur's hybrid ring defect is a REGRESSION introduced by the Amiga profile or a PRE-EXISTING hybrid-ring defect the Amiga route merely made visible. That needs `stories/Arthur.blb` with the **default (IbmPc)** interpreter — not with Amiga selected. Still asked of the user.

WHAT IT CHANGES PRACTICALLY, and this is large: **every Amiga-profile defect is now reproducible from an in-repo fixture.** `stories/Journey.blb` and `stories/Arthur.blb` plus an explicit interpreter number gives the same behaviour as the disk image, so tests do not need the `.adf` files — which live outside the repo, are the user's own media, and could never be test fixtures. Any harness for this quest, SQ-0740 or SQ-0741 should be built that way.

--- note 2026-08-09T23:40:45Z ---

CORRECTION 2026-08-09 — the affected game is **Journey, not Arthur**. User: "sorry, i misspoke previously. it wasn't arthur that was showing this problem. it was journey!" Title updated. Read every earlier mention of Arthur on this quest as Journey.

This also re-frames their other message, which now reads as the same investigation rather than a separate data point: "journey.z6 using blorb with amiga interpreter set behaves the same as the amiga disk version" is a report about **this defect** — the ring frame not scaling and the clicks landing wrong reproduce from `stories/Journey.blb` with the Amiga interpreter selected, exactly as they do from the disk image. So the container is not the variable; the profile is.

THE OPEN QUESTION IS THEREFORE NARROWER AND SHARPER, still the user's to answer: does it reproduce with `stories/Journey.blb` under the **default (IbmPc)** interpreter? Same fixture, same renderer, only the profile changing.
- If YES → a pre-existing hybrid-ring defect that the Amiga route merely made visible, and the profile is exonerated.
- If NO → the profile changed something the ring transform depends on, and the diff between the two runs localises it immediately.

Journey is a good specimen for this quest independently of the correction: it is one of the titles whose plate reaches all four screen edges (measured during SQ-0729 — its title is a solid full-screen plate, which is why the "encloses" predicate had to be narrowed to "encloses AND does not fill"), and it keeps a real viewport with a left picture column and a bottom command menu. So it exercises the ring path hard, and its frames are already pinned frame-by-frame in the corpus tests that any fix must not move.

--- note 2026-08-09T23:43:49Z ---

KEY MECHANISM, from the user 2026-08-09 — and it undercuts the one-transform hypothesis recorded above:

> "journey.z6 uses reversed space to draw borders (with ibmpc). the amiga interpreter switches those to line drawing."

**The border is TEXT, not artwork.** Under IbmPc the game draws it with reverse-video spaces; under Amiga it switches to line-drawing characters. Either way the frame is character cells the game printed, not a picture the hybrid ring composited.

So symptom 1 ("doesn't scale to our window width") is probably NOT a ring transform defect at all. It looks like the SQ-0727 family: the game draws its border against its own 640px screen at its own column count, and hybrid renders those runs as terminal cells in a pane with a different number of columns, so the border stops short. Raster looks right for the matching reason — it rasterises the runs at the game's own pixel positions into the 640x400 composite and scales the whole frame, preserving proportion.

CONSEQUENCES:
- The two symptoms may be **separate defects that merely appeared together**. The click inaccuracy can still be a ring/letterbox inversion (the path scopa was on in SQ-0706, and the one SQ-0738 never cleared); the border is text placement. Decide on evidence, and split this quest if they diverge.
- The IbmPc-vs-Amiga comparison is now worth more, for a new reason: **the game emits different characters under the two profiles.** If the border misfits only under Amiga, suspect the metrics of the glyphs it switched to — ZMSD's character-graphics font (font 3) has its own metrics, and a font selected specifically for drawing borders is exactly what a cell path can get wrong in width.
- A fix that stretches or repeats a text border to the pane width is a placement rule with corpus reach, not a Journey tweak. Zork Zero and Shogun both paint chrome text through the same cell paths.

The running agent has been sent this.

--- note 2026-08-09T23:44:31Z ---

DECISIVE DATA POINT, user 2026-08-09: **"ibmpc mouse clicks work properly."**

The click inaccuracy is therefore **Amiga-profile-specific**, not a pre-existing hybrid-ring defect. The ring transform does not change between profiles and clicks are correct under IbmPc, so the ring is exonerated and the SQ-0706 scopa lineage is a red herring here. The comparison this quest was blocked on is answered.

**REVISED UNIFIED THEORY — the two symptoms share a cause after all, but not the one first proposed.** Under the Amiga profile, Journey's TEXT RUNS are drawn at the wrong positions. That one fault yields both:
- the border (which is text — reverse-video spaces under IbmPc, line-drawing glyphs under Amiga) stops short of the pane edge; and
- clicking a menu item where it is drawn misses, because the drawn position no longer matches the game's hit test.

It also explains the user's oddest detail precisely — *"if you click way above the menu you will get a hit"*. If the drawn menu sits BELOW the game's real hit boxes, clicking above the drawn menu lands on them. That is exactly SQ-0738's shape (fmvpoker's labels drawn one text row above their hit band, so the drawn row was inert and the empty row below it worked), in the opposite direction.

WHAT TO MEASURE, in order:
1. **The glyphs.** The game switches to line-drawing characters under Amiga. If they come from ZMSD's character-graphics font (font 3), its metrics differ from the normal font, and a run measured at the wrong width lands wrong and stops short.
2. **Font cell metrics carried by the profile** — anything changing pixels-per-cell moves placement and the click inverse together.
3. **Screen / standard-window geometry per profile.** Journey.blb declares a `Reso`, so this should not differ; verify rather than assume.

The concrete first measurement: render the same Journey frame under both profiles, dump each text run's declared native position alongside the cell it was drawn at, and diff. Whatever moved is the defect.

The running agent has been sent all of this.

--- note 2026-08-09T23:45:58Z ---

SCREENSHOT ANALYSIS 2026-08-09 (user-supplied, Journey hybrid under the Amiga profile). Measurements approximate, from a 723px-wide capture, but the shape is unambiguous.

**The border and the transcript disagree about how wide the screen is.**
- The outer line-drawing box spans roughly x 13..650, and so does the bottom panel with its "The Party" / "Individual Commands" sub-boxes.
- The transcript text runs to roughly x 710 — PAST the border's right edge.
- The border's vertical rule visibly cuts THROUGH the prose: `"...seems; an | there"` and `"a few who can recall those years, a d| fewer"`, with the box rule sitting between a word and its continuation.

Ratio 637/697 ≈ 0.91, about what you get if the border lands at the game's 80 columns while the transcript wraps at ~87. So the frame is not "too small" in isolation; the two disagree.

**WHY IbmPc LOOKS FINE — hypothesis, and it points at code changed today.** Under IbmPc the border is reverse-video SPACES, i.e. runs with an explicit background, and `fill_explicit_bg_rows` pads such a row out to its full window width (SQ-0519, narrowed by SQ-0721 to "a window is a bar only when its runs reach both of its edges"). An IbmPc border therefore gets stretched to the window edge automatically and looks correct **by accident of that rule**. Under Amiga the border is line-drawing GLYPHS — discrete characters with no background band — so nothing stretches them and they land at their literal column count while the prose wraps wider.

**IF THAT HOLDS, THE DEFECT IS NOT THE BORDER.** It is that **the transcript wraps to the pane width instead of the game's declared story-window width**, and IbmPc merely conceals it because its border stretches to meet the prose. The fix would then belong on the transcript side, and SQ-0697 is the adjacent work — it made a story window's box PLACE its transcript without also constraining it to the box's WIDTH.

This also bears on the clicks: if the transcript uses a width the game did not declare, mapping a clicked cell back to a game pixel through that width lands off — consistent with "click above the drawn menu and you get a hit".

TO VERIFY, not adopt: dump the story window's declared box, the pane's column count, and the column at which the transcript actually wraps, under both profiles. If the wrap column tracks the pane rather than the box, that is it.

CORPUS CAUTION, now sharper: the transcript wrap width is shared by every v6 game — Zork Zero, Shogun and Arthur all wrap prose in a viewport. Their frames must stay byte-identical.

The running agent has been sent this.

--- note 2026-08-09T23:46:59Z ---

ROOT CAUSE CONFIRMED 2026-08-09. User: **"if i resize the window to match everything, the mouse click works!"** — with a second screenshot at the matching width.

At that width the frame is correct in every respect: the line-drawing border reaches both edges, the prose wraps INSIDE it instead of running past it, the illustration sits in its left column, and the bottom panel shows all its columns ("The Party" with Bergon/Praxix/Esher/Tag; Get Advice / Cast / Scout / Drop; Examine / Inventory; Tell Legend). Clicks land correctly. The capture is ~658px wide against the broken one's ~723px — they shrank the terminal until the pane matched the game.

So the width hypothesis in the previous note is the root cause, not a candidate, and **both symptoms are one defect**: when the pane is wider in columns than the game's screen, the transcript wraps at the pane width while the game's border stays at the game's width, and the click map goes through the wrong width. Make the widths agree and both vanish together. We now have a positive control as well as a negative one.

ACCEPTANCE CRITERION, and it is stronger than the original report: the goal is NOT "correct at the matching width". At ANY pane width, Journey's frame must look like that second screenshot — border meeting the prose, panel columns intact, clicks landing where things are drawn. Any test must sweep several widths INCLUDING deliberately wider than the game's screen, and assert the invariant. A fix that only holds when the numbers coincide is the same bug wearing a different hat.

OPEN DESIGN POINT, deliberately not decided: what the surplus columns become when the pane is wider than the game's screen — transcript constrained to the declared story-window width with the remainder as margin (which is effectively what the correct screenshot shows), versus centring or letterboxing. If that choice is visible enough to be the user's, it should be put to them rather than picked.

--- note 2026-08-09T23:48:10Z ---

FIX DIRECTION SET BY THE USER 2026-08-09: **"we already solved this problem for the ibmpc approach (with reverse space for borders)"**.

This reverses the direction floated in the previous note. The IbmPc behaviour — `fill_explicit_bg_rows` padding a reverse-video row out to its full window width (SQ-0519, narrowed by SQ-0721) — is **the established solution**, not an accident to be routed around. It is why IbmPc's border meets the prose at any pane width.

So: **extend the chrome to the pane, do not constrain the transcript to the game's width.** The earlier suggestion of clipping the transcript and leaving a margin is withdrawn — it would letterbox the terminal, which is the opposite of what this codebase does and worse than the current state.

MECHANISM IS HARDER THAN THE BAR CASE, and should not be rushed. Padding a reverse-video row is a background fill over a run with no internal structure. A line-drawing box has structure:
- horizontal rules are runs of a repeated glyph terminated by a corner: extending means repeating the rule glyph and relocating the corner, not filling;
- the right-hand vertical rule occurs once per row and must move to the pane's last column on every row of the box;
- the bottom panel carries INTERNAL vertical dividers (between "The Party", the command columns, etc.). What happens to those when the outer edge moves is a real decision — leaving them where the game put them and moving only the outer edge is probably right, but it should be chosen deliberately and shown in a PNG.

Prefer generalising the existing bar-padding path over adding a parallel rule — parallel rules for the same concept are how the hybrid/raster divergences in this codebase began — but only if the generalisation is honest rather than forced.

Acceptance criterion unchanged: at ANY pane width, including deliberately wider than the game's screen, the frame must match the user's correct screenshot — border meeting the prose, panel columns intact, clicks landing where things are drawn.

--- note 2026-08-10T00:11:16Z ---

CONTROLLED A/B, user-supplied 2026-08-09 — the Amiga render at the SAME pane width as the IbmPc reference. Approximate measurements off the two captures (786px and 784px wide; effectively identical panes):

**Amiga (broken):** line-drawing border box spans x ≈ 13..645, stopping short of the pane edge (~775); prose runs to x ≈ 765, past the border; the vertical rule cuts through words — `"Infocom, In│."`, `"years, an│ fewer still"`, `"speak of what f│llowed."`, `"and less wise.│It seems"`; the bottom panel also stops at ≈645.

**IbmPc (correct):** chrome bands reach the pane edge (~775); prose column x ≈ 296..752.

**WHAT IT SETTLES.** The prose column is roughly the same width in both (≈444px Amiga, ≈456px IbmPc). It is the CHROME that differs — 645 vs 775. So of the two branches posed earlier, it is the first: **the transcript's wrap column is not the variable; the chrome's end column is.** The fix is confined to padding the line-drawing chrome out to its box, as the user said, and does not need to touch how the transcript chooses its width.

That materially reduces blast radius: the transcript wrap path is shared with Zork Zero, Shogun and Arthur, and there is now evidence it can be left alone.

Caveat on these numbers: they are read off screenshots, good enough to point but not to build on. The agent has been asked to confirm the same conclusion from the model — chrome run end column and prose wrap column, both profiles, one pane width.

--- note 2026-08-10T02:03:06Z ---

REOPENED 2026-08-09 after `c7bd0bd8`. The path fix worked — Journey now takes the ring under Amiga and the outer frame reaches the pane — but four problems remain, and at least two look like regressions from the rule-collapse half. User:

> "the graphics on left is not honoring the frame (i.e. it is too wide). The border glyphs are slightly out of alignment at the top corner (perhaps they are from different ascii sections). The bottom menu columns are not aligned at most widths. At some widths you can see that we are rendering the left image behind the left panel (the top is peeking out)."

SCREENSHOT ANALYSIS (791px-wide capture; measurements approximate, good enough to point):

1. **The picture panel overruns the frame.** The lavender picture-window background spans x ≈ 10..335 with the illustration at x ≈ 32..302. It does not sit inside the frame's left column the way the correct 658px capture showed.

2. **The illustration is drawn BEHIND the panel and peeks above it.** A dark strip of the picture's own top (sky and mountains) is visible at x ≈ 18..320, y ≈ 30..55 — above the lavender panel, which begins at y ≈ 50. So the image and its window background are being placed by different rules, and the image is not clipped to its window.

3. **The bottom panel's labels are being EATEN.** It reads `─The─` and `─Individual C─` where the game prints "The Party" and "Individual Commands". The rule-collapse fill ("fills from the previous drawn run's end to the next run's start so corners and the JOURNEY title don't punch holes") is overwriting label text at the bottom while the top "JOURNEY" title survives. Whatever spares the top title is not sparing these.

4. **The bottom dividers are scattered and duplicated.** Content columns sit at x ≈ 15 (Proceed/Enter/Game), 145 (Bergon/Praxix/Esher/Tag), 310 (Get Advice/Cast/Drop), 440 (Examine/Inventory). Dividers appear at x ≈ 150, 305, 385, 435, 515, 560, 640, 690, 715 — three roughly align with content, the rest do not, and several sit in empty space right of "Inventory". "Tell Legend", present in the correct capture, is missing entirely.

DIAGNOSIS DIRECTION, not yet measured: (3) and (4) are the rule-collapse half of `c7bd0bd8` — the fill is too greedy horizontally and the "internal dividers spread proportionally" decision is not holding up at arbitrary widths. (1) and (2) are about the picture window, which that commit did not touch — so they may be pre-existing under the ring path and merely newly visible now that Journey reaches it, or they may be a second consequence of the path change. **Establish which before fixing**: compare the picture window's box and the image's drawn rect under IbmPc and Amiga at the same pane width, exactly as the chrome comparison was done.

The correct reference remains the user's 658px capture: tight line-drawn boxes, panel columns all present and aligned with their content, picture inside its column, prose stopping at the rule.

--- note 2026-08-10T02:44:52Z ---

SECOND PASS DONE, commit `8c9d150c` — "a lone line-drawing glyph is a divider, and stands in its own column".

WHAT WAS FIXED: the user's *"the bottom menu columns are not aligned at most widths"*. Root cause measured, not guessed. SQ-0509's fragment merge exists to re-assemble a WORD (a proportional-metrics game hands over "Churchyard" as three abutting runs), but a merged run is POSITIONED by its first fragment's scaled pixel and then advances one terminal CELL per character. Journey sets each party member's `-->` marker flush against the `▌` that divides the party column from the commands — `-->` ends at native px 246, `▌` starts at 248 — so the divider was glued on and rode the marker's character advance. It landed three columns left of its own column on the four rows carrying a marker, and at its true column on the "Game" row, which carries none. The panel's dividers zig-zagged.

Fix: a lone box-drawing or block glyph (U+2500..U+259F) is kept out of the merge and stamped at its own scaled column, alongside the repeated-glyph RULE the first pass already placed by pixels. A rule is a distance, a divider is a position; prose is neither. The class is deliberately the frame-drawing characters and not "punctuation" — Arthur emits one run per glyph and its apostrophes must keep merging into their words (the first pass's lesson, kept).

MEASURED BEFORE AND AFTER, headless sweep of 18414 configurations (cell metrics 8x18 / 10x20 / 7x15, both `honor_game_colours` modes, rows 26..56, cols 72..170, the exact game state from the screenshot): **15248 sizes had a ragged divider before, 0 after.** "At most widths" was literally 83%.

FALSIFIED, verbatim, by disabling the new branch:
```
honor=true w=96: the menu's '▌' dividers stand in 3 different columns ([20, 40, 41]) —
a divider is a POSITION, and every body row must put it in the same one
 45|│Proceed          ▌ Bergon       -->▌                   │                   │                  │|
 46|│Back             ▌ Praxix       -->▌   Cast            │  Examine          │                  │|
 47|│                 ▌ Minar        -->▌   Scout           │                   │                  │|
 48|│                 ▌ Tag          -->▌   Drop            │  Inventory        │                  │|
 49|│Game             ▌                  ▌                  │                   │                  │|
```
Column 40 on the four marker rows, 41 on the "Game" row — the reported symptom exactly. At a 138-column pane the same drift is three columns (51 vs 54).

New test: `journey_amiga_menu_dividers_line_up_down_the_panel` in `v6_journey_amiga_frame.rs`, swept over the existing 80/96/110/124/138/150 widths × both honour modes. It pins two things: every body row puts each divider in the SAME column, and each divider stands the same distance (±1 for sub-cell rounding) from the column it heads, since the game drew both 16 native pixels ahead of their text.

BLAST RADIUS: the branch is inside `collapse_row_rules`, which only `draw_chrome_text_strip` calls, which only the hybrid ring's two Text-strip call sites reach — so raster is untouched by construction. The IBM PC profile draws these dividers as reverse-video SPACES, which carry no box glyph and take the old path unchanged. Gates: full test gate 0 errors, 106 `Running`/`Doc-tests` = 106 `test result:`, 0 SKIP, clippy clean.

THE OTHER THREE ITEMS ARE SPLIT OUT TO **SQ-0747** with their measurements: (A) the picture PANEL FILL overrunning the frame's inner rule — measured PRE-EXISTING and identical under IbmPc, the flank band always runs to the viewport rather than to the rule; (B) the top corner glyph out of alignment with the vertical below it — a floor/ceil vs round mismatch between the text strip and `flank_divider_extension` on one shared boundary; (C) the picture's top peeking above its panel — a leftover one-row full-width art band that IbmPc never produces at all, so a ring defect Journey/Amiga is the first to hit, and one whose fix reaches Zork Zero's and Shogun's flanks. Not fixed here because none is cheap-and-safe, per the brief.

ALSO RECORDED ON SQ-0747 so it is not chased again: the screenshot reading that had the panel's LABELS being eaten (`─The─`, `─Individual C─`) does not reproduce in the model at any of those 18414 sizes, and cannot happen analytically — a rule's left edge is the previous span's END, so it cannot reach back into the label before it. If it survives `8c9d150c`, the missing variable is something the headless harness does not model (most likely real kitty/sixel compositing above the cells) and a fresh screenshot with the terminal size stated would settle it.
