---
title: 'Journey''s picture column: the panel fill overruns the frame''s inner rule, and a leftover full-width band paints the picture''s top above it'
status: done
type: bug
priority: low
created: 2026-08-10T02:43:48Z
completed: 2026-08-12T01:56:30Z
commits:
  - 5fbe02d558d6dee248c059a5bcabc8fabe4fd0aa
  - 44a50336861cdc3ab9f20413cc9786d4bf2a7a9b
  - 2d0807d51c6e8d0e9a00dd0aaa9a1b8496aee63e
  - d309aad4e5ac849bb3ff48c1d15d4e3eeec63b11
  - 2292f05796bcf1e4fcbba97e40c925011983be2b
  - 27305c98a9957b79c48580c4404b183fc43c6220
  - a0e933ce0124627c57e1ddde26f3475f3282e0ea
  - 6012653be18bbfc2ec0cca649980bb8dc636dbd8
context: |-
  branch: main
  head: 2dd327cc
  cwd: /Volumes/Videos/Source/babelmap
  current: SQ-0708

  Split off SQ-0742's second pass: measured under both interpreter profiles and found to be ring defects, not rule-collapse regressions, so left out of that quest's fix.
---

--- note 2026-08-10T02:44:26Z ---

MEASURED, not inferred. Split off SQ-0742's second pass. The user's report, Journey in hybrid under the Amiga profile:

> "the graphics on left is not honoring the frame (i.e. it is too wide). The border glyphs are slightly out of alignment at the top corner (perhaps they are from different ascii sections). ... At some widths you can see that we are rendering the left image behind the left panel (the top is peeking out)."

SQ-0742's second pass fixed the fourth item (the crooked menu columns, commit 8c9d150c) and measured these three rather than fixing them. The controlled A/B is IbmPc vs Amiga at the SAME pane, cell 8x18, honour on, headless hybrid, Journey at its party menu (`stories/journey-r83-s890706.z6`, six inputs from boot).

**(A) THE PANEL FILL OVERRUNS THE FRAME'S INNER RULE — PRE-EXISTING, IDENTICAL UNDER BOTH PROFILES.** Not "the graphics are too wide": the ILLUSTRATION is placed correctly. It is the Menu-plan flank PANEL FILL (`menu_flank_panel` → `fill_pane_page(band, bg, buf)`) that floods the entire side band, and the band always runs from the pane's left edge to the VIEWPORT's left column — which is one to two columns past the frame's inner rule.

| pane | profile | inner rule (native px → col) | picture image cells | side band cells | overrun |
|---|---|---|---|---|---|
| 96x51  | IbmPc | 233 (rev. space) → 35 | (0,0,35,20) | (0,0,**36**,45) | 1 |
| 96x51  | Amiga | 257 (`│`) → 38        | (1,1,37,18) | (0,2,**40**,42) | 2 |
| 110x51 | IbmPc | 233 → 40              | (0,0,40,23) | (0,0,**42**,44) | 2 |
| 110x51 | Amiga | 257 → 44              | (1,1,43,21) | (0,2,**46**,40) | 2 |
| 138x51 | IbmPc | 233 → 50              | (0,0,50,29) | (0,0,**52**,42) | 2 |
| 138x51 | Amiga | 257 → 55              | (2,2,53,26) | (0,2,**57**,38) | 2 |

Same structure at every width, both profiles. IbmPc hides it because its "panel" colour and its border are the same reverse-video black, so the spill is invisible; the Amiga panel is a distinct colour and its border is a thin glyph, so the fill shows to the RIGHT of the rule. **Pre-existing under the ring path, newly visible because SQ-0742 routed Journey/Amiga to the ring.** Fix direction: the flank panel fill should stop at the frame's inner rule (which `flank_divider_extension` already locates), not at the viewport.

**(B) THE TOP CORNER GLYPH IS OUT OF ALIGNMENT WITH THE VERTICAL BELOW IT — a ceil-vs-round mismatch on a shared boundary.** The user's "perhaps they are from different ascii sections" is close: they are from different SUBSYSTEMS. The top border row is a TEXT strip (crisp glyphs at `run_cell` columns, which ROUND), while the vertical rules below it come from `flank_divider_extension`, whose `dcell0`/`dcell1` FLOOR and CEIL the same native pixel. Two roundings of one boundary, so the corner and the shaft it should sit on can differ by a column. This is the exact class CLAUDE.md flags ("watch for ceil-vs-round mismatches on shared boundaries").

**(C) A LEFTOVER FULL-WIDTH ART BAND PAINTS THE PICTURE'S TOP ABOVE THE PANEL — AMIGA-ONLY, WIDTH-DEPENDENT.** `/dump-windows` strips at the same panes:

- IbmPc, 96/110/138 x51: **no full-width strip at all.**
- Amiga 96x51: `strip:text(74 runs) (0,0,96,1)` then `strip:art (0,1,96,1)` — DRAWN.
- Amiga 110x51: `strip:text(74 runs) (0,0,110,1)` then `strip:art (0,1,110,1)` — DRAWN.
- Amiga 138x51: same two, but the art band is `skipped — no art behind it`.

The flank panel starts at row 2; that one-row full-width band at row 1 rasterises a slice of the chrome canvas across the pane and lands ABOVE the panel. In a halfblock dump at 110x51 row 1 reads `▄▄ ▄▄▄▄▄…▄▄` with ink only in cols 0..44 and 108..109 — i.e. the picture's own top sliver and the right border column, sitting one row above the lavender panel that should contain them. Exactly "the top is peeking out", and exactly "at some widths" (drawn at 96 and 110, skipped at 138).

WHY AMIGA ONLY: Amiga's frame has a TEXT top border row (game row 0), so `decompose_chrome_strips` carves row 0 as Text and leaves row 1 as a separate full-width Art band above a viewport that starts at row 2. IbmPc's picture window starts at native y=0 with no top border row, the viewport starts at row 0, and no full-width band is ever produced. So this is a RING defect that Journey/Amiga is the first title to hit — not pre-existing-and-visible like (A), and not caused by the rule-collapse either.

Fix direction, deliberately NOT taken in SQ-0742 because it has corpus reach (Zork Zero and Shogun flanks go through the same decomposition): a Menu-plan flank owns its columns top to bottom, so a full-width chrome band must not paint the columns a flank panel claims. Also worth checking the `.max(0.0)` clamp in `strip_has_art`/`draw_chrome_band`: a band whose native span maps entirely above the canvas is clamped to native row 0 and drawn anyway rather than skipped, which is what makes this width-dependent.

NOT REPRODUCIBLE, recorded so it is not chased again: the reading of the screenshot that had the bottom panel's LABELS being eaten (`─The─`, `─Individual C─`) does not occur in the model. 18414 configurations were swept — cell metrics 8x18 / 10x20 / 7x15, both `honor_game_colours` modes, rows 26..56, cols 72..170, and the exact game state from the screenshot (Bergon/Praxix/Esher/Tag with "Get Advice") — and "The Party" and "Individual Commands" were intact in every one, with no menu drawn twice and no extra dividers. The only defect at those sizes was the crooked column divider, which SQ-0742 fixed. Analytically a rule cannot eat the label before it either: its left edge is the previous span's END. If the user still sees eaten labels after 8c9d150c, the missing variable is something the headless harness does not model — most likely the real kitty/sixel image compositing ABOVE the cells — and a fresh screenshot at a stated terminal size would settle it.

--- note 2026-08-10T02:53:21Z ---

USER, 2026-08-09, three corrections/additions to the first pass:

1. The eaten menu labels (`─The─`, `─Individual C─`) DO reproduce constantly in hybrid — "all the time". Repro given: 100 columns, map pane closed. The user then clarified that the ROW COUNT MAKES NO DIFFERENCE, so the tall-terminal theory is dead. 100 columns is inside the first pass's swept range (72-170), so a clean sweep at that width means the missing variable is something the harness held fixed: most likely the map pane being closed (a different story-pane column budget at the same terminal width than the default split ever produces), or the game state (the sweep pinned one — Bergon/Praxix/Esher/Tag with "Get Advice"). Real terminal compositing above the cells is the fallback only if both come back clean.

2. "we are still using unmatched lines on the outside edges of the journey frame" — the outer edge of the frame is drawn from glyphs that do not match each other. This is reported as still-present after the first pass, and it is stated as a GLYPH mismatch, distinct from item 2 of the first pass's findings (the top corner's ceil-vs-round column mismatch, which is a POSITION defect). Measure before merging the two: dump the actual characters drawn along each outer edge and each corner and check whether they come from one box-drawing family or several (e.g. light vs heavy vs double vs half-block). A position fix will not fix a glyph-family mismatch and vice versa.

--- note 2026-08-10T03:06:04Z ---

THIRD PASS — the eaten labels chased to a null result at the user's stated size, and the "unmatched lines" measured. Commit `5fbe02d5` (height sweep). Worktree synced to `main` at `56b94fdb` before the gates.

## 1. EATEN LABELS AT 100 COLUMNS: STILL DOES NOT REPRODUCE. Here is exactly what was rendered.

Both coordinator leads were tested and neither is it.

**Row count is not the variable** (as the coordinator later confirmed from the user). Rendered Journey/Amiga at **100x71** and **98x69** — the second being the real story-pane content size at a 100-column terminal with the map closed, since `main.rs` hands `render_story_pane` the `draw_panel` CONTENT rect, which is the terminal minus the panel border on each side. Header row at 100x71, verbatim:

```
 63|│───────────────────────The Party──────────────────────────Individual Commands─────────────────────│|
 64|│Proceed           ▌ Bergon        --> ▌ Get Advice        │                   │                   │|
 65|│Enter             ▌ Praxix        --> ▌ Cast              │ Examine           │                   │|
 66|│                  ▌ Esher         --> ▌                   │                   │                   │|
 67|│                  ▌ Tag           --> ▌ Drop              │ Inventory         │                   │|
 68|│Game              ▌                   ▌                   │                   │                   │|
 69|└──────────────────────────────────────────────────────────────────────────────────────────────────┘|
```
Intact, both honour modes, both game states tried.

**Game state is not the variable.** Drove the game 80 inputs from boot, rendering EVERY state at 48x40, 60x50, 78x60, 98x69, 98x40 and 120x50 in both honour modes — 960 renders. Result: `STATES BAD = 0`. Not one eaten label at any pane ≥48 columns in any state, including the one from the screenshot (Bergon/Praxix/Esher/Tag with "Get Advice").

**WHAT DOES EAT THEM — the exact threshold, which is well below the user's pane.** Swept cols 24..120 at 40 and 69 rows:

> `rows=40: labels intact from 48 columns upward`
> `rows=69: labels intact from 48 columns upward`

Below 48 story-pane columns the header collapses, e.g. at 28 columns `"│──────The PartyIndividual C"` and at 24 `"│─────The PartIndividual"` — which is the closest thing in the whole search space to the `─The─` / `─Individual C─` reading. But it is not overwriting: it is the game's 80-column menu not fitting, so the inter-label rules shrink to zero width and the row runs off the pane edge. **48 columns is roughly what a SPLIT layout gives at a 100-column terminal** — worth asking the user to confirm the map pane really was closed, because a split pane lands right on this threshold.

Analytically it still cannot happen at 98 columns. Overflow needs the scale below one terminal column per native 8px cell, i.e. `min(cols·cell_w/640, rows·cell_h/400) < 1`; at 98 columns with an 8px cell that is 1.225, and no plausible cell metric or row count brings it under 1 while cols ≥ 80. And a rule cannot reach back into the label before it — its left edge IS that label's span end.

**CONCLUSION, stated plainly as asked:** at the user's stated pane this does not reproduce headlessly, so the remaining candidate is real terminal compositing above the cells (kitty/sixel band images drawn over the menu rows), which no buffer-level harness can see. To settle it, the useful things from the user are: a fresh screenshot **taken against `8c9d150c` or later**, the output of `/dump-windows` on that frame, and confirmation of whether the map pane was open. The original screenshot predates the divider fix.

## 2. "UNMATCHED LINES ON THE OUTSIDE EDGES" — MEASURED, AND IT IS A RENDERER MISMATCH, NOT A POSITION ONE.

Tallied every character drawn in the frame's outer-left and outer-right columns over the frame's whole row span. At 100x71 (rows 0..69):

| | left col 0 | right col 99 |
|---|---|---|
| `┌` U+250C / `┐` U+2510 | x1 | x1 |
| `└` U+2514 / `┘` U+2518 | x1 | x1 |
| `│` U+2502 | **x6** | **x6** |
| `▄` U+2584 | x1 | x1 |
| `▀` U+2580 | — | x2 |
| space | **x61** | x59 |

Same shape at 98x69, 110x51 and 138x51. The top and bottom rules are clean single-family box drawing (`─` U+2500 x91 plus the two corners).

So one continuous vertical line is produced by **three different renderers**:

1. **box-drawing glyphs** (U+2502) — but only on the SIX menu-strip rows at the bottom, plus the four corners, because those rows are chrome TEXT strips;
2. **an uploaded image** over the ~61 story rows, which is why those cells are blank in the buffer — `decompose_chrome_strips` makes every side band (width < pane) a single `ChromeStrip::Art`, wholesale, so the `│` runs the game printed on every story row are rasterised instead of stamped;
3. **block elements** (U+2580/U+2584) leaking into the edge column at the band boundary.

This is the user's *"perhaps they are from different ascii sections"*, and it is confirmed as a GLYPH-FAMILY/renderer split — **distinct from item (B)**, the corner's floor/ceil-vs-round COLUMN mismatch. Fixing (B) would not repair this and vice versa, exactly as the coordinator suspected. Note family 3 is an artefact of the halfblocks picker the harness uses; in the user's Ghostty the ~61 rows are a true kitty image, so on their screen it is family 1 (font `│` at the terminal's own weight) against family 2 (a bitmap of the game's `│` scaled by a non-integer factor). Those cannot match in weight, colour or antialiasing however well they line up.

FIX DIRECTION, deliberately not taken: the frame's own border COLUMN in a Menu-plan flank is TEXT the game printed, not artwork, and could be stamped as glyphs like the corners and menu rows already are — making all ~69 rows one family. `flank_divider_extension` already locates that column in native pixels. It is NOT cheap: the same function serves Zork Zero's and Shogun's flanks, where the border genuinely IS artwork and a glyph would be wrong, so it needs a measured "is there a chrome text run at this native column on this row" test plus corpus proof. That is a quest of its own, not a rider on this one.

## 3. WHAT SHIPPED THIS PASS

`5fbe02d5` — the divider regression test now sweeps HEIGHTS 30/51/71/84 against the same six widths and both honour modes (48 configurations), so the tall and short regimes stay covered. Behaviour unchanged. Re-falsified in the newly covered regime: disabling the box-glyph branch fails at `w=96 h=30` with `'▌' dividers stand in 3 different columns ([20, 40, 41])`.

Gates on the merged result (`main` at `56b94fdb`, which brought in SQ-0746's prose-window change): 0 errors, 106 `Running`/`Doc-tests` = 106 `test result:`, 0 SKIP, clippy clean. Not pushed.

PROCESS NOTE worth carrying: `cargo fmt -p app` reformats the ENTIRE crate (206 files, ~48k lines here) and would have collided head-on with the two agents live in `render/` and `session.rs`. It was reverted with `git checkout -- crates/app/` and the intended edits reapplied by hand. Do not run `cargo fmt` at crate scope in a shared lane.

--- note 2026-08-10T03:14:43Z ---

USER 2026-08-09, answering the pane-width question: **"nothing else is docked."**

So at a 100-column terminal the story pane really is ~98 columns, and the second pass's measurement stands unchallenged: labels are intact from 48 story-pane columns upward, 960 renders across every game state and six sizes came back clean, and overflow at 98 columns is analytically impossible (it needs `min(cols*cell_w/640, rows*cell_h/400) < 1`, which is 1.225 there). Both coordinator leads — row count and game state — are now dead, and so is the narrow-pane threshold.

That leaves the fallback the second pass named, and there is a concrete mechanism for it already measured on this very frame.

**HYPOTHESIS: the labels are not eaten in the cell buffer at all — a kitty image is composited OVER them.** The headless harness renders cells and would never see this; the terminal draws graphics above the text layer.

Two prior measurements make this specific rather than hand-waving:
- SQ-0750: `decompose_chrome_strips` turns every side band into a single `Art` strip wholesale, so whole regions of this frame are uploaded images rather than glyphs.
- SQ-0747 first pass, item 4: Journey/Amiga produces `strip:art (0,1,W,1)` — a FULL-WIDTH art strip at row 1 — drawn at 96 and 110 columns, skipped at 138. Its halfblock content at 110x51 was ink in cols 0-44 and 108-109.

A full-width art strip is an image spanning an entire row. If any art strip's placed pixel extent overlaps the menu header row, the terminal composites it over "The Party" and "Individual Commands" and the player sees exactly `-The-` / `-Individual C-` while the cell buffer holds the full labels. The same strip is what the user separately described as "we are rendering the left image behind the left panel (the top is peeking out)" — one defect with two visible faces.

HOW TO TEST IT: stop asserting on cells. Dump every art strip's PLACED PIXEL RECT and every text run's placed pixel rect for the Journey menu frame at 100 columns, and check for overlap. The question is not what the cells contain but which strips cover them, and in what order they are drawn.

Note the width-dependence already recorded (drawn at 96 and 110, skipped at 138) — that is the shape of a defect that appears "at most widths" and vanishes at others, which matches the user's original wording better than any threshold effect does.

--- note 2026-08-10T03:38:12Z ---

--file /private/tmp/claude-501/-Volumes-Videos-Source-babelmap/8882ca52-c45d-4607-bc1a-10714e553fba/scratchpad/note.txt

--- note 2026-08-10T03:46:23Z ---

DECISIVE USER EVIDENCE, 2026-08-09. Journey/Amiga at 140x71 terminal (pane 138x68), cell 8x18, hybrid.

- The truncation is present **without any resize** — launching straight into that size shows it.
- **Resizing does not fix it.**
- Going LARGER and then back to 140 makes it **worse** — part of "Individual Commands" gets truncated too.

That last point is the diagnostic one. A defect that ACCUMULATES across frames is not a property of any single frame's geometry, and every investigation so far — 18414 configs, then 960, then 22000 placement-level configs — rendered **one frame from scratch**. The model genuinely does not contain this defect, and that is consistent rather than contradictory: the defect is in the frame-to-frame **lifecycle** of uploaded images, not in any frame's layout.

MECHANISM, now with two independent sources of stranded placements:

1. Already located in code (SQ-0747's prior pass): in `screen.rs` an Art strip failing `strip_has_art` is `continue`d without being drawn, yet its key is still in the `live` set handed to `retain_chrome_bands` — so it is never dropped. Anything placed at that key on an earlier frame persists with nothing replacing it.
2. NEW, from the user's own dump: `recent render paths (oldest first): raster x2 - hybrid-ring x27`. Journey's opening frames go through the RASTER path, which uploads a **full-frame** image, and then the menu switches to the hybrid ring. If the raster full-frame placement is not deleted on that transition, a whole-screen image from the title sequence stays composited above the text — which would truncate labels at launch with no resize at all, exactly as reported. `band uploads since launch: 78` shows plenty of churn.

Both produce the same signature: images the terminal still displays that no current frame owns, invisible to every cell-based AND every placement-based single-frame harness.

WHAT THE NEXT INVESTIGATION MUST DO DIFFERENTLY: stop rendering one frame. Drive a SEQUENCE — boot through the raster title frames into the hybrid ring menu, then a resize larger, then back — and assert on the graphics op stream that every placement from a prior frame is either replaced or explicitly deleted. The invariant to test is "no placement outlives the strip that owns it", checked across transitions, not within a frame.

Live band state at the reported size, from the user's dump: `band 2x55@(137,3)` and `band 138x1@(1,68)` only. Note the row-1 full-width strip is absent at 138 columns (it is drawn at 96 and 110) — so at this width that strip's key is precisely one that can go stale.

Caveat on the dump: it reads `render path: cell - modal overlay open: palette` with all four windows `NOT DRAWN this frame`, so its window geometry describes the palette overlay, not the menu. The band list, cell metrics and path history are still live state and are what is used above.

--- note 2026-08-10T04:22:50Z ---

--file /private/tmp/claude-501/-Volumes-Videos-Source-babelmap/8882ca52-c45d-4607-bc1a-10714e553fba/scratchpad/note747.txt

--- note 2026-08-10T05:45:49Z ---

THE REAL FRAME, captured 2026-08-10T05:42:56Z via the fixed `/dump-windows` (SQ-0756). Journey/Amiga, hybrid, pane **163x61**, cell 8x18, scale 2.03. User: "amiga journey seems to be rendering the text in the correct spot now. however, still have the truncated 'The Pa'."

    pane 163x61 at (1,1) · story viewport 92x46 at (69,3)
    win1  640x400 at (1,1)   model: Grid — 633 run(s)
            cells: 163x45 at (1,1) as grid
    win2  368x272 at (265,17)  model: Buffer — the story window
            cells: 92x46 at (69,3) as viewport
    win3  248x272 at (9,17)   model: Graphics
            cells: 63x31 at (3,3) as art
            cells: 63x31 at (3,3) as grid
    chrome ring strips:
      strip:text(74 runs) 163x1 at (1,1)
      strip:art (skipped — no art behind it) 163x1 at (1,2)
      strip:art 68x46 at (1,3)
      strip:art 3x46 at (161,3)
    ring: plan menu, ring not clipped

**OVERLAP 1 — the menu header row is claimed twice.** `strip:text(74 runs)` occupies `163x1 at (1,1)`. `win1`'s grid occupies `163x45 at (1,1)` — the SAME row 1, the same 163 columns, plus 44 rows below. The header labels and win1's 633 paint runs are both drawn into row 1. Whichever draws second wins, per cell. That is a mechanism for eating a label's tail that no previous investigation looked for, because every earlier harness treated the chrome strips and the window grids as occupying disjoint regions.

This also explains the varying truncation across sightings (`-The-`, `-Individual C-`, `The Par`, now `The Pa`): what survives depends on which of win1's runs happen to carry content in those cells at that moment, not on any fixed geometry. A geometry defect would truncate identically every time; this would not.

**OVERLAP 2 — win3 is drawn twice at the same rect**, `63x31 at (3,3)` as BOTH `art` and `grid`. That is the picture-behind-the-panel duplication the user reported separately ("we are rendering the left image behind the left panel"), now visible in the dump rather than inferred.

MEASURE, do not assume: confirm the draw ORDER of the chrome text strip against win1's grid, and confirm which cells of row 1 win1 actually writes. If win1 writes blanks or background into the label's cells, that is the truncation. If it writes nothing there, this overlap is harmless and the cause is elsewhere — say so plainly.

Note the prose misplacement (SQ-0755) is reported FIXED by the user at this size; do not assume it is gone at every size, but it is not the subject here.

--- note 2026-08-10T06:11:13Z ---

FIFTH PASS — the two overlaps in the 2026-08-10 dump are both DUMP ARTEFACTS, measured not assumed. The truncation is still not in the model. Commit d309aad4.

## OVERLAP 1 — the menu header row claimed twice: HARMLESS. Here is the measurement.

Reproduced the user's dump EXACTLY (pane 163x61 at (1,1), cell 8x18, scale 2.03, viewport 92x46 at (69,3), win1 grid 163x45 at (1,1), the same four strips including the skipped row-2 one), with the session TRANSCRIPT fed in.

**win1 writes NOTHING into row 1, or into any row.** The `cells:` lines under each window come from `for pw in &layout.chrome { map.push(rec(kind, native, px_rect_to_cells(pw))) }` — a diagnostic MAPPING of every chrome window's native rect onto the pane, pushed unconditionally. In the hybrid ring path a chrome Grid is never drawn at that rect: it is rasterised into `build_chrome_canvas` and reaches the screen only through the strips, and `clear_text_rows` carves every text-strip run's native rows OUT of that canvas precisely so it cannot double-draw. win1 is a 640x400 grid, i.e. the whole screen, so its mapping covers the whole scaled canvas by construction and will overlap every strip in the dump, always, on every game.

Row 1 of the rendered buffer is written by exactly one thing, the `strip:text(74 runs)` strip, and it reads:
    ┌────────…────JOURNEY────…────────┐
So: the overlap does not exist. Stated plainly, as asked. The varying truncation across sightings still needs an explanation and this is not it.

## OVERLAP 2 — win3 drawn twice at 63x31 at (3,3): also the dump.

Journey publishes TWO layered leaves at native (8,16,248,272): a Graphics leaf (the picture) and a Grid leaf with **0 runs** — an empty placeholder for window 3. `/dump-windows` matches placements by native rect, so both mapping lines land under win3 and read as two draws. One of them carries nothing at all. Nothing is drawn twice.

## SO THE DUMP WAS FIXED, because two passes have now been spent on this

The hybrid-ring window records now say `grid (rasterised into the ring)` / `art (rasterised into the ring)`; only the primary Buffer is drawn at the rect beside it. And a `flank-divider` record now carries its native CROP, which is where SQ-0750's real defect was hiding — see that quest.

## THE HEADER LABELS AT THE USER'S OWN FRAME: STILL WHOLE

At 163x61 at (1,1), transcript fed, both profiles, both honour modes, the menu header renders verbatim:

    │───────────────────────────────────────The Party───────────────────────────────────────────────Individual Commands──────────────────────────────────────────────│

and no uploaded image covers row 49: the art strips are (1,3,68,46) and (161,3,3,46), the flank dividers (66,3,2,46) and (162,3,1,46), `menu:art` is (1,61,163,1), and the row-2 strip is skipped for having no art. The header row is a `menu:text(187 runs)` strip. Pinned as a regression case (`journey_menu_header_labels_are_whole_at_the_users_pane`) so the 18414/960/22000/800-configuration search space is never re-swept from scratch and so any future change that DOES start eating the labels fails in CI.

**Harness note for whoever picks this up next, and it invalidates part of every earlier sweep:** Journey's prose never reaches the story window's `lines`. It arrives as the session transcript and is drawn from `AppState`. `v6_journey_amiga_frame.rs`'s original harness renders `session.screen()` into a pane at the buffer ORIGIN with no transcript — an empty viewport at the wrong origin. The new cases render at (1,1) with `take_transcript()` fed in. That did not change the header's contents, but it is now the frame the user has rather than an approximation of it.

## WHAT SHIPPED, AND WHY IT MAY BE THE TRUNCATION AFTER ALL

SQ-0750's fix landed in the same commit: the left and right frame borders were being drawn as SOLID FILLED BLOCKS two and one terminal columns wide (a 1-pixel `│` stroke magnified 16x by `draw_chrome_band_stretched`), for their whole height, as uploaded images. Worth re-testing the truncation against this build before assuming it survives: two of the frame's four sides were images four to sixteen times wider than they should have been, and an image is what composites above the text layer. It does not explain a label eaten at column 47 of row 49 on the model — nothing does yet — but it is the first thing to change in the region since the user's last capture.

## IF IT SURVIVES, the next datum

Everything cell-level and everything placement-level is exhausted (four passes, recorded above). What has never been captured is the terminal's own view: the escape sequences babelmap actually writes for one of these frames. `~/.babelmap/dump-windows.log` now exists (SQ-0756); a companion capture of the frame's kitty traffic — or simply whether the truncation survives a forced full redraw — is the one measurement nobody has taken.

## GATES

`cargo test -p blorb -p zvm -p gvm -p scott -p app | grep -acE '^error(\[|:)| [1-9][0-9]* failed'` → 0. 113 Running/Doc-tests = 113 test result:, **0 SKIP** (`stories/` symlinked, so the real-game suites genuinely ran). `cargo clippy --workspace --all-targets -- -D warnings` clean. Not pushed.

--- note 2026-08-10T07:14:14Z ---

--file /private/tmp/claude-501/-Volumes-Videos-Source-babelmap/8882ca52-c45d-4607-bc1a-10714e553fba/scratchpad/n747b.txt

--- note 2026-08-10T14:18:47Z ---

USER 2026-08-10, at pane 157x61 (scale 1.96, cell 8x18, hybrid, Journey/Amiga): "the left side (graphics) are overruning and showing up in the menu below as garbage."

THE DUMP LOOKS CLEAN AT THIS SIZE, and there is a known reason it would. Full accounting from the 14:17:11 capture:

    cols  1..65    left flank   strip:art 65x47 at (1,3)
    cols  66..155  viewport     90x47 at (66,3)
    cols  156..157 right flank  strip:art 2x47 at (156,3)      = 157 ✓
    row   1        strip:text (74 runs)
    row   2        strip:art — SKIPPED, draws nothing
    rows  3..49    viewport + flanks
    rows  50..60   menu:text (475 runs) 157x11 at (1,50)
    row   61       menu:art                                     = 61 ✓
    win3 picture   61x29 at (3,3) → rows 3..31, inside the flank

Every row and column accounted for, picture well inside its flank, nothing overlapping. So the overrun is NOT in the strip geometry.

**LEADING HYPOTHESIS — the dump reports the STRIP rect while the panel draws at a different DEST rect.** SQ-0747's band-lifecycle pass established exactly this: *"A Menu-plan flank draws at `menu_flank_panel`'s dest rect; the band cache is keyed on where a band is drawn; `live` claimed only the strip rect."* That mismatch was the cache-eviction bug then; it is a reporting blind spot now. If the dest rect is taller than 47 rows, the picture is drawn straight into rows 50..60 — over the menu — and no strip in the dump would show it.

Corroborating detail from the same capture: the band list carries only `2x47@(156,3)` (right flank) and `157x1@(1,61)` (bottom). **The left flank is not in the band list at all**, at this or any earlier capture (159x61, 138x68 likewise). So the left flank does not go through the chrome-band path — it is drawn by `menu_flank_panel` directly, which is precisely the path whose dest rect is unreported.

TWO THINGS TO DO:
1. Fix the overrun — bound the panel's DEST rect to the flank's rows, not just its columns. The recent boundary fix bounded the panel by its two borders horizontally; check whether the vertical bound was left as the band's.
2. Fix the blind spot — `/dump-windows` must report the rect a flank panel actually DRAWS at, not only its strip rect. A diagnostic that cannot see the draw it is meant to diagnose has now cost two passes.

Size-dependent per the user ("for some sizes this looks great, but others we have some issues"), so sweep widths rather than fixing one pane. 157x61 is a known-bad size; 159x61 was reported good.

--- note 2026-08-10T14:20:29Z ---

SCREENSHOT, user 2026-08-10 07:18:52, Journey/Amiga hybrid. **This closes the truncated-label mystery: the picture is painting over the menu header.**

What the image shows:
- The left flank's picture occupies roughly the bottom sixth of the pane — mountains and orange sky — spanning the menu region and continuing to the LAST row.
- The menu header reads `───Individual Commands───` with **"The Party" entirely absent**; its columns are painted over by the picture. The right-hand label survives because the picture does not reach that far.
- The bottom hint bar is overpainted; only a `Ct` fragment survives at the far left.
- A thin sliver of the same picture also appears near the TOP of the flank, above the main picture area — consistent with more than one placement of the same image.

**Every truncation variant reported across this quest is one defect**: `-The-`, `-Individual C-`, `The Par`, `The Pa`, and now the label gone entirely, are the picture covering a different number of the header's leading columns as the scale changes with pane width. That explains, at last:
- why the amount eaten varied between sightings (scale-dependent coverage, not geometry);
- why five sweeps of the cell buffer found the labels intact (an uploaded image composites ABOVE the cell layer);
- why the 22000-configuration placement sweep also missed it (the flank's dest rect is unreported — the same blind spot now being fixed).

So the truncation is not a separate investigation and never was. It is the flank overrun, seen at the one row where text happens to sit.

ALSO VISIBLE, and belonging to SQ-0755 rather than here: Journey's title block still stands above the opening passage with `[Press any key to begin]` appearing TWICE. SQ-0755's fix is merged, so this is either a stale binary on the user's side or an incomplete fix — asked the user to confirm which. Recorded here only so the screenshot's contents are fully accounted for.

--- note 2026-08-10T15:42:48Z ---

--file /private/tmp/claude-501/-Volumes-Videos-Source-babelmap/8882ca52-c45d-4607-bc1a-10714e553fba/scratchpad/n747c.txt

--- note 2026-08-11T18:37:44Z ---

REOPENED as partial 2026-08-11 — item C's stray band is gone, but its removal left a HOLE rather than a border. Measured on a real capture (Journey ADF r30, hybrid, 117x64, kitty negotiated, HEAD ffd8dca7), surfaced by the SQ-0775 PNG:

```
  1 |│┌──────────────────────────────────────────────JOURNEY─────────────────...─────┐│|
  2 |│·············································································│|
  3 |│▒                                             ▒·It was a Golden Age, ...  ···▒▒│|
```
Row 2 is `·` — NEVER WRITTEN — across all of columns 1..115. The game's frame top is row 1, content starts at row 3, and row 2 belongs to nothing. That is the user's "one line gap at the top", and SQ-0742's notes already identified it as this quest's item C.

So the earlier fix removed the leftover full-width art band that used to paint the picture's top above the panel (that symptom is genuinely gone), but nothing subsequently claims row 2, so the frame's top border no longer meets its flanks — the flank bands themselves start at row 3, not row 2.

NOT a regression from today's SQ-0772 placement work: the pre-fix capture (Lane A, before 73adfeff) has row 2 blank in exactly the same way.

ACCEPTANCE: row 2 must either carry the frame (border glyphs or flank continuation) or not exist — i.e. the frame's top and the flanks' first row must agree. A test can assert it directly off the harness's cell grid: no fully-unwritten row between the frame's top border and its first content row.

--- note 2026-08-11T20:56:39Z ---

THE ROW-2 HOLE IS FIXED, commit a0e933ce (not pushed), same lane as SQ-0750 — one mechanism, one file.

## CAUSE

`story_viewport_box` quantizes the story's top edge OUTWARD to a whole cell while the top band runs down to that quantized row. The terminal row that falls between them is the half-cell the viewport rounded away, and it belonged to nothing: no runs map into it, so `decompose_chrome_strips` classes it Empty → Art, and `strip_has_art` then skips it. Journey ADF r30 at 117x64: terminal row 2 was never written across all 115 columns — a bare stripe through the picture panel and a one-row hole where the frame's top rule should meet its two side rules.

Measured at other widths the SAME row is the other half of this quest's title: where the picture's first native pixels fall inside it, the full-width band DOES draw, and what it draws is a one-row squashed slice of the whole canvas — the picture's top standing above its own panel, smeared across the pane. At a 96-column pane that row read `▄ ▄▄▄▄▄▄▄▄▄…▄▄            ▄`. So "the leftover full-width band paints the picture's top above it" was not gone; it had moved to the widths nobody had captured.

## FIX

A full-width Art strip immediately above the viewport is the quantization remainder — not chrome — when EITHER its own native span already lies inside the story box, OR it draws nothing at all. Such a strip is dropped and its rows are given to the SIDE bands, which have real content in those columns (their borders, their panel ground); the story's own columns keep the story's ground, which is what stands beside the prose one row lower.

WHOLE STRIPS ONLY, and that is load-bearing. A first cut walked ROWS and trimmed one off Zork Zero's banner: its sides went 8..61 → 7..61 and the banner 1..7 → 1..6, caught by the pty capture, not by any cell test. A band carrying real chrome art down to the viewport is one tall strip whose first row is above the story box, so neither test holds for it and it is untouched.

## BEFORE / AFTER (Journey ADF r30, 117x64, kitty)

```
BEFORE  2 |│·······················································································│|
AFTER   2 |││                                             │·····································││|
```

## TESTS

`crates/app/tests/v6_frame_border_medium.rs::no_unwritten_row_stands_between_the_frames_top_rule_and_the_story` — both honour modes, widths 80/96/110/115/138/150, two origins, two heights. Note the predicate: an untouched ratatui cell is blank + `Color::Reset` on BOTH channels + no modifier. `Style::bg.is_some()` is `Some(Reset)` on a cell nothing has touched, and a first cut of this case passed vacuously because of it.

FALSIFIED verbatim (absorption disabled):
> `Journey - The Quest Begins.adf None honor=true pane (0, 0, 115, 61): row 1 lies between the frame's top rule (ends at row 1) and the story's first row (2) and is written by nothing at all — the flanks must claim the gap the viewport's quantization opens`
and, with the continuity clause:
> `Journey - The Quest Begins.adf None honor=true pane (0, 0, 96, 61): flank-divider (glyph '│' style=00) starts at row 2 — the frame's side rule must reach the top rule (which ends at row 1), not the quantized viewport top (2)`

Gate 0, 124 Running = 124 test result:, 0 SKIP. Clippy clean. Zork Zero and Arthur re-captured after the fix: placement rects and row background maps identical to before.

## STILL OPEN ON THIS QUEST

The truncated menu label is NOT this. Nothing here touches it, and `journey_release_30_menu_header_labels_are_whole` still passes.

--- note 2026-08-11T23:44:28Z ---

STILL PARTIAL — the leftover full-width band moved rather than went away, and it now has a second symptom. User 2026-08-11, after the SQ-0779 fix (commit 3251fd2b): "when the art is full width we are now introducing blank row directly below the art (cutting through the side borders)" and "when this happens the mouse detect is off by one row".

REPRODUCED at HEAD 0ff85398. `stories/Journey - The Quest Begins.adf` (release 30 / serial 890322), hybrid, **121x36**, 8x18px cells, kitty negotiated, keys `cr,wait:800,cr,wait:800,cr`:

```
  20 |││▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒│     …  █│ │|
  23 |││▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒│     …  █│ │|
  24 |│▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒  …▒▒▒▒▒▒│|
  25 |││───────────────────────────The Party──────────…  ────│ │|
```
Placements:
```
  image rows  2..23 cols 2..48    (the picture, correctly inset since SQ-0779)
  image rows 24..24 cols 1..119   (119 cols x 1 row — the leftover band)
```
Row 24 is a FULL-WIDTH one-row image placement sitting between the picture's last row (23) and the menu header (25). It paints over BOTH side rules — note rows 20..23 carry `│` at columns 1 and 49 and end `█│ │`, while row 24 carries placeholder cells right across and ends `▒▒▒▒▒▒│`. So the frame's verticals are interrupted for exactly one row.

RELATION TO THIS QUEST'S EARLIER STATE: this is the same "leftover full-width band" recorded as item C, but BELOW the art instead of above it. SQ-0779's absorption drops a full-width Art strip lying immediately ABOVE the viewport (the quantization remainder) and therefore does not catch this one. Whatever rule absorbs the top remainder needs a counterpart for the bottom — or, better, one rule expressed in terms of "a full-width Art strip whose native span is the remainder of the picture's own box", regardless of which side of the viewport it falls on.

SECOND SYMPTOM, user-reported and NOT yet verified headlessly: **the mouse is off by one row when this happens.** The mechanism is plausible and worth checking first because it would be the same fault: a phantom row occupying terminal row 24 shifts every row below it by one in the strip decomposition, and the click map inverts that same decomposition (SQ-0742's lineage: "the click map is the INVERSE of that transform"). If so, removing the band fixes the clicks with no separate work — and a click test at this pane size is the falsification. Do NOT fix the click map independently before establishing that.

ACCEPTANCE: at 121x36 (and across a sweep of short panes, since SQ-0779 proved this regime is aspect- not width-dependent — `Letterbox` when `18·rows ≤ 5·cols`), no full-width Art strip stands between the picture's last row and the menu, the side rules are unbroken down the frame's full height, and a click lands on the row it is drawn on. Pin both `honor_game_colours` modes. Corpus: Zork Zero, Shogun and Arthur screen-identical at 117x64 AND at a short pane.
