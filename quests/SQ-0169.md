---
title: Story debug + disassembly
status: partial
type: feature
priority: low
created: 2026-07-03T15:27:35Z
commits:
  - 81ddd2c122a39c442a4c6690c08e428a5df731f9
  - a1e61cf1b62b747ad4eba09f06541ce348dcfc40
  - 9a93590cec55857d2eab1173647800b40abd090f
  - 6e00c65beeab1b80790249485200e127ac089caa
  - 08ee9bd15e5ebf93983e1391518048909233e86c
  - 4d5c9396319ec406d9703bcdb719e07517a80748
  - 0113edf09998e69dea6426ef5c26dafb1dedae16
  - 8c90ed69da117fd04785d78ede67886748745f15
context: 'Story debug + disassembly — a developer/curiosity feature riding the SP3b `Engine` abstraction''s `Debugger` capability seam. Per-engine tools (Z-machine ops != Glulx ops) surfaced through `engine.debugger() -> Option<&mut dyn Debugger>`, reusing each VM crate''s existing instruction decoder. Scope: a DISASSEMBLY view (decode the routine/region at an address into mnemonics), a STEPPER (single-step, run-to, breakpoints on PC/opcode), and INSPECTORS (memory hex/dump, the object tree, the call stack, globals/locals, the dictionary). Generic debug UI in the app works against whichever engine is loaded; zvm-cli/gvm-cli can expose the same headless. BUILD ORDER: Z-machine first (immediately useful for the existing games — zvm already has the decoder + object model + dictionary), Glulx second. Gate on the SP3b Engine/Debugger seam existing. Its own spec when we get there. [tooling/dev; new debug module per VM + app debug UI; rides the Engine abstraction]'
tags:
  area: APP
  category: tooling/dev
  horizon: Mid
  import: babelmap
  source_file: TODO.md
---

--- note 2026-07-19T14:37:21Z ---

TODO (queued behind the hover-tooltip feature — same files): Call Stack frames should be collapsible with ▶/▼ disclosure (same pattern as the Objects tree). Clicking a frame expands it to show that frame's locals as indented detail lines (one per local), collapse on re-click. Drop the inline `locals=[...]` from the collapsed frame line since expansion covers it. Reuse the ObjRow/objects_rows shared-display-model discipline (a CallStackRow model + a stack_click_at hit-test), and add expanded_frames state + a per-frame locals accessor. Must be built AFTER the hover-tooltip commit lands (both touch debug_panel.rs/main.rs/render/debug_panel.rs).

--- note 2026-07-19T14:44:49Z ---

TODO (queued, zvm-only — do after hover lands to avoid concurrent cargo builds): Add an `OpRole::VarRef` to the disassembler operand-role classifier for opcodes whose operand IS a variable number (indirect variable reference): load (1OP:0x0E), inc (1OP:0x05), dec (1OP:0x06), store (2OP:0x0D), dec_chk (2OP:0x04), inc_chk (2OP:0x05), pull (VAR:0x09) — operand index 0. A CONSTANT operand in a VarRef position renders as the variable it names via fmt_var (0->sp, 1-15->local{n-1}, 16-255->g{n-16:02x}) instead of `#nn`. push (VAR:0x08) is NOT included (its operand is a value). These rendered gNN/localN/sp tokens then inherit the hover-value tooltip automatically. Add zvm tests per role.

--- note 2026-07-19T15:01:42Z ---

TODO (queued behind call-stack expand — same files): Raw disassembly toggle. A key (propose 'r') in the Disassembly section toggles `raw_mode: bool` on DebugPanelState; refresh/goto re-render disasm in the chosen mode. RAW FORMAT (bytes + raw decode, no lookups): `{addr:06x}: {raw instruction bytes as hex, addr..next_pc}   {CLASS}:0x{opcode:02x}  {operands} -> {store} ?{branch}` where CLASS ∈ 0OP/1OP/2OP/VAR/EXT; each operand by FORM+raw value: Large=`L#{:04x}`, Small=`S#{:02x}`, Var=`V{:02x}`; store=`-> V{:02x}` (raw var number); branch=`?{T|F}{signed offset}` (T/F = on_true, no target computed). NO mnemonic name, NO operand-role sigils (@0x/obj#), NO variable naming (sp/localN/gNN), NO packed-addr unpacking, NO object/dict annotations — the whole point is to see ground truth to find bugs in the translation layer. Text-carrying ops (print/print_ret): keep the decoded string (it's not a debug-layer lookup) but flag for review. Impl: zvm add `format_instr_raw(instr, &bytes)` + `disassemble_raw(mem, start, version, lines)` in disasm.rs; Debugger add `fn disassemble_raw(&self, addr, lines) -> Vec<String>` (additive; stub 3 doubles); panel picks disassemble vs disassemble_raw by raw_mode; handle_key 'r' toggles + recompute snapshot.disasm; add 'r'/raw to DEBUG_HINTS. Note: clickable/hover naturally don't apply in raw mode (raw tokens aren't sigils) — acceptable.
