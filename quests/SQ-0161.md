---
title: Refactor main app code to be more modular and more efficient to maintain
status: open
type: feature
priority: low
created: 2026-07-03T15:27:34Z
commits: []
context: Refactor main app code to be more modular and more efficient to maintain
tags:
  area: APP
  horizon: Near
  import: babelmap
  source_file: TODO.md
---

--- note 2026-07-13T21:31:34Z ---

Scope ideas from user (2026-07-13), beyond general maintainability — the planning effort should investigate each:

1. Extract Glk into its own crate: shared by app + gvm-cli (evaluate whether zvm-cli benefits too). Audit what's missing for a FULL Glk implementation (gap analysis vs Glk spec 0.7.6).
2. Find duplicated functionality that can be combined — especially in the widget domain: panes, dialogs, prompts, etc. (Note: SQ-0289 already unified save-name onto the common dialog; the Prompt bottom-bar overlay vs dialog framework split still exists for RenameRoom etc.)
3. More elegant styling architecture — consider modeling after Textual's CSS-lite mechanism (selectors/cascade) instead of the current ColorScheme-field-per-element approach (style.rs ~2.5k lines).
4. Efficiency audit: obvious hot spots / wasted work.
5. Feature inventory: anything unsupported that should be, or implemented but no longer needed (dead features to remove).

Current shape for context: app crate ~60.6k lines; input.rs 8.7k, main.rs 8.0k, render/map.rs 5.0k, state.rs 3.9k (single AppState bag), render/transcript.rs 3.3k, style.rs 2.5k.
