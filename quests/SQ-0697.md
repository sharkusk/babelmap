---
title: 'Shogun''s opening ignores the game''s declared placement: per-line set_cursor centring is discarded when prose streams to the transcript, win2 (the menu) needs its own origin, and paint must survive win0 shrinking'
status: partial
type: bug
priority: low
created: 2026-08-09T03:49:53Z
completed: 2026-08-09T16:26:10Z
commits:
  - 9c63237e9a805a60db1453453777b045e3689acb
  - 59f55bf92324b465d9bd3675329f1ffd3f508aa2
context: |-
  branch: main
  head: ab017db6
  cwd: /Volumes/Videos/Source/babelmap

  Surfaced while comparing babelmap against Spatterlight right after closing SQ-0696 (Anchorhead's box quotes), suggesting we drop v6 window placement/centering hints Shogun relies on.
tags:
  queue: v6-compositing-2-running
---

--- note 2026-08-09T06:11:51Z ---

MEASURED 2026-08-09 — Shogun DOES declare the layout; we are not honouring the window boxes. Sequence: boot shows the GRAPHIC SPLASH (win0 and win7 both full-screen 640x400, win1 collapsed 640x0) and waits on read_char. The intro TEXT streams into win0 WHILE IT IS STILL FULL-SCREEN, so we capture it as ordinary transcript: 'SHOGUN / A Story of Japan / Copyright (c) 1988 by Infocom / ... / IBM Interpreter version 6.65 / You may choose to:'. THEN, on the first keypress, the game repositions into its real three-window layout: win1 = HEADER (47,1) 548x32 (inset ~46px each side of 640 — that inset IS the 'centred' look the user sees, and 548 = 640-2*46); win0 = STORY (47,337) 548x64, i.e. only 4 text rows, anchored at the BOTTOM (y=337..401), Lmargin=2 Rmargin=2; win2 = MENU (235,337) 169x48, opened to the RIGHT of win0's left edge. win2 carries the three menu items as PAINTED runs (per-glyph V6Text), style=1 for the selected one: y=337 'START the game', y=353 'RESTORE a saved game', y=369 'QUIT...'. That is exactly the reported Spatterlight layout — 'You may choose to:' on the left (win0's last prose line at x=47) with the 3-item menu on the right (win2 at x=235). So the placement hints are all present in the window table; the render is not using them. NOT the margin properties: Lmargin/Rmargin are 2/2 here (session.rs:240-253 already reads them, but only for the margin-PICTURE case; a later screen shows Rmargin=328 which is that feature). Likely the same class as SQ-0696: v6 text is PAINT, so text streamed into win0 while it was full-screen must stay where it was painted when win0 later shrinks to the 548x64 bottom box — plus win2 needs rendering at its own (235,337) origin rather than being folded into the story viewport.

--- note 2026-08-09T06:15:32Z ---

CENTRING RESOLVED 2026-08-09 — the game does NOT centre; Spatterlight does. Measured every header line the game emits: SHOGUN / A Story of Japan / Copyright (c) 1988 by Infocom / All rights reserved. / SHOGUN is a trademark... / Original Literary Work... / Licensed by Noble House... / Release 322 / Pix 322 / Serial number 890706 / IBM Interpreter version 6.65 / You may choose to: — EVERY ONE has leading_spaces = 0, and they arrive through the PROSE stream (win0's transcript), not as painted V6Text runs with x coordinates. So there is no padding for us to be losing and no cursor positioning for us to be ignoring. Combined with the spec check done earlier this session (ZMSD has NO centring/justification/alignment facility anywhere — sect 8.8.6.1 is the only picture text and sect 8 carries nothing on alignment), per-line centring cannot come from the story file. Also ruled out INTERPRETER NUMBER as the source: booted with none/2/3(Macintosh)/4(Amiga)/5(Atari ST)/10(Apple IIgs) — the header text is byte-identical and unpadded in every case (2 and 10 skip straight to 'You may choose to:', i.e. a shorter banner, still unpadded). So identifying as something other than IBM PC does not unlock a centred layout. CONCLUSION: same disposition as SQ-0698 — Spatterlight is applying a per-game presentation enhancement, not reading a hint we are missing. The genuinely actionable half of this quest is UNCHANGED and still stands: win2 (the menu, box (235,337) 169x48, 50 painted runs) must render at its own origin, and text painted into win0 while it was full-screen must survive win0 shrinking to the (47,337) 548x64 bottom box — that part IS the game's own declared layout.

--- note 2026-08-09T06:15:53Z ---

USER CORROBORATION 2026-08-09: Spatterlight defaults to the AMIGA interpreter, but they also ran it as IBM and got the SAME centred behaviour. That closes the interpreter-number hypothesis from the other side — they varied the interpreter inside Spatterlight and centring persisted; I varied it inside babelmap (none/2/3/4/5/10) and the emitted text stayed byte-identical and unpadded. Interpreter number is therefore not the variable, and per-line centring is confirmed as Spatterlight's own presentation layer.

--- note 2026-08-09T06:17:39Z ---

SCOPE NARROWED 2026-08-09 and retitled. OUT OF SCOPE BY DESIGN — DO NOT RE-INVESTIGATE: the per-line centring of the header. Proven twice over (see the two notes above) that the game emits no padding, the interpreter number does not change it, and the Z-machine has no alignment facility, so Spatterlight's centring is its own presentation enhancement. Same disposition as SQ-0698, which was discarded for the same reason. IN SCOPE — the game's OWN declared layout, which we are not honouring: (1) win2 = the opening menu, box (235,337) 169x48, carrying 50 painted glyph runs ('START the game' y=337 style=1 selected, 'RESTORE a saved game' y=353, 'QUIT' y=369) — must render at its own origin instead of being folded into the story viewport; that alone gives the reported prompt-left / menu-right reading, since 'You may choose to:' is win0's last prose line at x=47. (2) Text painted into win0 while it was FULL-SCREEN must survive win0 shrinking to (47,337) 548x64 — v6 text is paint (same principle as SQ-0696's box quote, one layer up). Also still unverified and worth checking first: what win1 (the header box (47,1) 548x32) actually receives after the reposition — it was EMPTY immediately after, then showed 8 paint runs a keypress later, so the visible header may be pre-reposition paint rather than a redraw. QUEUEING NOTE: overlaps SQ-0704's per-window-background work in the same v6 classify_windows/chrome compositing path — do not run both concurrently.

--- note 2026-08-09T06:21:51Z ---

*** CORRECTION 2026-08-09 — MY EARLIER 'Spatterlight enhancement' CONCLUSION WAS WRONG. DISREGARD THE TWO NOTES ABOVE THAT SAY THE CENTRING IS OUT OF SCOPE. *** User checked Apple II and DOS (IBM) walkthroughs on YouTube: BOTH show the intro text centred, on real Infocom interpreters. That falsified the enhancement theory, so I traced the boot screen ops (trace_from_boot) instead of only inspecting the emitted text — and the game centres every line ITSELF, by CURSOR POSITION rather than by padding. Per line it reads the window's own geometry and positions in PIXELS: '@get_wind_prop(win=0, prop=4) -> 49' (current y_cursor), '@get_wind_prop(win=0, prop=3) -> 640' (window WIDTH), then '@set_cursor(row=49, col=297, window=0)'. Verified the arithmetic is exact centring at 8px/char on a 640px window: SHOGUN len 6 -> col 297 (ideal 297); 'A Story of Japan' len 16 -> 257 (257); 'Copyright (c) 1988 by Infocom' len 29 -> 205 (205); 'All rights reserved.' len 20 -> 241 (241); 'SHOGUN is a trademark of James Clavell' len 38 -> 169 (169); 'Original Literary Work Copyright 1975 by James Clavell' len 54 -> 105 (105). ALL MATCH, row stepping 49/65/81/97/113/129/145 (16px = one text row). WHY I MISSED IT: I measured the emitted TEXT and found leading_spaces=0 on every line, and concluded there was no centring information. The centring is not in the text at all — it is in the set_cursor x that precedes each line, and our capture streams that text to the host transcript while discarding the pixel column. Lesson: for v6, inspect the SCREEN OPS, not just the text; 'the game emits no padding' does not mean 'the game does not position'. THE CENTRING IS THEREFORE IN SCOPE and is the same root cause as the rest of this quest: we are dropping the game's declared placement. Retitle/rescope accordingly.

--- note 2026-08-09T15:33:40Z ---

REOPENED 2026-08-09 for its second half, with the design settled by the user. The COLUMN half shipped (9c63237e): prose now carries the pixel column a set_cursor declared, and Shogun's six header lines land on their ideal columns. What did NOT ship is PLACEMENT: "You may choose to:" still sits directly under the header instead of down beside the menu.

CORRECTION TO THE SHIPPED COMMIT. Its B3 finding — "window 0 never paints, it streams, so painted text surviving a shrink does not apply here" — is WRONG, and the reasoning in that commit message should not be trusted on this point. ZMSD sect.15 is explicit that window_size/move_window do not change the current display: text already printed stays as pixels where it was drawn. Shogun's screen ops show the shape plainly — the six header lines are printed while window 0 is full-screen, then @set_cursor(row=1, col=1, window=0) and the prompt is printed after window 0 has been REPOSITIONED near the menu (win2 at 234,336). A real interpreter leaves the header painted up top and prints the prompt in the moved window. We stream both into one scrolling transcript, so they end up adjacent.

DESIGN SETTLED: freeze the prose when window 0 moves. When a wrap+scroll window is repositioned or resized, RETIRE the prose it has already emitted to a painted layer at its OLD coordinates, and restart the transcript at the new origin. The header stays where it was painted; the prompt lands with the menu.

Rejected: a full paint model for window 0 (its output painting at absolute coordinates like every other v6 window, with the transcript becoming a view over that surface). More faithful to sect.15 throughout and it would probably subsume SQ-0712's overlap too, but it rewrites every v6 render path and the scrollback with it. Revisit only if the narrow freeze proves insufficient.

Implementation notes for whoever takes it:
- zvm already models painted text: `ZWindow::texts` holds runs at absolute pixel positions and `erase_screen_rect` trims the ones an erase covers. The retirement should produce runs of that same kind, so nothing new is needed downstream to render them.
- The scrolling window's live prose is `ZWindow::prose` (SQ-0585), which the host mirrors as the transcript. That is what gets retired.
- HAZARD, found by the SQ-0707 agent: zvm's v6 erase_window never sets erase_lower_requested, so clear_anchor is never set for any v6 game, and build_main_text ignores clear_anchor even when it is. Do NOT route this through apply_game_driven_result's SQ-0407 truncate — it would destroy the scrollback.
- Guard the corpus: Zork Zero, Arthur and Journey all move and resize window 0 during play. Retiring prose on every move must not eat their transcripts.

--- note 2026-08-09T17:46:14Z ---

Reopened to partial 2026-08-09 on the user's report, after SQ-0717 landed. Their words, from a live Ghostty session (hybrid, kitty graphics):

> "shogun's starting text is centered, but the 'You may choose to:' prompt is still not next to the menu."

So the two halves have separated cleanly:
- **DONE** — the nine centred banner lines freeze as paint where the game printed them (`59f55bf9`), and they keep their centring in hybrid and frameless too (`605f9e1b`, SQ-0717).
- **NOT DONE** — the transcript's RESTART position. After freezing, Shogun moves window 0 to a small box at the bottom of the screen beside its START/RESTORE/QUIT menu (window 2) and prints "You may choose to:" into it. That prompt is not landing beside the menu. This is the "win2 (the menu) needs its own origin" clause of this quest's original title, still outstanding.

HYPOTHESIS, not measured: in hybrid the transcript is a terminal region — the story pane — rather than pixels placed at arbitrary v6 coordinates, so prose resuming at window 0's *new* small box may simply be streaming into the story pane wherever the pane already is, ignoring the origin the game moved the window to. If that is the shape, the fix is about giving the resumed transcript the window's new box, and it is adjacent to (but distinct from) SQ-0717's band-anchoring work. Verify before building on it; SQ-0712's `draw_anchored_status_band` changes are recent and touch neighbouring code.

QUEUED, not scheduled: SQ-0721's agent currently owns the repo and its likely lane (`render/screen.rs`, `render/v6_layout.rs`) overlaps. Launch after it lands. Ask the user for a `/dump-windows` at the Shogun title screen first — they have supplied those before and it names every window's box and origin, which is exactly what this needs.

--- note 2026-08-09T17:47:52Z ---

User-supplied `/dump-windows` at the Shogun title screen, 2026-08-09 (hybrid/Ghostty, pane 95x57 cells, terminal 8x18px, game sees 760x1026):

    current window 2, input window 0
    render path: cell — modal overlay open: palette
    win0  548x64 at (47,337)  [wrap scroll transcript buffered]  <- input
            game: attrs=1111, 0 paint run(s), 0 prose line(s)
            model: Buffer — the story window
    win1  548x32 at (47,1)    [buffered]   Grid — 0 runs
    win2  169x48 at (235,337) [buffered]  <- current
            game: attrs=1000, 50 paint run(s)
            model: Grid — 50 runs: "START the game" / "RESTORE a saved game" / "QUIT the game"
    win7  640x400 at (1,1)    Graphics
    ring: plan —, ring not clipped
    recent render paths (oldest first): raster x2 · cell — painted menu takeover routed here ·
      cell — modal overlay open: hotkey_dialog · cell — modal overlay open: palette x5

THE ORIGIN IS CORRECT — this kills the hypothesis in the previous note. win0 sits at **(47,337) 548x64**, and win2 (the menu) at **(235,337) 169x48**, sharing a top edge with the menu inset inside win0's horizontal span. That is exactly the layout the game intends: the prompt printing at win0's left edge, the menu box to its right. We are NOT ignoring the moved origin and we do NOT have the wrong one. The defect is downstream, in rendering.

TWO NEW LEADS, in priority order:

1. **win0 carries `0 prose line(s)` and `0 paint run(s)`.** If "You may choose to:" had been printed into win0 after the move, the streamed shadow should hold a prose line. It holds nothing. Suspect the SQ-0697 freeze is too EAGER — capturing the prompt into the retired/frozen layer along with the banner, so it renders at the coordinates it was first printed at (up with the banner) rather than travelling to the window's new box. That would match the user's words exactly: centred banner correct, prompt "still not next to the menu". FIRST measurement to take: does the game print the prompt before or after `move_window`, and which side of the freeze boundary does it land on?

2. **The path is `cell — painted menu takeover routed here`**, not the ring arm — the SQ-0477 positioned-text presentation, whose routing SQ-0711 changed. Whatever places text on that path is where win0's box must be honoured.

NOT A BUG, do not chase: every window reads `cells: NOT DRAWN this frame` because the dump was taken with a modal overlay open (the dump overlay itself). The `recent render paths` line is the useful signal, not that one.

STILL UNKNOWN, ask the user: WHERE on screen does "You may choose to:" actually appear — up near the frozen banner, or somewhere in the pane's ordinary transcript area? That single answer separates lead 1 from lead 2.

--- note 2026-08-09T17:48:35Z ---

User observation 2026-08-09, decisive: **"You may choose to: scrolled down below the window-dump text."**

That kills lead 1 (too-eager freeze) from the previous note. The prompt is not frozen at old coordinates — it is LIVE in the ordinary transcript stream, flowing and scrolling along with `/dump-windows`'s own output. Lead 2 is the live one.

So the defect is now precisely stated: after the freeze, the transcript RESUMES CORRECTLY but is presented in the pane's ordinary bottom-anchored flow instead of inside the box the game moved the story window to. The game says window 0 is a 548x64 box at (47,337) — about four rows, sitting to the LEFT of the menu at (235,337) and vertically level with it. babelmap renders that prose as the normal scrolling story pane, so the prompt lands wherever the pane's flow puts it and scrolls away with everything else.

Path involved: `cell — painted menu takeover routed here` (SQ-0477's positioned-text presentation, routing changed by SQ-0711). That path places other windows' runs at their boxes; the story window's prose is the thing not being placed.

DESIGN QUESTION, deliberately not answered here — put it to the user before any code is written (standing rule: no guessed render geometry). Making a story window's box authoritative for where prose renders is not a local change: every v6 game has a story window, and in hybrid the transcript is a terminal region rather than pixels. At minimum it needs an answer on whether a small placed story box should clip and scroll its transcript inside those four rows, and whether that applies on every render path or only where the story window has been moved away from the pane's own geometry. Blast radius includes Arthur and Zork Zero, both of which move window 0 during play.
