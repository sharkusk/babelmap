---
title: 'fmvpoker.v6: rendering wrong in both hybrid and raster — needs re-measuring now that SQ-0714 and SQ-0715 have changed its picture handling'
status: partial
type: bug
priority: low
created: 2026-08-09T18:27:18Z
commits:
  - 9c48ce297c173d6eb009615d808d160244308ad6
context: |-
  branch: main
  head: 751b4898
  cwd: /Volumes/Videos/Source/babelmap
  current: SQ-0708

  Raised by the user as still outstanding after the v6 batch landed; fmvpoker was collateral in SQ-0714 (its full-screen backdrop was being floated into the transcript, leaving a blank white raster composite) so its symptoms have almost certainly moved since the user last described them.
---

--- note 2026-08-09T18:27:34Z ---

HISTORY — this title has moved twice since the user last described it, so their original words are now unreliable as a spec.

The user's report, from BEFORE SQ-0714/0715 landed: "i'm getting some graphics, but the cards are missing in hybrid mode, and the outline is missing in raster mode." Note the setup: fmvpoker depends on Zork Zero's native picture file (renamed to FMVPOKER.EG1 in the original release); the user made it work by copying Zork0.blb alongside it, so `stories/fmvpoker.blb` is byte-identical to `stories/Zork0.blb` (both 1036226 bytes). Any measurement must account for the two games sharing one resource file — a picture id means different things to each.

WHAT CHANGED UNDER IT:
- **SQ-0714** (`69fda84c`): fmvpoker draws picture 99 at 640x480 into a 640-wide window 0 with the cursor at the origin, which looked exactly like a Zork Zero drop-cap. It was being floated into the transcript, and its raster composite was BLANK WHITE. It is now classified as a backdrop and reaches the screen; the agent's PNG showed the poker-table frame arriving.
- **SQ-0715** (`98b29882`): pictures now record the window box at the moment of the call and freeze onto the paint surface when their window moves. The agent noted fmvpoker has two painted windows, both overlapping its full-screen story Buffer, so they are skipped by the page-filling rule (SQ-0716) — worth re-checking whether that skip is still right.
- **SQ-0697** (`7e5b3b4d`): the cell paths now place the transcript at the story window's declared box. fmvpoker was byte-identical before and after in the corpus diff, so this one should not have moved it.

FIRST STEP: get a CURRENT description or screenshot from the user for both modes, then re-measure. Do not start from the old symptom list — "cards missing in hybrid, outline missing in raster" was written against a build where the backdrop never reached the screen at all.

--- note 2026-08-09T18:33:01Z ---

CURRENT STATE, from user screenshots 2026-08-09 (post SQ-0714/0715/0697). The two modes fail in exactly complementary ways.

HYBRID — text renders, NO graphics at all. Page is white. Visible: "FROBOZZ MAGIC VIDEOPOKER / by Zach Matley / Release 60 / Serial number 001227 / Inform v6.21" at top in dim grey, "Press 'i' for more information. Press any other key to begin", "Current Bet: / 10", "Total Winnings: / 1000", and a black band across the bottom carrying the menu. Not one picture is drawn.

  SECOND DEFECT VISIBLE IN THE SAME SHOT: the menu labels are CONCATENATED —
      PLAY CURRENT BETCHANGE CURRENT BETSAVERESTOREQUIT
  where the game means PLAY / CURRENT BET / CHANGE CURRENT BET / SAVE / RESTORE / QUIT as separately positioned runs. Their x coordinates are being collapsed, so runs butt against each other instead of landing at their columns. That is the same column-mapping family as SQ-0727 (advent's help bar dropping characters and clipping its tail) and should be investigated with it, not separately.

RASTER — graphics render, NO text at all. The blue frame with its two arrow tabs draws correctly; the interior is blank white. The user adds: "raster mode will show the cards if you blindly press 'p' for play" — so gameplay graphics are fine and the loss is specifically text.

THIS IS PROBABLY NOT AN FMVPOKER BUG. Raster losing text now has three independent witnesses:
- SQ-0727: advent's help navigation rows render as cells but are "missing from the RASTER composite" (SQ-0712 agent's words).
- SQ-0728: Shogun's title shows no prose in raster — the story box computes to `viewport_rows: 0`.
- this quest: fmvpoker shows no text whatsoever in raster.
Whoever picks this up should look for one cause behind all three before treating them as three bugs. If they are one, fixing it closes three quests; if they are not, the differences are diagnostic.

SETUP REMINDER: `stories/fmvpoker.blb` is a byte-identical copy of `stories/Zork0.blb` (both 1036226 bytes), because the original release ships Zork Zero's picture file renamed to FMVPOKER.EG1. The blue frame is therefore a Zork Zero picture drawn deliberately by fmvpoker, not a mix-up.

--- note 2026-08-09T19:11:04Z ---

RASTER HALF FIXED 2026-08-09 (commit 9c48ce29, worktree branch worktree-agent-a5638a57fe9a3fae0). The HYBRID half is UNTOUCHED and still open.

VERDICT on the "one cause behind all three?" question: TWO causes across the three quests, not one and not three.
- SQ-0727 (advent) + SQ-0728 (Shogun) share a root cause: the raster path used "is this pixel opaque?" as a proxy for "is there artwork here?", and rasterized glyphs are opaque. Two call sites, one mistake.
- SQ-0729 (fmvpoker) is a different mistake: `story_prose_box` measured window 0's plate by its BOUNDING BOX.

ROOT CAUSE HERE, measured. fmvpoker's poker table is a 640x400 picture that is mostly HOLE — 42830 of 256000 pixels opaque (16.7%), and only 4.0% of the middle half. SQ-0714 correctly classifies it as a backdrop and puts it on window 0's canvas; SQ-0707's `story_prose_box` then yields the whole screen to any window-0 plate whose bbox leaves no wrappable margin strip. A hollow frame's bbox is the whole screen, so `story_prose_box` returned None, `build_v6_raster_canvas` took its plate-owns-the-screen early return, and NOT ONE line of text was drawn. `raster_metrics` was None on every frame.

FIX: `story_prose_box` now measures the largest rectangle of the clear interior that the plate painted no pixel of (histogram/monotone-stack largest-rectangle sweep over the plate's alpha mask), replacing the bbox + four-margin-strips rule. Measured after:
  fmvpoker title frame  -> (24,54) 586x172
  fmvpoker menu frame   -> (22,234) 594x158   (exactly the frame's interior)
  Arthur graveyard plate-> (0,0) 28x400, below the 8-column floor -> still None, plate still owns the screen
  Journey title (100% opaque win-0 picture) -> None, unchanged
PNG after: the banner, "Press 'i' for more information…", "Current Bet: / 10", "Total Winnings: / 1000" all render inside the blue frame, cards still drawn.

STILL OPEN HERE (hybrid, not mine): (1) no graphics at all in hybrid — the page is white and not one picture is drawn; (2) the menu labels concatenate (PLAY CURRENT BETCHANGE CURRENT BETSAVERESTOREQUIT). Note for whoever takes it: at the frame where those labels appear, the window at native (22,235) 594x156 is published as a NON-PRIMARY `WinNode::Buffer` whose `lines[0]` is ALREADY the concatenated string "PLAY CURRENT BETCHANGE CURRENT BETSAVERESTOREQUIT" — so the runs are being flattened into buffer lines before any render code sees them, not collapsed by the cell mapper. That also means `build_chrome_canvas` never draws it in raster at all (it renders Graphics and Grid, never a chrome Buffer); one frame later the same window becomes an empty `Grid 74x9`, so the labels are lost on both paths for that reason too. Worth measuring before assuming it is the same column-mapping bug as SQ-0727.

--- note 2026-08-09T19:21:32Z ---

User field check after `9c48ce29` (the raster text-loss fix), 2026-08-09, verbatim:

> "729: no difference in hybrid mode. raster shows some text, but some of the frame is cuttoff and there are no text hints"

Three readings, in order of confidence:

1. **Hybrid unchanged — expected.** The hybrid half of this quest was never touched by `9c48ce29`; it is briefed to the agent running now. No new information.
2. **Raster now shows text — partial progress confirmed.** "some text" rather than "the text", so the fix landed but did not finish the job.
3. **TWO NEW RASTER SYMPTOMS**, neither previously reported: part of the FRAME ART is cut off, and "no text hints" — meaning unclear, needs the user to say which text. Candidates from their earlier hybrid screenshot: the boot instruction line "Press 'i' for more information. Press any other key to begin", the "Current Bet: / 10" and "Total Winnings: / 1000" labels, or the bottom menu bar and its "Select an option with your mouse or by typing the first letter."

Note the frame cutoff is a REGRESSION CANDIDATE, not obviously pre-existing: before `9c48ce29` the user reported raster drew "a graphic outline" with no text, and their screenshot showed the blue frame complete with both arrow tabs. If the frame is now clipped where it previously was not, the story-text box that fix introduced — `(22,234) 594x158`, derived from the plate's painted mask rather than its bounding box — is the first thing to check: a text box carved out of the frame's hole could be overdrawing the frame's own edges.

Do not treat "some of the frame is cut off" as cosmetic until that is ruled out.
