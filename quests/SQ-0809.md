---
title: 'mysterious01: at the boot frame the prompt wraps into a 16-column strip beside the title cards, because plate_free_box picks the largest-AREA free rectangle rather than the one the cursor is in'
status: open
type: bug
priority: low
created: 2026-08-12T16:53:35Z
commits: []
context: |-
  branch: main
  head: 221d5026
  cwd: /Volumes/Videos/Source/babelmap
  current: SQ-0726

  Found while fixing SQ-0722/SQ-0725: with both 512x192 title cards now on window 0's plate (rows 0..382 of a 640x400 screen), story_prose_box's largest-rectangle sweep prefers the bare right-hand column (128x400 = 51200 px, 16 text cols) over the band below the cards (640x17 = 10880 px, 80 cols x 1 row), so "Resume play on a game ?" wraps into a narrow right-hand strip instead of sitting under the cards where the game's own cursor is. Only the single boot frame is affected — from step 1 on the game erases the first card and the prose box is the full-width top band, which is correct. plate_free_box lives in crates/app/src/render/v6_layout.rs, owned by another lane during this wave, so it was deliberately left alone; the fix is likely to prefer the free rect containing window 0's text cursor, or the widest rather than the largest.
---
