---
title: Verify in-game @restore re-applies the archive's screen.json (window-config redraw)
status: open
type: bug
priority: low
created: 2026-07-09T03:02:55Z
commits: []
context: |-
  branch: main
  head: 6b39647
  cwd: /Volumes/Videos/Source/babelmap

  Flagged during SQ-0163: the screen.json restore is documented as wired for "Ctrl+R / auto-load" (archive.rs:102); confirm the in-game @restore host flow also re-applies it so a once-split game's upper window redraws after an in-game restore (host-side screen reapply, independent of the now-standard Quetzal save format).
---

--- note 2026-07-09T07:12:06Z ---

VERIFIED — NOT A BUG (2026-07-09), correct by construction. The in-game @restore path loads a bare `.qzl` (VM state only, no screen.json); the VM restore (complete_restore_success) never reads/writes machine.screen, and the resumed game redraws its own upper window on the next turn (ZMSD §8). screen.json reapply is correctly scoped to the `.babelmap` Save-State resume paths (startup auto-load, Ctrl+R, saves-manager Resumed, apply_launch_resume — all reapply it via `machine.screen = scr`). No fix needed; reapplying screen.json on the @restore path would be WRONG (no screen data in a bare .qzl; would clobber the game's redraw).

Coverage: minizork_v3_ingame_save_restore_round_trips (session.rs, NON-skippable, locks VM-state round-trip of the in-game @restore path) + bureaucracy_ingame_restore_redraws_status_grid (session.rs, v4 upper-window redraw; runs+passes when stories/bureaucr.z4 is present, skips loudly otherwise).

A NON-skippable screen-redraw test was attempted but is infeasible with committed fixtures: v3 minizork never splits an upper window, so screen.upper.cells is EMPTY (verified empirically) — there is no upper-window grid to observe or stamp. Only a v4+ split-window game (bureaucr.z4, not committed) exercises the redraw. Optional follow-up if desired: author/commit a small public-domain v4+ split-window story that reaches @save/@restore, to de-skip bureaucracy_ingame_restore_redraws_status_grid in CI.
