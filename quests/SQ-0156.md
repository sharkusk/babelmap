---
title: Glulx room detection (SP4 automapping)
status: open
type: feature
priority: low
created: 2026-07-03T15:27:34Z
commits: []
context: '[TODO-64d3ae] Glulx room detection (SP4 automapping). Glulx games get NO automapping today — `GlulxSession::current_location()` always returns `None`. A survey of 13 local Glulx stories found the reliable signal is the Inform 7 **room heading** printed in the story buffer with `GlkStyle::Subheader` (works even for FooFoo/Superluminal, whose status grid holds stats, not the room). DESIGN: detect the room as the last `Subheader` line per turn (skips the banner title), sticky across heading-less turns, name-based synthetic id (Glulx world model is opaque — no readable object tree). Needs: AppGlk to retain the raw Subheader signal (render path flattens all bold to one bit); GlulxSession to thread it into `TurnResult.location`/`current_location`; a NEW trusted `LocationMethod` (room-heading) that BYPASSES the BeyondZork NameOnly-empty-graph gate (Glulx never yields an object-backed room, so reusing NameOnly would suppress the first room forever). Self-gates pre-game menus (they emit no Subheader). Full design: docs/superpowers/specs/2026-07-02-glulx-room-detection-design.md. [automapping; app glk_backend.rs heading capture + glulx_session.rs location + LocationMethod] — completed 2026-07-02'
tags:
  import: babelmap
  source_file: COMPLETED.md
---
