---
title: Per-game style files are not truly "frozen" for fields explicitly set to terminal-default
status: open
type: feature
priority: low
created: 2026-07-03T15:27:32Z
commits: []
context: '[TODO-ddc097] Per-game style files are not truly "frozen" for fields explicitly set to terminal-default. `write_style_full`/`style_to_decl` serialize a default (None) color as an OMITTED key, and `merge_decl` is field-level (`over.fg.or(base.fg)`), so on reload a per-game field set to "default" re-inherits the GLOBAL value instead of staying default. Narrow: only fields explicitly reset to default while global sets a non-default there; all explicitly-colored fields ARE frozen and other games are unaffected. FIX options: emit an explicit `default` sentinel in `write_style_full` so the per-game override wins at merge (NOTE: also changes the global on-disk format — needs round-trip + format-preservation tests), OR document the limitation. Surfaced by the Phase 2.2 final whole-branch review. [customization; style.rs write_style_full/style_to_decl + merge] — completed 2026-07-02'
tags:
  import: babelmap
  source_file: COMPLETED.md
---
